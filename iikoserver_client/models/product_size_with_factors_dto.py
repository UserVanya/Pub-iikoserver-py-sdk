# coding: utf-8

"""
    iikoServer API

    API Server позволяет сделать интеграции, которые предназначены для работы с номенклатурой, отчетами, документами, данными сотрудников и приказами.   ### Ограничения и рекомендации - Запросы должны выполняться последовательно друг за другом - Запрашивайте данные за период не длиннее одного месяца, в идеале — за один день или неделю - Для OLAPv2-отчетов рекомендуется устанавливать build-summary=false - При построении OLAPv2-отчета рекомендуется использовать не более 7 полей - Перед выполнением запросов проверяйте их на демо сервере

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from iikoserver_client.models.product_size_factor_dto import ProductSizeFactorDto
from typing import Optional, Set, Union
from typing_extensions import Self

# XML support imports
from lxml import etree
from lxml.etree import Element, SubElement

class ProductSizeWithFactorsDto(BaseModel):
    """
    ProductSizeWithFactorsDto
    """ # noqa: E501
    name: Optional[StrictStr] = Field(description="Название размера.")
    short_name: Optional[StrictStr] = Field(default=None, description="Короткое название.", alias="shortName")
    priority: Optional[StrictInt] = Field(default=None, description="Местоположение размера в списке размеров шкалы.")
    default: Optional[StrictBool] = Field(default=None, description="Является ли размером по умолчанию для данной шкалы. У шкалы может быть не больше одного размера по умолчанию.")
    id: Optional[StrictStr] = Field(default=None, description="UUID размера.")
    deleted: Optional[StrictBool] = Field(default=None, description="Удалён или нет.")
    disabled: Optional[StrictBool] = Field(default=None, description="Доступен ли данный размер в текущем продукте")
    factors: Optional[List[ProductSizeFactorDto]] = Field(default=None, description="Коэффициенты для данного размера")
    __properties: ClassVar[List[str]] = ["name", "shortName", "priority", "default", "id", "deleted", "disabled", "factors"]
    
    # XML metadata for the class
    __xml_config: ClassVar[Dict[str, Any]] = {
        "root_name": "ProductSizeWithFactorsDto",
        
        
        "field_configs": {
            "name": {
                "xml_name": "name",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "short_name": {
                "xml_name": "shortName",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "priority": {
                "xml_name": "priority",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "default": {
                "xml_name": "default",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "id": {
                "xml_name": "id",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "deleted": {
                "xml_name": "deleted",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "disabled": {
                "xml_name": "disabled",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "factors": {
                "xml_name": "factors",
                
                
                "is_attribute": False,
                "is_wrapped": False
            }
        }
    }


    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ProductSizeWithFactorsDto from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in factors (list)
        _items = []
        if self.factors:
            for _item_factors in self.factors:
                if _item_factors:
                    _items.append(_item_factors.to_dict())
            _dict['factors'] = _items
        # set to None if name (nullable) is None
        # and model_fields_set contains the field
        if self.name is None and "name" in self.model_fields_set:
            _dict['name'] = None

        # set to None if short_name (nullable) is None
        # and model_fields_set contains the field
        if self.short_name is None and "short_name" in self.model_fields_set:
            _dict['shortName'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ProductSizeWithFactorsDto from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "name": obj.get("name"),
            "shortName": obj.get("shortName"),
            "priority": obj.get("priority"),
            "default": obj.get("default"),
            "id": obj.get("id"),
            "deleted": obj.get("deleted"),
            "disabled": obj.get("disabled"),
            "factors": [ProductSizeFactorDto.from_dict(_item) for _item in obj["factors"]] if obj.get("factors") is not None else None
        })
        return _obj



    @classmethod
    def from_xml(cls, xml_str: str) -> Optional[Self]:
        """Create an instance of ProductSizeWithFactorsDto from an XML string"""
        try:
            if isinstance(xml_str, str):
                xml_str = xml_str.encode('utf-8')
            root = etree.fromstring(xml_str)
            return cls.from_xml_element(root)
        except Exception as e:
            raise ValueError(f"Failed to parse XML: {e}")

    @classmethod
    def from_xml_element(cls, element: Element) -> Optional[Self]:
        """Create an instance of ProductSizeWithFactorsDto from an XML element"""
        return cls._from_xml_element_internal(element)

    @classmethod
    def _from_xml_element_internal(cls, element: Element) -> Optional[Self]:
        """Internal method to create instance from XML element"""
        if element is None:
            return None

        data = {}
        
        # Process field: name (XML name: name)
        # Handle XML element: name
        # Single element field: name
        child_element = element.find("name")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["name"] = text_value
                
        
        # Process field: short_name (XML name: shortName)
        # Handle XML element: short_name
        # Single element field: short_name
        child_element = element.find("shortName")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["shortName"] = text_value
                
        
        # Process field: priority (XML name: priority)
        # Handle XML element: priority
        # Single element field: priority
        child_element = element.find("priority")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["priority"] = int(text_value)
                except ValueError:
                    data["priority"] = text_value
                
        
        # Process field: default (XML name: default)
        # Handle XML element: default
        # Single element field: default
        child_element = element.find("default")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["default"] = text_value.lower() == 'true'
                
        
        # Process field: id (XML name: id)
        # Handle XML element: id
        # Single element field: id
        child_element = element.find("id")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["id"] = text_value
                
        
        # Process field: deleted (XML name: deleted)
        # Handle XML element: deleted
        # Single element field: deleted
        child_element = element.find("deleted")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["deleted"] = text_value.lower() == 'true'
                
        
        # Process field: disabled (XML name: disabled)
        # Handle XML element: disabled
        # Single element field: disabled
        child_element = element.find("disabled")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["disabled"] = text_value.lower() == 'true'
                
        
        # Process field: factors (XML name: factors)
        # Handle XML element: factors
        # Array field: factors
        # Non-wrapped array - direct elements
        # Complex items in non-wrapped array
        child_elements = element.findall("factors")
        items = []
        for child in child_elements:
            item_obj = ProductSizeFactorDto.from_xml_element(child)
            if item_obj is not None:
                items.append(item_obj)
        data["factors"] = items
        
        
        return cls.from_dict(data)

    def to_xml(self) -> str:
        """Create an XML string representation of the model"""
        root_element = Element(self.__xml_config.get("root_name", self.__class__.__name__))
        
        # Populate the XML element with model data
        self._to_xml_element_internal(root_element)
        
        # Convert to string with XML declaration
        return etree.tostring(root_element, encoding="utf-8", pretty_print=True, xml_declaration=True).decode("utf-8")

    def _to_xml_element_internal(self, element: Element) -> None:
        """Internal method to populate an XML element with model data"""
        
        # Process field: name (XML name: name)
        value = getattr(self, "name", None)
        if value is not None:  # Skip None values
            # Single element field: name
            child_element = SubElement(element, "name")
            child_element.text = value
            
        # Process field: short_name (XML name: shortName)
        value = getattr(self, "short_name", None)
        if value is not None:  # Skip None values
            # Single element field: short_name
            child_element = SubElement(element, "shortName")
            child_element.text = value
            
        # Process field: priority (XML name: priority)
        value = getattr(self, "priority", None)
        if value is not None:  # Skip None values
            # Single element field: priority
            child_element = SubElement(element, "priority")
            child_element.text = str(value)
            
        # Process field: default (XML name: default)
        value = getattr(self, "default", None)
        if value is not None:  # Skip None values
            # Single element field: default
            child_element = SubElement(element, "default")
            child_element.text = str(value).lower()
            
        # Process field: id (XML name: id)
        value = getattr(self, "id", None)
        if value is not None:  # Skip None values
            # Single element field: id
            child_element = SubElement(element, "id")
            child_element.text = value
            
        # Process field: deleted (XML name: deleted)
        value = getattr(self, "deleted", None)
        if value is not None:  # Skip None values
            # Single element field: deleted
            child_element = SubElement(element, "deleted")
            child_element.text = str(value).lower()
            
        # Process field: disabled (XML name: disabled)
        value = getattr(self, "disabled", None)
        if value is not None:  # Skip None values
            # Single element field: disabled
            child_element = SubElement(element, "disabled")
            child_element.text = str(value).lower()
            
        # Process field: factors (XML name: factors)
        value = getattr(self, "factors", None)
        if value is not None:  # Skip None values
            # Array field: factors
            # Non-wrapped array - add elements directly
            for item in value:
                # Complex items in non-wrapped array
                if hasattr(item, "_to_xml_element_internal"):
                    item_element = SubElement(element, "factors")
                    item._to_xml_element_internal(item_element)


