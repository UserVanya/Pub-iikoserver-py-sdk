# coding: utf-8

"""
    iikoServer API

    API Server позволяет сделать интеграции, которые предназначены для работы с номенклатурой, отчетами, документами, данными сотрудников и приказами.   ### Ограничения и рекомендации - Запросы должны выполняться последовательно друг за другом - Запрашивайте данные за период не длиннее одного месяца, в идеале — за один день или неделю - Для OLAPv2-отчетов рекомендуется устанавливать build-summary=false - При построении OLAPv2-отчета рекомендуется использовать не более 7 полей - Перед выполнением запросов проверяйте их на демо сервере

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt
from typing import Any, ClassVar, Dict, List, Optional
from iikoserver_client.models.error_dto import ErrorDto
from iikoserver_client.models.menu_change_document_dto import MenuChangeDocumentDto
from iikoserver_client.models.request_result_dto_enum import RequestResultDtoEnum
from typing import Optional, Set, Union
from typing_extensions import Self

# XML support imports
from lxml import etree
from lxml.etree import Element, SubElement

class MenuChangeDocumentListResponseDto(BaseModel):
    """
    MenuChangeDocumentListResponseDto
    """ # noqa: E501
    result: RequestResultDtoEnum
    errors: Optional[List[ErrorDto]] = Field(default=None, description="Список ошибок, не позволивших сделать успешную операцию.")
    response: Optional[List[MenuChangeDocumentDto]] = Field(default=None, description="Список приказов")
    revision: Optional[StrictInt] = Field(default=None, description="Максимальная ревизия, доступная для выгрузки во внешние системы на момент запроса")
    __properties: ClassVar[List[str]] = ["result", "errors", "response", "revision"]
    
    # XML metadata for the class
    __xml_config: ClassVar[Dict[str, Any]] = {
        "root_name": "MenuChangeDocumentListResponseDto",
        
        
        "field_configs": {
            "result": {
                "xml_name": "result",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "errors": {
                "xml_name": "errors",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "response": {
                "xml_name": "response",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "revision": {
                "xml_name": "revision",
                
                
                "is_attribute": False,
                "is_wrapped": False
            }
        }
    }


    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of MenuChangeDocumentListResponseDto from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in errors (list)
        _items = []
        if self.errors:
            for _item_errors in self.errors:
                if _item_errors:
                    _items.append(_item_errors.to_dict())
            _dict['errors'] = _items
        # override the default output from pydantic by calling `to_dict()` of each item in response (list)
        _items = []
        if self.response:
            for _item_response in self.response:
                if _item_response:
                    _items.append(_item_response.to_dict())
            _dict['response'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of MenuChangeDocumentListResponseDto from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "result": obj.get("result"),
            "errors": [ErrorDto.from_dict(_item) for _item in obj["errors"]] if obj.get("errors") is not None else None,
            "response": [MenuChangeDocumentDto.from_dict(_item) for _item in obj["response"]] if obj.get("response") is not None else None,
            "revision": obj.get("revision")
        })
        return _obj



    @classmethod
    def from_xml(cls, xml_str: str) -> Optional[Self]:
        """Create an instance of MenuChangeDocumentListResponseDto from an XML string"""
        try:
            if isinstance(xml_str, str):
                xml_str = xml_str.encode('utf-8')
            root = etree.fromstring(xml_str)
            return cls.from_xml_element(root)
        except Exception as e:
            raise ValueError(f"Failed to parse XML: {e}")

    @classmethod
    def from_xml_element(cls, element: Element) -> Optional[Self]:
        """Create an instance of MenuChangeDocumentListResponseDto from an XML element"""
        return cls._from_xml_element_internal(element)

    @classmethod
    def _from_xml_element_internal(cls, element: Element) -> Optional[Self]:
        """Internal method to create instance from XML element"""
        if element is None:
            return None

        data = {}
        
        # Process field: result (XML name: result)
        # Handle XML element: result
        # Single element field: result
        child_element = element.find("result")
        if child_element is not None:
            # Enum field
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["result"] = text_value
        
        # Process field: errors (XML name: errors)
        # Handle XML element: errors
        # Array field: errors
        # Non-wrapped array - direct elements
        # Complex items in non-wrapped array
        child_elements = element.findall("errors")
        items = []
        for child in child_elements:
            item_obj = ErrorDto.from_xml_element(child)
            if item_obj is not None:
                items.append(item_obj)
        data["errors"] = items
        
        # Process field: response (XML name: response)
        # Handle XML element: response
        # Array field: response
        # Non-wrapped array - direct elements
        # Complex items in non-wrapped array
        child_elements = element.findall("response")
        items = []
        for child in child_elements:
            item_obj = MenuChangeDocumentDto.from_xml_element(child)
            if item_obj is not None:
                items.append(item_obj)
        data["response"] = items
        
        # Process field: revision (XML name: revision)
        # Handle XML element: revision
        # Single element field: revision
        child_element = element.find("revision")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["revision"] = int(text_value)
                except ValueError:
                    data["revision"] = text_value
                
        
        
        return cls.from_dict(data)

    def to_xml(self) -> str:
        """Create an XML string representation of the model"""
        root_element = Element(self.__xml_config.get("root_name", self.__class__.__name__))
        
        # Populate the XML element with model data
        self._to_xml_element_internal(root_element)
        
        # Convert to string with XML declaration
        return etree.tostring(root_element, encoding="utf-8", pretty_print=True, xml_declaration=True).decode("utf-8")

    def _to_xml_element_internal(self, element: Element) -> None:
        """Internal method to populate an XML element with model data"""
        
        # Process field: result (XML name: result)
        value = getattr(self, "result", None)
        if value is not None:  # Skip None values
            # Single element field: result
            child_element = SubElement(element, "result")
            # Enum field
            child_element.text = str(value)
        # Process field: errors (XML name: errors)
        value = getattr(self, "errors", None)
        if value is not None:  # Skip None values
            # Array field: errors
            # Non-wrapped array - add elements directly
            for item in value:
                # Complex items in non-wrapped array
                if hasattr(item, "_to_xml_element_internal"):
                    item_element = SubElement(element, "errors")
                    item._to_xml_element_internal(item_element)
        # Process field: response (XML name: response)
        value = getattr(self, "response", None)
        if value is not None:  # Skip None values
            # Array field: response
            # Non-wrapped array - add elements directly
            for item in value:
                # Complex items in non-wrapped array
                if hasattr(item, "_to_xml_element_internal"):
                    item_element = SubElement(element, "response")
                    item._to_xml_element_internal(item_element)
        # Process field: revision (XML name: revision)
        value = getattr(self, "revision", None)
        if value is not None:  # Skip None values
            # Single element field: revision
            child_element = SubElement(element, "revision")
            child_element.text = str(value)
            


