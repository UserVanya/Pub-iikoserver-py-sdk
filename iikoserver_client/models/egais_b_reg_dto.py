# coding: utf-8

"""
    iikoServer API

    API Server позволяет сделать интеграции, которые предназначены для работы с номенклатурой, отчетами, документами, данными сотрудников и приказами.   ### Ограничения и рекомендации - Запросы должны выполняться последовательно друг за другом - Запрашивайте данные за период не длиннее одного месяца, в идеале — за один день или неделю - Для OLAPv2-отчетов рекомендуется устанавливать build-summary=false - При построении OLAPv2-отчета рекомендуется использовать не более 7 полей - Перед выполнением запросов проверяйте их на демо сервере

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from iikoserver_client.models.egais_mark_state_dto import EgaisMarkStateDto
from typing import Optional, Set, Union
from typing_extensions import Self

# XML support imports
from lxml import etree
from lxml.etree import Element, SubElement

class EgaisBRegDto(BaseModel):
    """
    EgaisBRegDto
    """ # noqa: E501
    source_rar_id: StrictStr = Field(description="РАР-идентификатор источника", alias="sourceRarId")
    alc_code: StrictStr = Field(description="Код алкогольной продукции", alias="alcCode")
    date_to: Optional[datetime] = Field(default=None, description="Дата-время актуальности состояния", alias="dateTo")
    marks_on_balance: Dict[str, EgaisMarkStateDto] = Field(description="Акцизные марки на балансе", alias="marksOnBalance")
    marks_written_off: Dict[str, EgaisMarkStateDto] = Field(description="Акцизные марки, списанные с баланса", alias="marksWrittenOff")
    __properties: ClassVar[List[str]] = ["sourceRarId", "alcCode", "dateTo", "marksOnBalance", "marksWrittenOff"]
    
    # XML metadata for the class
    __xml_config: ClassVar[Dict[str, Any]] = {
        "root_name": "EgaisBRegDto",
        
        
        "field_configs": {
            "source_rar_id": {
                "xml_name": "sourceRarId",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "alc_code": {
                "xml_name": "alcCode",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "date_to": {
                "xml_name": "dateTo",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "marks_on_balance": {
                "xml_name": "marksOnBalance",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "marks_written_off": {
                "xml_name": "marksWrittenOff",
                
                
                "is_attribute": False,
                "is_wrapped": False
            }
        }
    }


    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of EgaisBRegDto from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each value in marks_on_balance (dict)
        _field_dict = {}
        if self.marks_on_balance:
            for _key_marks_on_balance in self.marks_on_balance:
                if self.marks_on_balance[_key_marks_on_balance]:
                    _field_dict[_key_marks_on_balance] = self.marks_on_balance[_key_marks_on_balance].to_dict()
            _dict['marksOnBalance'] = _field_dict
        # override the default output from pydantic by calling `to_dict()` of each value in marks_written_off (dict)
        _field_dict = {}
        if self.marks_written_off:
            for _key_marks_written_off in self.marks_written_off:
                if self.marks_written_off[_key_marks_written_off]:
                    _field_dict[_key_marks_written_off] = self.marks_written_off[_key_marks_written_off].to_dict()
            _dict['marksWrittenOff'] = _field_dict
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of EgaisBRegDto from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "sourceRarId": obj.get("sourceRarId"),
            "alcCode": obj.get("alcCode"),
            "dateTo": obj.get("dateTo"),
            "marksOnBalance": dict(
                (_k, EgaisMarkStateDto.from_dict(_v))
                for _k, _v in obj["marksOnBalance"].items()
            )
            if obj.get("marksOnBalance") is not None
            else None,
            "marksWrittenOff": dict(
                (_k, EgaisMarkStateDto.from_dict(_v))
                for _k, _v in obj["marksWrittenOff"].items()
            )
            if obj.get("marksWrittenOff") is not None
            else None
        })
        return _obj



    @classmethod
    def from_xml(cls, xml_str: str) -> Optional[Self]:
        """Create an instance of EgaisBRegDto from an XML string"""
        try:
            if isinstance(xml_str, str):
                xml_str = xml_str.encode('utf-8')
            root = etree.fromstring(xml_str)
            return cls.from_xml_element(root)
        except Exception as e:
            raise ValueError(f"Failed to parse XML: {e}")

    @classmethod
    def from_xml_element(cls, element: Element) -> Optional[Self]:
        """Create an instance of EgaisBRegDto from an XML element"""
        return cls._from_xml_element_internal(element)

    @classmethod
    def _from_xml_element_internal(cls, element: Element) -> Optional[Self]:
        """Internal method to create instance from XML element"""
        if element is None:
            return None

        data = {}
        
        # Process field: source_rar_id (XML name: sourceRarId)
        # Handle XML element: source_rar_id
        # Single element field: source_rar_id
        child_element = element.find("sourceRarId")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["sourceRarId"] = text_value
                
        
        # Process field: alc_code (XML name: alcCode)
        # Handle XML element: alc_code
        # Single element field: alc_code
        child_element = element.find("alcCode")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["alcCode"] = text_value
                
        
        # Process field: date_to (XML name: dateTo)
        # Handle XML element: date_to
        # Single element field: date_to
        child_element = element.find("dateTo")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                
                # Handle other primitive types (date, datetime, uuid, etc.)
                # DateTime field - handle "null" specially  
                if text_value == "null":
                    data["dateTo"] = None
                else:
                    data["dateTo"] = text_value
                
                
        
        # Process field: marks_on_balance (XML name: marksOnBalance)
        # Handle XML element: marks_on_balance
        # Map field: marks_on_balance (not commonly used in XML)
        # This is a simplified implementation
        child_elements = element.findall("marksOnBalance")
        map_data = {}
        for child in child_elements:
            key = child.get('key') or child.tag
            value_obj = EgaisMarkStateDto.from_xml_element(child)
            if value_obj is not None:
                map_data[key] = value_obj
        data["marksOnBalance"] = map_data
        
        # Process field: marks_written_off (XML name: marksWrittenOff)
        # Handle XML element: marks_written_off
        # Map field: marks_written_off (not commonly used in XML)
        # This is a simplified implementation
        child_elements = element.findall("marksWrittenOff")
        map_data = {}
        for child in child_elements:
            key = child.get('key') or child.tag
            value_obj = EgaisMarkStateDto.from_xml_element(child)
            if value_obj is not None:
                map_data[key] = value_obj
        data["marksWrittenOff"] = map_data
        
        
        return cls.from_dict(data)

    def to_xml(self) -> str:
        """Create an XML string representation of the model"""
        root_element = Element(self.__xml_config.get("root_name", self.__class__.__name__))
        
        # Populate the XML element with model data
        self._to_xml_element_internal(root_element)
        
        # Convert to string with XML declaration
        return etree.tostring(root_element, encoding="utf-8", pretty_print=True, xml_declaration=True).decode("utf-8")

    def _to_xml_element_internal(self, element: Element) -> None:
        """Internal method to populate an XML element with model data"""
        
        # Process field: source_rar_id (XML name: sourceRarId)
        value = getattr(self, "source_rar_id", None)
        if value is not None:  # Skip None values
            # Single element field: source_rar_id
            child_element = SubElement(element, "sourceRarId")
            child_element.text = value
            
        # Process field: alc_code (XML name: alcCode)
        value = getattr(self, "alc_code", None)
        if value is not None:  # Skip None values
            # Single element field: alc_code
            child_element = SubElement(element, "alcCode")
            child_element.text = value
            
        # Process field: date_to (XML name: dateTo)
        value = getattr(self, "date_to", None)
        if value is not None:  # Skip None values
            # Single element field: date_to
            child_element = SubElement(element, "dateTo")
            
            # Handle other primitive types (date, datetime, uuid, etc.)
            child_element.text = str(value)
            
        # Process field: marks_on_balance (XML name: marksOnBalance)
        value = getattr(self, "marks_on_balance", None)
        if value is not None:  # Skip None values
            # Map field: marks_on_balance (not commonly used in XML)
            # This is a simplified implementation
            for key, item in value.items():
                map_element = SubElement(element, "marksOnBalance")
                map_element.set("key", key)
                if hasattr(item, "_to_xml_element_internal"):
                    item._to_xml_element_internal(map_element)
        # Process field: marks_written_off (XML name: marksWrittenOff)
        value = getattr(self, "marks_written_off", None)
        if value is not None:  # Skip None values
            # Map field: marks_written_off (not commonly used in XML)
            # This is a simplified implementation
            for key, item in value.items():
                map_element = SubElement(element, "marksWrittenOff")
                map_element.set("key", key)
                if hasattr(item, "_to_xml_element_internal"):
                    item._to_xml_element_internal(map_element)


