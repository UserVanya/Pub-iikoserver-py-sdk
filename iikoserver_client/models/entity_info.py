# coding: utf-8

"""
    iikoServer API

    API Server позволяет сделать интеграции, которые предназначены для работы с номенклатурой, отчетами, документами, данными сотрудников и приказами.   ### Ограничения и рекомендации - Запросы должны выполняться последовательно друг за другом - Запрашивайте данные за период не длиннее одного месяца, в идеале — за один день или неделю - Для OLAPv2-отчетов рекомендуется устанавливать build-summary=false - При построении OLAPv2-отчета рекомендуется использовать не более 7 полей - Перед выполнением запросов проверяйте их на демо сервере

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictFloat, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional, Union
from iikoserver_client.models.root_type_enum import RootTypeEnum
from typing import Optional, Set, Union
from typing_extensions import Self

# XML support imports
from lxml import etree
from lxml.etree import Element, SubElement

class EntityInfo(BaseModel):
    """
    EntityInfo
    """ # noqa: E501
    id: StrictStr = Field(description="UUID сущности")
    deleted: StrictBool = Field(description="Удален")
    name: StrictStr = Field(description="Имя")
    root_type: RootTypeEnum = Field(alias="rootType")
    code: Optional[StrictStr] = Field(default=None, description="Код объекта (в том числе артикул, табельный номер, и т.п.). Является строкой: \"1234\", \"3.04\". Может быть null.")
    short_name: Optional[StrictStr] = Field(default=None, description="Короткое название для размера (только для ProductSize).", alias="shortName")
    vat_percent: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Значение ставки НДС (только для TaxCategory).", alias="vatPercent")
    order_service_type: Optional[StrictStr] = Field(default=None, description="Режим обслуживания (только для OrderType): COMMON - Обычный заказ DELIVERY_BY_COURIER - Доставка курьером DELIVERY_PICKUP - Доставка самовывоз", alias="orderServiceType")
    default_for_service_type: Optional[StrictBool] = Field(default=None, description="У каждого из режимов обслуживания может быть выбран один тип заказа по умолчанию (только для OrderType).", alias="defaultForServiceType")
    __properties: ClassVar[List[str]] = ["id", "deleted", "name", "rootType", "code", "shortName", "vatPercent", "orderServiceType", "defaultForServiceType"]
    
    # XML metadata for the class
    __xml_config: ClassVar[Dict[str, Any]] = {
        "root_name": "EntityInfo",
        
        
        "field_configs": {
            "id": {
                "xml_name": "id",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "deleted": {
                "xml_name": "deleted",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "name": {
                "xml_name": "name",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "root_type": {
                "xml_name": "rootType",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "code": {
                "xml_name": "code",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "short_name": {
                "xml_name": "shortName",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "vat_percent": {
                "xml_name": "vatPercent",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "order_service_type": {
                "xml_name": "orderServiceType",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "default_for_service_type": {
                "xml_name": "defaultForServiceType",
                
                
                "is_attribute": False,
                "is_wrapped": False
            }
        }
    }


    @field_validator('order_service_type')
    def order_service_type_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['COMMON', 'DELIVERY_BY_COURIER', 'DELIVERY_PICKUP']):
            raise ValueError("must be one of enum values ('COMMON', 'DELIVERY_BY_COURIER', 'DELIVERY_PICKUP')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of EntityInfo from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # set to None if code (nullable) is None
        # and model_fields_set contains the field
        if self.code is None and "code" in self.model_fields_set:
            _dict['code'] = None

        # set to None if short_name (nullable) is None
        # and model_fields_set contains the field
        if self.short_name is None and "short_name" in self.model_fields_set:
            _dict['shortName'] = None

        # set to None if vat_percent (nullable) is None
        # and model_fields_set contains the field
        if self.vat_percent is None and "vat_percent" in self.model_fields_set:
            _dict['vatPercent'] = None

        # set to None if order_service_type (nullable) is None
        # and model_fields_set contains the field
        if self.order_service_type is None and "order_service_type" in self.model_fields_set:
            _dict['orderServiceType'] = None

        # set to None if default_for_service_type (nullable) is None
        # and model_fields_set contains the field
        if self.default_for_service_type is None and "default_for_service_type" in self.model_fields_set:
            _dict['defaultForServiceType'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of EntityInfo from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "id": obj.get("id"),
            "deleted": obj.get("deleted"),
            "name": obj.get("name"),
            "rootType": obj.get("rootType"),
            "code": obj.get("code"),
            "shortName": obj.get("shortName"),
            "vatPercent": obj.get("vatPercent"),
            "orderServiceType": obj.get("orderServiceType"),
            "defaultForServiceType": obj.get("defaultForServiceType")
        })
        return _obj



    @classmethod
    def from_xml(cls, xml_str: str) -> Optional[Self]:
        """Create an instance of EntityInfo from an XML string"""
        try:
            if isinstance(xml_str, str):
                xml_str = xml_str.encode('utf-8')
            root = etree.fromstring(xml_str)
            return cls.from_xml_element(root)
        except Exception as e:
            raise ValueError(f"Failed to parse XML: {e}")

    @classmethod
    def from_xml_element(cls, element: Element) -> Optional[Self]:
        """Create an instance of EntityInfo from an XML element"""
        return cls._from_xml_element_internal(element)

    @classmethod
    def _from_xml_element_internal(cls, element: Element) -> Optional[Self]:
        """Internal method to create instance from XML element"""
        if element is None:
            return None

        data = {}
        
        # Process field: id (XML name: id)
        # Handle XML element: id
        # Single element field: id
        child_element = element.find("id")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["id"] = text_value
                
        
        # Process field: deleted (XML name: deleted)
        # Handle XML element: deleted
        # Single element field: deleted
        child_element = element.find("deleted")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["deleted"] = text_value.lower() == 'true'
                
        
        # Process field: name (XML name: name)
        # Handle XML element: name
        # Single element field: name
        child_element = element.find("name")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["name"] = text_value
                
        
        # Process field: root_type (XML name: rootType)
        # Handle XML element: root_type
        # Single element field: root_type
        child_element = element.find("rootType")
        if child_element is not None:
            # Enum field
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["rootType"] = text_value
        
        # Process field: code (XML name: code)
        # Handle XML element: code
        # Single element field: code
        child_element = element.find("code")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["code"] = text_value
                
        
        # Process field: short_name (XML name: shortName)
        # Handle XML element: short_name
        # Single element field: short_name
        child_element = element.find("shortName")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["shortName"] = text_value
                
        
        # Process field: vat_percent (XML name: vatPercent)
        # Handle XML element: vat_percent
        # Single element field: vat_percent
        child_element = element.find("vatPercent")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["vatPercent"] = float(text_value)
                except ValueError:
                    data["vatPercent"] = text_value
                
        
        # Process field: order_service_type (XML name: orderServiceType)
        # Handle XML element: order_service_type
        # Single element field: order_service_type
        child_element = element.find("orderServiceType")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["orderServiceType"] = text_value
                
        
        # Process field: default_for_service_type (XML name: defaultForServiceType)
        # Handle XML element: default_for_service_type
        # Single element field: default_for_service_type
        child_element = element.find("defaultForServiceType")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["defaultForServiceType"] = text_value.lower() == 'true'
                
        
        
        return cls.from_dict(data)

    def to_xml(self) -> str:
        """Create an XML string representation of the model"""
        root_element = Element(self.__xml_config.get("root_name", self.__class__.__name__))
        
        # Populate the XML element with model data
        self._to_xml_element_internal(root_element)
        
        # Convert to string with XML declaration
        return etree.tostring(root_element, encoding="utf-8", pretty_print=True, xml_declaration=True).decode("utf-8")

    def _to_xml_element_internal(self, element: Element) -> None:
        """Internal method to populate an XML element with model data"""
        
        # Process field: id (XML name: id)
        value = getattr(self, "id", None)
        if value is not None:  # Skip None values
            # Single element field: id
            child_element = SubElement(element, "id")
            child_element.text = value
            
        # Process field: deleted (XML name: deleted)
        value = getattr(self, "deleted", None)
        if value is not None:  # Skip None values
            # Single element field: deleted
            child_element = SubElement(element, "deleted")
            child_element.text = str(value).lower()
            
        # Process field: name (XML name: name)
        value = getattr(self, "name", None)
        if value is not None:  # Skip None values
            # Single element field: name
            child_element = SubElement(element, "name")
            child_element.text = value
            
        # Process field: root_type (XML name: rootType)
        value = getattr(self, "root_type", None)
        if value is not None:  # Skip None values
            # Single element field: root_type
            child_element = SubElement(element, "rootType")
            # Enum field
            child_element.text = str(value)
        # Process field: code (XML name: code)
        value = getattr(self, "code", None)
        if value is not None:  # Skip None values
            # Single element field: code
            child_element = SubElement(element, "code")
            child_element.text = value
            
        # Process field: short_name (XML name: shortName)
        value = getattr(self, "short_name", None)
        if value is not None:  # Skip None values
            # Single element field: short_name
            child_element = SubElement(element, "shortName")
            child_element.text = value
            
        # Process field: vat_percent (XML name: vatPercent)
        value = getattr(self, "vat_percent", None)
        if value is not None:  # Skip None values
            # Single element field: vat_percent
            child_element = SubElement(element, "vatPercent")
            child_element.text = str(value)
            
        # Process field: order_service_type (XML name: orderServiceType)
        value = getattr(self, "order_service_type", None)
        if value is not None:  # Skip None values
            # Single element field: order_service_type
            child_element = SubElement(element, "orderServiceType")
            child_element.text = value
            
        # Process field: default_for_service_type (XML name: defaultForServiceType)
        value = getattr(self, "default_for_service_type", None)
        if value is not None:  # Skip None values
            # Single element field: default_for_service_type
            child_element = SubElement(element, "defaultForServiceType")
            child_element.text = str(value).lower()
            


