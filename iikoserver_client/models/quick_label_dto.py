# coding: utf-8

"""
    iikoServer API

    API Server позволяет сделать интеграции, которые предназначены для работы с номенклатурой, отчетами, документами, данными сотрудников и приказами.   ### Ограничения и рекомендации - Запросы должны выполняться последовательно друг за другом - Запрашивайте данные за период не длиннее одного месяца, в идеале — за один день или неделю - Для OLAPv2-отчетов рекомендуется устанавливать build-summary=false - При построении OLAPv2-отчета рекомендуется использовать не более 7 полей - Перед выполнением запросов проверяйте их на демо сервере

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from iikoserver_client.models.quick_label_entity_type_enum import QuickLabelEntityTypeEnum
from typing import Optional, Set, Union
from typing_extensions import Self

# XML support imports
from lxml import etree
from lxml.etree import Element, SubElement

class QuickLabelDto(BaseModel):
    """
    Ячейка быстрого меню
    """ # noqa: E501
    day: Optional[Annotated[int, Field(le=6, strict=True, ge=0)]] = Field(default=None, description="День недели. Понедельник - 0, Воскресение - 6")
    page: Annotated[int, Field(le=2, strict=True, ge=0)] = Field(description="Номер страницы. Допустимые значения: 0, 1, 2")
    x: Annotated[int, Field(le=2, strict=True, ge=0)] = Field(description="Х-координата ячейки. Допустимые значения: 0, 1, 2")
    y: Annotated[int, Field(le=7, strict=True, ge=0)] = Field(description="Y-координата ячейки. Допустимые значения: 0, 1, 2, 3, 4, 5, 6, 7")
    entity_id: StrictStr = Field(description="UUID сущности", alias="entityId")
    entity_type: Optional[QuickLabelEntityTypeEnum] = Field(default=None, alias="entityType")
    __properties: ClassVar[List[str]] = ["day", "page", "x", "y", "entityId", "entityType"]
    
    # XML metadata for the class
    __xml_config: ClassVar[Dict[str, Any]] = {
        "root_name": "QuickLabelDto",
        
        
        "field_configs": {
            "day": {
                "xml_name": "day",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "page": {
                "xml_name": "page",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "x": {
                "xml_name": "x",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "y": {
                "xml_name": "y",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "entity_id": {
                "xml_name": "entityId",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "entity_type": {
                "xml_name": "entityType",
                
                
                "is_attribute": False,
                "is_wrapped": False
            }
        }
    }


    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of QuickLabelDto from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # set to None if day (nullable) is None
        # and model_fields_set contains the field
        if self.day is None and "day" in self.model_fields_set:
            _dict['day'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of QuickLabelDto from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "day": obj.get("day"),
            "page": obj.get("page"),
            "x": obj.get("x"),
            "y": obj.get("y"),
            "entityId": obj.get("entityId"),
            "entityType": obj.get("entityType")
        })
        return _obj



    @classmethod
    def from_xml(cls, xml_str: str) -> Optional[Self]:
        """Create an instance of QuickLabelDto from an XML string"""
        try:
            if isinstance(xml_str, str):
                xml_str = xml_str.encode('utf-8')
            root = etree.fromstring(xml_str)
            return cls.from_xml_element(root)
        except Exception as e:
            raise ValueError(f"Failed to parse XML: {e}")

    @classmethod
    def from_xml_element(cls, element: Element) -> Optional[Self]:
        """Create an instance of QuickLabelDto from an XML element"""
        return cls._from_xml_element_internal(element)

    @classmethod
    def _from_xml_element_internal(cls, element: Element) -> Optional[Self]:
        """Internal method to create instance from XML element"""
        if element is None:
            return None

        data = {}
        
        # Process field: day (XML name: day)
        # Handle XML element: day
        # Single element field: day
        child_element = element.find("day")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["day"] = int(text_value)
                except ValueError:
                    data["day"] = text_value
                
        
        # Process field: page (XML name: page)
        # Handle XML element: page
        # Single element field: page
        child_element = element.find("page")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["page"] = int(text_value)
                except ValueError:
                    data["page"] = text_value
                
        
        # Process field: x (XML name: x)
        # Handle XML element: x
        # Single element field: x
        child_element = element.find("x")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["x"] = int(text_value)
                except ValueError:
                    data["x"] = text_value
                
        
        # Process field: y (XML name: y)
        # Handle XML element: y
        # Single element field: y
        child_element = element.find("y")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["y"] = int(text_value)
                except ValueError:
                    data["y"] = text_value
                
        
        # Process field: entity_id (XML name: entityId)
        # Handle XML element: entity_id
        # Single element field: entity_id
        child_element = element.find("entityId")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["entityId"] = text_value
                
        
        # Process field: entity_type (XML name: entityType)
        # Handle XML element: entity_type
        # Single element field: entity_type
        child_element = element.find("entityType")
        if child_element is not None:
            # Enum field
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["entityType"] = text_value
        
        
        return cls.from_dict(data)

    def to_xml(self) -> str:
        """Create an XML string representation of the model"""
        root_element = Element(self.__xml_config.get("root_name", self.__class__.__name__))
        
        # Populate the XML element with model data
        self._to_xml_element_internal(root_element)
        
        # Convert to string with XML declaration
        return etree.tostring(root_element, encoding="utf-8", pretty_print=True, xml_declaration=True).decode("utf-8")

    def _to_xml_element_internal(self, element: Element) -> None:
        """Internal method to populate an XML element with model data"""
        
        # Process field: day (XML name: day)
        value = getattr(self, "day", None)
        if value is not None:  # Skip None values
            # Single element field: day
            child_element = SubElement(element, "day")
            child_element.text = str(value)
            
        # Process field: page (XML name: page)
        value = getattr(self, "page", None)
        if value is not None:  # Skip None values
            # Single element field: page
            child_element = SubElement(element, "page")
            child_element.text = str(value)
            
        # Process field: x (XML name: x)
        value = getattr(self, "x", None)
        if value is not None:  # Skip None values
            # Single element field: x
            child_element = SubElement(element, "x")
            child_element.text = str(value)
            
        # Process field: y (XML name: y)
        value = getattr(self, "y", None)
        if value is not None:  # Skip None values
            # Single element field: y
            child_element = SubElement(element, "y")
            child_element.text = str(value)
            
        # Process field: entity_id (XML name: entityId)
        value = getattr(self, "entity_id", None)
        if value is not None:  # Skip None values
            # Single element field: entity_id
            child_element = SubElement(element, "entityId")
            child_element.text = value
            
        # Process field: entity_type (XML name: entityType)
        value = getattr(self, "entity_type", None)
        if value is not None:  # Skip None values
            # Single element field: entity_type
            child_element = SubElement(element, "entityType")
            # Enum field
            child_element.text = str(value)


