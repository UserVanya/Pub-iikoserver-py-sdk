# coding: utf-8

"""
    iikoServer API

    API Server позволяет сделать интеграции, которые предназначены для работы с номенклатурой, отчетами, документами, данными сотрудников и приказами.   ### Ограничения и рекомендации - Запросы должны выполняться последовательно друг за другом - Запрашивайте данные за период не длиннее одного месяца, в идеале — за один день или неделю - Для OLAPv2-отчетов рекомендуется устанавливать build-summary=false - При построении OLAPv2-отчета рекомендуется использовать не более 7 полей - Перед выполнением запросов проверяйте их на демо сервере

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set, Union
from typing_extensions import Self

# XML support imports
from lxml import etree
from lxml.etree import Element, SubElement

class ReplicationStatusDto(BaseModel):
    """
    Статус репликации
    """ # noqa: E501
    department_id: Optional[StrictStr] = Field(default=None, description="Идентификатор департамента", alias="departmentId")
    department_name: Optional[StrictStr] = Field(default=None, description="Название департамента", alias="departmentName")
    last_receive_date: Optional[datetime] = Field(default=None, description="Время последнего получения данных", alias="lastReceiveDate")
    last_send_date: Optional[datetime] = Field(default=None, description="Время последней отправки данных", alias="lastSendDate")
    status: Optional[StrictStr] = Field(default=None, description="Статус репликации")
    __properties: ClassVar[List[str]] = ["departmentId", "departmentName", "lastReceiveDate", "lastSendDate", "status"]
    
    # XML metadata for the class
    __xml_config: ClassVar[Dict[str, Any]] = {
        "root_name": "replicationStatusDto",
        
        
        "field_configs": {
            "department_id": {
                "xml_name": "departmentId",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "department_name": {
                "xml_name": "departmentName",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "last_receive_date": {
                "xml_name": "lastReceiveDate",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "last_send_date": {
                "xml_name": "lastSendDate",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "status": {
                "xml_name": "status",
                
                
                "is_attribute": False,
                "is_wrapped": False
            }
        }
    }


    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ReplicationStatusDto from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # set to None if department_id (nullable) is None
        # and model_fields_set contains the field
        if self.department_id is None and "department_id" in self.model_fields_set:
            _dict['departmentId'] = None

        # set to None if department_name (nullable) is None
        # and model_fields_set contains the field
        if self.department_name is None and "department_name" in self.model_fields_set:
            _dict['departmentName'] = None

        # set to None if last_receive_date (nullable) is None
        # and model_fields_set contains the field
        if self.last_receive_date is None and "last_receive_date" in self.model_fields_set:
            _dict['lastReceiveDate'] = None

        # set to None if last_send_date (nullable) is None
        # and model_fields_set contains the field
        if self.last_send_date is None and "last_send_date" in self.model_fields_set:
            _dict['lastSendDate'] = None

        # set to None if status (nullable) is None
        # and model_fields_set contains the field
        if self.status is None and "status" in self.model_fields_set:
            _dict['status'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ReplicationStatusDto from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "departmentId": obj.get("departmentId"),
            "departmentName": obj.get("departmentName"),
            "lastReceiveDate": obj.get("lastReceiveDate"),
            "lastSendDate": obj.get("lastSendDate"),
            "status": obj.get("status")
        })
        return _obj



    @classmethod
    def from_xml(cls, xml_str: str) -> Optional[Self]:
        """Create an instance of ReplicationStatusDto from an XML string"""
        try:
            if isinstance(xml_str, str):
                xml_str = xml_str.encode('utf-8')
            root = etree.fromstring(xml_str)
            return cls.from_xml_element(root)
        except Exception as e:
            raise ValueError(f"Failed to parse XML: {e}")

    @classmethod
    def from_xml_element(cls, element: Element) -> Optional[Self]:
        """Create an instance of ReplicationStatusDto from an XML element"""
        return cls._from_xml_element_internal(element)

    @classmethod
    def _from_xml_element_internal(cls, element: Element) -> Optional[Self]:
        """Internal method to create instance from XML element"""
        if element is None:
            return None

        data = {}
        
        # Process field: department_id (XML name: departmentId)
        # Handle XML element: department_id
        # Single element field: department_id
        child_element = element.find("departmentId")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["departmentId"] = text_value
                
        
        # Process field: department_name (XML name: departmentName)
        # Handle XML element: department_name
        # Single element field: department_name
        child_element = element.find("departmentName")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["departmentName"] = text_value
                
        
        # Process field: last_receive_date (XML name: lastReceiveDate)
        # Handle XML element: last_receive_date
        # Single element field: last_receive_date
        child_element = element.find("lastReceiveDate")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                
                # Handle other primitive types (date, datetime, uuid, etc.)
                # DateTime field - handle "null" specially  
                if text_value == "null":
                    data["lastReceiveDate"] = None
                else:
                    data["lastReceiveDate"] = text_value
                
                
        
        # Process field: last_send_date (XML name: lastSendDate)
        # Handle XML element: last_send_date
        # Single element field: last_send_date
        child_element = element.find("lastSendDate")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                
                # Handle other primitive types (date, datetime, uuid, etc.)
                # DateTime field - handle "null" specially  
                if text_value == "null":
                    data["lastSendDate"] = None
                else:
                    data["lastSendDate"] = text_value
                
                
        
        # Process field: status (XML name: status)
        # Handle XML element: status
        # Single element field: status
        child_element = element.find("status")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["status"] = text_value
                
        
        
        return cls.from_dict(data)

    def to_xml(self) -> str:
        """Create an XML string representation of the model"""
        root_element = Element(self.__xml_config.get("root_name", self.__class__.__name__))
        
        # Populate the XML element with model data
        self._to_xml_element_internal(root_element)
        
        # Convert to string with XML declaration
        return etree.tostring(root_element, encoding="utf-8", pretty_print=True, xml_declaration=True).decode("utf-8")

    def _to_xml_element_internal(self, element: Element) -> None:
        """Internal method to populate an XML element with model data"""
        
        # Process field: department_id (XML name: departmentId)
        value = getattr(self, "department_id", None)
        if value is not None:  # Skip None values
            # Single element field: department_id
            child_element = SubElement(element, "departmentId")
            child_element.text = value
            
        # Process field: department_name (XML name: departmentName)
        value = getattr(self, "department_name", None)
        if value is not None:  # Skip None values
            # Single element field: department_name
            child_element = SubElement(element, "departmentName")
            child_element.text = value
            
        # Process field: last_receive_date (XML name: lastReceiveDate)
        value = getattr(self, "last_receive_date", None)
        if value is not None:  # Skip None values
            # Single element field: last_receive_date
            child_element = SubElement(element, "lastReceiveDate")
            
            # Handle other primitive types (date, datetime, uuid, etc.)
            child_element.text = str(value)
            
        # Process field: last_send_date (XML name: lastSendDate)
        value = getattr(self, "last_send_date", None)
        if value is not None:  # Skip None values
            # Single element field: last_send_date
            child_element = SubElement(element, "lastSendDate")
            
            # Handle other primitive types (date, datetime, uuid, etc.)
            child_element.text = str(value)
            
        # Process field: status (XML name: status)
        value = getattr(self, "status", None)
        if value is not None:  # Skip None values
            # Single element field: status
            child_element = SubElement(element, "status")
            child_element.text = value
            


