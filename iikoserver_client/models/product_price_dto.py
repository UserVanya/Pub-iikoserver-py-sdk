# coding: utf-8

"""
    iikoServer API

    API Server позволяет сделать интеграции, которые предназначены для работы с номенклатурой, отчетами, документами, данными сотрудников и приказами.   ### Ограничения и рекомендации - Запросы должны выполняться последовательно друг за другом - Запрашивайте данные за период не длиннее одного месяца, в идеале — за один день или неделю - Для OLAPv2-отчетов рекомендуется устанавливать build-summary=false - При построении OLAPv2-отчета рекомендуется использовать не более 7 полей - Перед выполнением запросов проверяйте их на демо сервере

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from iikoserver_client.models.product_price_item_dto import ProductPriceItemDto
from typing import Optional, Set, Union
from typing_extensions import Self

# XML support imports
from lxml import etree
from lxml.etree import Element, SubElement

class ProductPriceDto(BaseModel):
    """
    Цены продукта
    """ # noqa: E501
    department_id: Optional[StrictStr] = Field(default=None, description="Идентификатор департамента, в котором осуществляется продажа данного продукта", alias="departmentId")
    product_id: Optional[StrictStr] = Field(default=None, description="Идентификатор продукта", alias="productId")
    product_size_id: Optional[StrictStr] = Field(default=None, description="Идентификатор размера продукта", alias="productSizeId")
    prices: Optional[List[ProductPriceItemDto]] = Field(default=None, description="Список цен на интервалах для данного продукта")
    __properties: ClassVar[List[str]] = ["departmentId", "productId", "productSizeId", "prices"]
    
    # XML metadata for the class
    __xml_config: ClassVar[Dict[str, Any]] = {
        "root_name": "ProductPriceDto",
        
        
        "field_configs": {
            "department_id": {
                "xml_name": "departmentId",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "product_id": {
                "xml_name": "productId",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "product_size_id": {
                "xml_name": "productSizeId",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "prices": {
                "xml_name": "prices",
                
                
                "is_attribute": False,
                "is_wrapped": False
            }
        }
    }


    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ProductPriceDto from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in prices (list)
        _items = []
        if self.prices:
            for _item_prices in self.prices:
                if _item_prices:
                    _items.append(_item_prices.to_dict())
            _dict['prices'] = _items
        # set to None if product_size_id (nullable) is None
        # and model_fields_set contains the field
        if self.product_size_id is None and "product_size_id" in self.model_fields_set:
            _dict['productSizeId'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ProductPriceDto from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "departmentId": obj.get("departmentId"),
            "productId": obj.get("productId"),
            "productSizeId": obj.get("productSizeId"),
            "prices": [ProductPriceItemDto.from_dict(_item) for _item in obj["prices"]] if obj.get("prices") is not None else None
        })
        return _obj



    @classmethod
    def from_xml(cls, xml_str: str) -> Optional[Self]:
        """Create an instance of ProductPriceDto from an XML string"""
        try:
            if isinstance(xml_str, str):
                xml_str = xml_str.encode('utf-8')
            root = etree.fromstring(xml_str)
            return cls.from_xml_element(root)
        except Exception as e:
            raise ValueError(f"Failed to parse XML: {e}")

    @classmethod
    def from_xml_element(cls, element: Element) -> Optional[Self]:
        """Create an instance of ProductPriceDto from an XML element"""
        return cls._from_xml_element_internal(element)

    @classmethod
    def _from_xml_element_internal(cls, element: Element) -> Optional[Self]:
        """Internal method to create instance from XML element"""
        if element is None:
            return None

        data = {}
        
        # Process field: department_id (XML name: departmentId)
        # Handle XML element: department_id
        # Single element field: department_id
        child_element = element.find("departmentId")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["departmentId"] = text_value
                
        
        # Process field: product_id (XML name: productId)
        # Handle XML element: product_id
        # Single element field: product_id
        child_element = element.find("productId")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["productId"] = text_value
                
        
        # Process field: product_size_id (XML name: productSizeId)
        # Handle XML element: product_size_id
        # Single element field: product_size_id
        child_element = element.find("productSizeId")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["productSizeId"] = text_value
                
        
        # Process field: prices (XML name: prices)
        # Handle XML element: prices
        # Array field: prices
        # Non-wrapped array - direct elements
        # Complex items in non-wrapped array
        child_elements = element.findall("prices")
        items = []
        for child in child_elements:
            item_obj = ProductPriceItemDto.from_xml_element(child)
            if item_obj is not None:
                items.append(item_obj)
        data["prices"] = items
        
        
        return cls.from_dict(data)

    def to_xml(self) -> str:
        """Create an XML string representation of the model"""
        root_element = Element(self.__xml_config.get("root_name", self.__class__.__name__))
        
        # Populate the XML element with model data
        self._to_xml_element_internal(root_element)
        
        # Convert to string with XML declaration
        return etree.tostring(root_element, encoding="utf-8", pretty_print=True, xml_declaration=True).decode("utf-8")

    def _to_xml_element_internal(self, element: Element) -> None:
        """Internal method to populate an XML element with model data"""
        
        # Process field: department_id (XML name: departmentId)
        value = getattr(self, "department_id", None)
        if value is not None:  # Skip None values
            # Single element field: department_id
            child_element = SubElement(element, "departmentId")
            child_element.text = value
            
        # Process field: product_id (XML name: productId)
        value = getattr(self, "product_id", None)
        if value is not None:  # Skip None values
            # Single element field: product_id
            child_element = SubElement(element, "productId")
            child_element.text = value
            
        # Process field: product_size_id (XML name: productSizeId)
        value = getattr(self, "product_size_id", None)
        if value is not None:  # Skip None values
            # Single element field: product_size_id
            child_element = SubElement(element, "productSizeId")
            child_element.text = value
            
        # Process field: prices (XML name: prices)
        value = getattr(self, "prices", None)
        if value is not None:  # Skip None values
            # Array field: prices
            # Non-wrapped array - add elements directly
            for item in value:
                # Complex items in non-wrapped array
                if hasattr(item, "_to_xml_element_internal"):
                    item_element = SubElement(element, "prices")
                    item._to_xml_element_internal(item_element)


