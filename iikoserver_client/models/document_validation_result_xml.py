# coding: utf-8

"""
    iikoServer API

    API Server позволяет сделать интеграции, которые предназначены для работы с номенклатурой, отчетами, документами, данными сотрудников и приказами.   ### Ограничения и рекомендации - Запросы должны выполняться последовательно друг за другом - Запрашивайте данные за период не длиннее одного месяца, в идеале — за один день или неделю - Для OLAPv2-отчетов рекомендуется устанавливать build-summary=false - При построении OLAPv2-отчета рекомендуется использовать не более 7 полей - Перед выполнением запросов проверяйте их на демо сервере

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set, Union
from typing_extensions import Self

# XML support imports
from lxml import etree
from lxml.etree import Element, SubElement

class DocumentValidationResultXml(BaseModel):
    """
    DocumentValidationResultXml
    """ # noqa: E501
    valid: StrictBool = Field(description="Результат валидации.")
    warning: StrictBool = Field(description="Указывает на то, что ошибка не критичная и служит в качестве предупреждения.")
    document_number: Optional[StrictStr] = Field(default=None, description="Номер валидируемого документа.", alias="documentNumber")
    other_suggested_number: Optional[StrictStr] = Field(default=None, description="Новый номер для документа. Отличен от null, если старый нарушает уникальность или не изменились влияющие на номер поля.", alias="otherSuggestedNumber")
    error_message: Optional[StrictStr] = Field(default=None, description="Текст ошибки (или только заголовок, если задано additionalInfo). Предназначен для показа пользователю, но в REST API не всегда локализован.", alias="errorMessage")
    additional_info: Optional[StrictStr] = Field(default=None, description="Для невалидного результата может быть указана дополнительная информация, содержащая детали ошибки.", alias="additionalInfo")
    __properties: ClassVar[List[str]] = ["valid", "warning", "documentNumber", "otherSuggestedNumber", "errorMessage", "additionalInfo"]
    
    # XML metadata for the class
    __xml_config: ClassVar[Dict[str, Any]] = {
        "root_name": "documentValidationResult",
        
        
        "field_configs": {
            "valid": {
                "xml_name": "valid",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "warning": {
                "xml_name": "warning",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "document_number": {
                "xml_name": "documentNumber",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "other_suggested_number": {
                "xml_name": "otherSuggestedNumber",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "error_message": {
                "xml_name": "errorMessage",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "additional_info": {
                "xml_name": "additionalInfo",
                
                
                "is_attribute": False,
                "is_wrapped": False
            }
        }
    }


    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of DocumentValidationResultXml from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # set to None if document_number (nullable) is None
        # and model_fields_set contains the field
        if self.document_number is None and "document_number" in self.model_fields_set:
            _dict['documentNumber'] = None

        # set to None if other_suggested_number (nullable) is None
        # and model_fields_set contains the field
        if self.other_suggested_number is None and "other_suggested_number" in self.model_fields_set:
            _dict['otherSuggestedNumber'] = None

        # set to None if error_message (nullable) is None
        # and model_fields_set contains the field
        if self.error_message is None and "error_message" in self.model_fields_set:
            _dict['errorMessage'] = None

        # set to None if additional_info (nullable) is None
        # and model_fields_set contains the field
        if self.additional_info is None and "additional_info" in self.model_fields_set:
            _dict['additionalInfo'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of DocumentValidationResultXml from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "valid": obj.get("valid"),
            "warning": obj.get("warning"),
            "documentNumber": obj.get("documentNumber"),
            "otherSuggestedNumber": obj.get("otherSuggestedNumber"),
            "errorMessage": obj.get("errorMessage"),
            "additionalInfo": obj.get("additionalInfo")
        })
        return _obj



    @classmethod
    def from_xml(cls, xml_str: str) -> Optional[Self]:
        """Create an instance of DocumentValidationResultXml from an XML string"""
        try:
            if isinstance(xml_str, str):
                xml_str = xml_str.encode('utf-8')
            root = etree.fromstring(xml_str)
            return cls.from_xml_element(root)
        except Exception as e:
            raise ValueError(f"Failed to parse XML: {e}")

    @classmethod
    def from_xml_element(cls, element: Element) -> Optional[Self]:
        """Create an instance of DocumentValidationResultXml from an XML element"""
        return cls._from_xml_element_internal(element)

    @classmethod
    def _from_xml_element_internal(cls, element: Element) -> Optional[Self]:
        """Internal method to create instance from XML element"""
        if element is None:
            return None

        data = {}
        
        # Process field: valid (XML name: valid)
        # Handle XML element: valid
        # Single element field: valid
        child_element = element.find("valid")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["valid"] = text_value.lower() == 'true'
                
        
        # Process field: warning (XML name: warning)
        # Handle XML element: warning
        # Single element field: warning
        child_element = element.find("warning")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["warning"] = text_value.lower() == 'true'
                
        
        # Process field: document_number (XML name: documentNumber)
        # Handle XML element: document_number
        # Single element field: document_number
        child_element = element.find("documentNumber")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["documentNumber"] = text_value
                
        
        # Process field: other_suggested_number (XML name: otherSuggestedNumber)
        # Handle XML element: other_suggested_number
        # Single element field: other_suggested_number
        child_element = element.find("otherSuggestedNumber")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["otherSuggestedNumber"] = text_value
                
        
        # Process field: error_message (XML name: errorMessage)
        # Handle XML element: error_message
        # Single element field: error_message
        child_element = element.find("errorMessage")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["errorMessage"] = text_value
                
        
        # Process field: additional_info (XML name: additionalInfo)
        # Handle XML element: additional_info
        # Single element field: additional_info
        child_element = element.find("additionalInfo")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["additionalInfo"] = text_value
                
        
        
        return cls.from_dict(data)

    def to_xml(self) -> str:
        """Create an XML string representation of the model"""
        root_element = Element(self.__xml_config.get("root_name", self.__class__.__name__))
        
        # Populate the XML element with model data
        self._to_xml_element_internal(root_element)
        
        # Convert to string with XML declaration
        return etree.tostring(root_element, encoding="utf-8", pretty_print=True, xml_declaration=True).decode("utf-8")

    def _to_xml_element_internal(self, element: Element) -> None:
        """Internal method to populate an XML element with model data"""
        
        # Process field: valid (XML name: valid)
        value = getattr(self, "valid", None)
        if value is not None:  # Skip None values
            # Single element field: valid
            child_element = SubElement(element, "valid")
            child_element.text = str(value).lower()
            
        # Process field: warning (XML name: warning)
        value = getattr(self, "warning", None)
        if value is not None:  # Skip None values
            # Single element field: warning
            child_element = SubElement(element, "warning")
            child_element.text = str(value).lower()
            
        # Process field: document_number (XML name: documentNumber)
        value = getattr(self, "document_number", None)
        if value is not None:  # Skip None values
            # Single element field: document_number
            child_element = SubElement(element, "documentNumber")
            child_element.text = value
            
        # Process field: other_suggested_number (XML name: otherSuggestedNumber)
        value = getattr(self, "other_suggested_number", None)
        if value is not None:  # Skip None values
            # Single element field: other_suggested_number
            child_element = SubElement(element, "otherSuggestedNumber")
            child_element.text = value
            
        # Process field: error_message (XML name: errorMessage)
        value = getattr(self, "error_message", None)
        if value is not None:  # Skip None values
            # Single element field: error_message
            child_element = SubElement(element, "errorMessage")
            child_element.text = value
            
        # Process field: additional_info (XML name: additionalInfo)
        value = getattr(self, "additional_info", None)
        if value is not None:  # Skip None values
            # Single element field: additional_info
            child_element = SubElement(element, "additionalInfo")
            child_element.text = value
            


