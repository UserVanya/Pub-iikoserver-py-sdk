# coding: utf-8

"""
    iikoServer API

    API Server позволяет сделать интеграции, которые предназначены для работы с номенклатурой, отчетами, документами, данными сотрудников и приказами.   ### Ограничения и рекомендации - Запросы должны выполняться последовательно друг за другом - Запрашивайте данные за период не длиннее одного месяца, в идеале — за один день или неделю - Для OLAPv2-отчетов рекомендуется устанавливать build-summary=false - При построении OLAPv2-отчета рекомендуется использовать не более 7 полей - Перед выполнением запросов проверяйте их на демо сервере

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictFloat, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional, Union
from typing import Optional, Set, Union
from typing_extensions import Self

# XML support imports
from lxml import etree
from lxml.etree import Element, SubElement

class ProductionDocumentItemXml(BaseModel):
    """
    ProductionDocumentItemXml
    """ # noqa: E501
    amount: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Количество товара в его основных единицах измерения")
    product: Optional[StrictStr] = Field(default=None, description="Товар (guid)")
    product_article: Optional[StrictStr] = Field(default=None, description="Товар (артикул). Можно задать вместо guid товара начиная с 5.0, guid имеет приоритет", alias="productArticle")
    num: Optional[StrictInt] = Field(default=None, description="Порядковый номер в документе")
    container_id: Optional[StrictStr] = Field(default=None, description="Фасовка (guid)", alias="containerId")
    amount_unit: Optional[StrictStr] = Field(default=None, description="Базовая единица измерения (guid)", alias="amountUnit")
    __properties: ClassVar[List[str]] = ["amount", "product", "productArticle", "num", "containerId", "amountUnit"]
    
    # XML metadata for the class
    __xml_config: ClassVar[Dict[str, Any]] = {
        "root_name": "ProductionDocumentItemXml",
        
        
        "field_configs": {
            "amount": {
                "xml_name": "amount",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "product": {
                "xml_name": "product",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "product_article": {
                "xml_name": "productArticle",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "num": {
                "xml_name": "num",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "container_id": {
                "xml_name": "containerId",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "amount_unit": {
                "xml_name": "amountUnit",
                
                
                "is_attribute": False,
                "is_wrapped": False
            }
        }
    }


    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ProductionDocumentItemXml from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # set to None if amount (nullable) is None
        # and model_fields_set contains the field
        if self.amount is None and "amount" in self.model_fields_set:
            _dict['amount'] = None

        # set to None if product (nullable) is None
        # and model_fields_set contains the field
        if self.product is None and "product" in self.model_fields_set:
            _dict['product'] = None

        # set to None if product_article (nullable) is None
        # and model_fields_set contains the field
        if self.product_article is None and "product_article" in self.model_fields_set:
            _dict['productArticle'] = None

        # set to None if num (nullable) is None
        # and model_fields_set contains the field
        if self.num is None and "num" in self.model_fields_set:
            _dict['num'] = None

        # set to None if container_id (nullable) is None
        # and model_fields_set contains the field
        if self.container_id is None and "container_id" in self.model_fields_set:
            _dict['containerId'] = None

        # set to None if amount_unit (nullable) is None
        # and model_fields_set contains the field
        if self.amount_unit is None and "amount_unit" in self.model_fields_set:
            _dict['amountUnit'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ProductionDocumentItemXml from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "amount": obj.get("amount"),
            "product": obj.get("product"),
            "productArticle": obj.get("productArticle"),
            "num": obj.get("num"),
            "containerId": obj.get("containerId"),
            "amountUnit": obj.get("amountUnit")
        })
        return _obj



    @classmethod
    def from_xml(cls, xml_str: str) -> Optional[Self]:
        """Create an instance of ProductionDocumentItemXml from an XML string"""
        try:
            if isinstance(xml_str, str):
                xml_str = xml_str.encode('utf-8')
            root = etree.fromstring(xml_str)
            return cls.from_xml_element(root)
        except Exception as e:
            raise ValueError(f"Failed to parse XML: {e}")

    @classmethod
    def from_xml_element(cls, element: Element) -> Optional[Self]:
        """Create an instance of ProductionDocumentItemXml from an XML element"""
        return cls._from_xml_element_internal(element)

    @classmethod
    def _from_xml_element_internal(cls, element: Element) -> Optional[Self]:
        """Internal method to create instance from XML element"""
        if element is None:
            return None

        data = {}
        
        # Process field: amount (XML name: amount)
        # Handle XML element: amount
        # Single element field: amount
        child_element = element.find("amount")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["amount"] = float(text_value)
                except ValueError:
                    data["amount"] = text_value
                
        
        # Process field: product (XML name: product)
        # Handle XML element: product
        # Single element field: product
        child_element = element.find("product")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["product"] = text_value
                
        
        # Process field: product_article (XML name: productArticle)
        # Handle XML element: product_article
        # Single element field: product_article
        child_element = element.find("productArticle")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["productArticle"] = text_value
                
        
        # Process field: num (XML name: num)
        # Handle XML element: num
        # Single element field: num
        child_element = element.find("num")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["num"] = int(text_value)
                except ValueError:
                    data["num"] = text_value
                
        
        # Process field: container_id (XML name: containerId)
        # Handle XML element: container_id
        # Single element field: container_id
        child_element = element.find("containerId")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["containerId"] = text_value
                
        
        # Process field: amount_unit (XML name: amountUnit)
        # Handle XML element: amount_unit
        # Single element field: amount_unit
        child_element = element.find("amountUnit")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["amountUnit"] = text_value
                
        
        
        return cls.from_dict(data)

    def to_xml(self) -> str:
        """Create an XML string representation of the model"""
        root_element = Element(self.__xml_config.get("root_name", self.__class__.__name__))
        
        # Populate the XML element with model data
        self._to_xml_element_internal(root_element)
        
        # Convert to string with XML declaration
        return etree.tostring(root_element, encoding="utf-8", pretty_print=True, xml_declaration=True).decode("utf-8")

    def _to_xml_element_internal(self, element: Element) -> None:
        """Internal method to populate an XML element with model data"""
        
        # Process field: amount (XML name: amount)
        value = getattr(self, "amount", None)
        if value is not None:  # Skip None values
            # Single element field: amount
            child_element = SubElement(element, "amount")
            child_element.text = str(value)
            
        # Process field: product (XML name: product)
        value = getattr(self, "product", None)
        if value is not None:  # Skip None values
            # Single element field: product
            child_element = SubElement(element, "product")
            child_element.text = value
            
        # Process field: product_article (XML name: productArticle)
        value = getattr(self, "product_article", None)
        if value is not None:  # Skip None values
            # Single element field: product_article
            child_element = SubElement(element, "productArticle")
            child_element.text = value
            
        # Process field: num (XML name: num)
        value = getattr(self, "num", None)
        if value is not None:  # Skip None values
            # Single element field: num
            child_element = SubElement(element, "num")
            child_element.text = str(value)
            
        # Process field: container_id (XML name: containerId)
        value = getattr(self, "container_id", None)
        if value is not None:  # Skip None values
            # Single element field: container_id
            child_element = SubElement(element, "containerId")
            child_element.text = value
            
        # Process field: amount_unit (XML name: amountUnit)
        value = getattr(self, "amount_unit", None)
        if value is not None:  # Skip None values
            # Single element field: amount_unit
            child_element = SubElement(element, "amountUnit")
            child_element.text = value
            


