# coding: utf-8

"""
    iikoServer API

    API Server позволяет сделать интеграции, которые предназначены для работы с номенклатурой, отчетами, документами, данными сотрудников и приказами.   ### Ограничения и рекомендации - Запросы должны выполняться последовательно друг за другом - Запрашивайте данные за период не длиннее одного месяца, в идеале — за один день или неделю - Для OLAPv2-отчетов рекомендуется устанавливать build-summary=false - При построении OLAPv2-отчета рекомендуется использовать не более 7 полей - Перед выполнением запросов проверяйте их на демо сервере

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictFloat, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional, Union
from iikoserver_client.models.barcode_dto import BarcodeDto
from iikoserver_client.models.choice_binding_dto import ChoiceBindingDto
from iikoserver_client.models.container_dto import ContainerDto
from iikoserver_client.models.product_type import ProductType
from iikoserver_client.models.rgb_color_dto import RGBColorDto
from typing import Optional, Set, Union
from typing_extensions import Self

# XML support imports
from lxml import etree
from lxml.etree import Element, SubElement

class BaseProductDto(BaseModel):
    """
    BaseProductDto
    """ # noqa: E501
    name: StrictStr = Field(description="Имя.")
    description: Optional[StrictStr] = Field(default=None, description="Описание.")
    num: Optional[StrictStr] = Field(default=None, description="Артикул, используется при печати документов (тех. карт и т.д.).")
    code: Optional[StrictStr] = Field(default=None, description="Код продукта. Используется для быстрого поиска продукта в экране редактирования заказа.")
    parent: Optional[StrictStr] = Field(default=None, description="UUID родительской группы продукта. Если продукт принадлежит корневой группе, то parent == null.")
    modifiers: Optional[List[ChoiceBindingDto]] = Field(default=None, description="Модификаторы. Поле не учитывает модификаторы из схемы модификаторов.")
    tax_category: Optional[StrictStr] = Field(default=None, description="UUID налоговой категории.", alias="taxCategory")
    category: Optional[StrictStr] = Field(default=None, description="UUID пользовательской категории.")
    accounting_category: Optional[StrictStr] = Field(default=None, description="UUID бухгалтерской категории.", alias="accountingCategory")
    color: Optional[RGBColorDto] = None
    font_color: Optional[RGBColorDto] = Field(default=None, alias="fontColor")
    front_image_id: Optional[StrictStr] = Field(default=None, description="UUID изображения для отображения в iikoFront.", alias="frontImageId")
    position: Optional[StrictInt] = Field(default=None, description="Позиция в меню.")
    main_unit: StrictStr = Field(description="UUID основной единицы измерения продукта.", alias="mainUnit")
    excluded_sections: Optional[List[StrictStr]] = Field(default=None, description="Множество UUID отделений ресторана, в которых нельзя продавать данный продукт (поле имеет смысл только для блюд).", alias="excludedSections")
    default_sale_price: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Цена, по которой по умолчанию продаётся продукт (если для него нет приказов о меню).", alias="defaultSalePrice")
    place_type: Optional[StrictStr] = Field(default=None, description="UUID места приготовления блюда (поле имеет смысл только для блюд).", alias="placeType")
    default_included_in_menu: Optional[StrictBool] = Field(default=None, description="Включать ли по умолчанию (если нет приказов о меню) позицию в меню.", alias="defaultIncludedInMenu")
    type: ProductType
    unit_weight: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Вес одной единицы в килограммах.", alias="unitWeight")
    unit_capacity: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Объем одной единицы в литрах.", alias="unitCapacity")
    not_in_store_movement: Optional[StrictBool] = Field(default=None, description="Участвует ли в перемещениях по складу.", alias="notInStoreMovement")
    containers: Optional[List[ContainerDto]] = Field(default=None, description="Фасовки.")
    modifier_schema_id: Optional[StrictStr] = Field(default=None, description="UUID схемы модификаторов.", alias="modifierSchemaId")
    product_scale_id: Optional[StrictStr] = Field(default=None, description="UUID шкалы размеров. Если задана схема модификаторов, то шкала размеров берется из нее, а эта не учитывается.", alias="productScaleId")
    cold_loss_percent: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Потери при холодной обработке (%).", alias="coldLossPercent")
    hot_loss_percent: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Потери при горячей обработке обработке (%).", alias="hotLossPercent")
    allergen_groups: Optional[List[StrictStr]] = Field(default=None, description="Набор идентификаторов групп аллергенов, которые присутствуют в данном элементе номенклатуры.", alias="allergenGroups")
    can_set_open_price: Optional[StrictBool] = Field(default=None, description="Свободная цена.", alias="canSetOpenPrice")
    barcodes: Optional[List[BarcodeDto]] = Field(default=None, description="Штрихкоды.")
    __properties: ClassVar[List[str]] = ["name", "description", "num", "code", "parent", "modifiers", "taxCategory", "category", "accountingCategory", "color", "fontColor", "frontImageId", "position", "mainUnit", "excludedSections", "defaultSalePrice", "placeType", "defaultIncludedInMenu", "type", "unitWeight", "unitCapacity", "notInStoreMovement", "containers", "modifierSchemaId", "productScaleId", "coldLossPercent", "hotLossPercent", "allergenGroups", "canSetOpenPrice", "barcodes"]
    
    # XML metadata for the class
    __xml_config: ClassVar[Dict[str, Any]] = {
        "root_name": "BaseProductDto",
        
        
        "field_configs": {
            "name": {
                "xml_name": "name",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "description": {
                "xml_name": "description",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "num": {
                "xml_name": "num",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "code": {
                "xml_name": "code",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "parent": {
                "xml_name": "parent",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "modifiers": {
                "xml_name": "modifiers",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "tax_category": {
                "xml_name": "taxCategory",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "category": {
                "xml_name": "category",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "accounting_category": {
                "xml_name": "accountingCategory",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "color": {
                "xml_name": "color",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "font_color": {
                "xml_name": "fontColor",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "front_image_id": {
                "xml_name": "frontImageId",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "position": {
                "xml_name": "position",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "main_unit": {
                "xml_name": "mainUnit",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "excluded_sections": {
                "xml_name": "excludedSections",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "default_sale_price": {
                "xml_name": "defaultSalePrice",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "place_type": {
                "xml_name": "placeType",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "default_included_in_menu": {
                "xml_name": "defaultIncludedInMenu",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "type": {
                "xml_name": "type",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "unit_weight": {
                "xml_name": "unitWeight",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "unit_capacity": {
                "xml_name": "unitCapacity",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "not_in_store_movement": {
                "xml_name": "notInStoreMovement",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "containers": {
                "xml_name": "containers",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "modifier_schema_id": {
                "xml_name": "modifierSchemaId",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "product_scale_id": {
                "xml_name": "productScaleId",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "cold_loss_percent": {
                "xml_name": "coldLossPercent",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "hot_loss_percent": {
                "xml_name": "hotLossPercent",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "allergen_groups": {
                "xml_name": "allergenGroups",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "can_set_open_price": {
                "xml_name": "canSetOpenPrice",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "barcodes": {
                "xml_name": "barcodes",
                
                
                "is_attribute": False,
                "is_wrapped": False
            }
        }
    }


    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of BaseProductDto from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in modifiers (list)
        _items = []
        if self.modifiers:
            for _item_modifiers in self.modifiers:
                if _item_modifiers:
                    _items.append(_item_modifiers.to_dict())
            _dict['modifiers'] = _items
        # override the default output from pydantic by calling `to_dict()` of color
        if self.color:
            _dict['color'] = self.color.to_dict()
        # override the default output from pydantic by calling `to_dict()` of font_color
        if self.font_color:
            _dict['fontColor'] = self.font_color.to_dict()
        # override the default output from pydantic by calling `to_dict()` of each item in containers (list)
        _items = []
        if self.containers:
            for _item_containers in self.containers:
                if _item_containers:
                    _items.append(_item_containers.to_dict())
            _dict['containers'] = _items
        # override the default output from pydantic by calling `to_dict()` of each item in barcodes (list)
        _items = []
        if self.barcodes:
            for _item_barcodes in self.barcodes:
                if _item_barcodes:
                    _items.append(_item_barcodes.to_dict())
            _dict['barcodes'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of BaseProductDto from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "name": obj.get("name"),
            "description": obj.get("description"),
            "num": obj.get("num"),
            "code": obj.get("code"),
            "parent": obj.get("parent"),
            "modifiers": [ChoiceBindingDto.from_dict(_item) for _item in obj["modifiers"]] if obj.get("modifiers") is not None else None,
            "taxCategory": obj.get("taxCategory"),
            "category": obj.get("category"),
            "accountingCategory": obj.get("accountingCategory"),
            "color": RGBColorDto.from_dict(obj["color"]) if obj.get("color") is not None else None,
            "fontColor": RGBColorDto.from_dict(obj["fontColor"]) if obj.get("fontColor") is not None else None,
            "frontImageId": obj.get("frontImageId"),
            "position": obj.get("position"),
            "mainUnit": obj.get("mainUnit"),
            "excludedSections": obj.get("excludedSections"),
            "defaultSalePrice": obj.get("defaultSalePrice"),
            "placeType": obj.get("placeType"),
            "defaultIncludedInMenu": obj.get("defaultIncludedInMenu"),
            "type": obj.get("type"),
            "unitWeight": obj.get("unitWeight"),
            "unitCapacity": obj.get("unitCapacity"),
            "notInStoreMovement": obj.get("notInStoreMovement"),
            "containers": [ContainerDto.from_dict(_item) for _item in obj["containers"]] if obj.get("containers") is not None else None,
            "modifierSchemaId": obj.get("modifierSchemaId"),
            "productScaleId": obj.get("productScaleId"),
            "coldLossPercent": obj.get("coldLossPercent"),
            "hotLossPercent": obj.get("hotLossPercent"),
            "allergenGroups": obj.get("allergenGroups"),
            "canSetOpenPrice": obj.get("canSetOpenPrice"),
            "barcodes": [BarcodeDto.from_dict(_item) for _item in obj["barcodes"]] if obj.get("barcodes") is not None else None
        })
        return _obj



    @classmethod
    def from_xml(cls, xml_str: str) -> Optional[Self]:
        """Create an instance of BaseProductDto from an XML string"""
        try:
            if isinstance(xml_str, str):
                xml_str = xml_str.encode('utf-8')
            root = etree.fromstring(xml_str)
            return cls.from_xml_element(root)
        except Exception as e:
            raise ValueError(f"Failed to parse XML: {e}")

    @classmethod
    def from_xml_element(cls, element: Element) -> Optional[Self]:
        """Create an instance of BaseProductDto from an XML element"""
        return cls._from_xml_element_internal(element)

    @classmethod
    def _from_xml_element_internal(cls, element: Element) -> Optional[Self]:
        """Internal method to create instance from XML element"""
        if element is None:
            return None

        data = {}
        
        # Process field: name (XML name: name)
        # Handle XML element: name
        # Single element field: name
        child_element = element.find("name")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["name"] = text_value
                
        
        # Process field: description (XML name: description)
        # Handle XML element: description
        # Single element field: description
        child_element = element.find("description")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["description"] = text_value
                
        
        # Process field: num (XML name: num)
        # Handle XML element: num
        # Single element field: num
        child_element = element.find("num")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["num"] = text_value
                
        
        # Process field: code (XML name: code)
        # Handle XML element: code
        # Single element field: code
        child_element = element.find("code")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["code"] = text_value
                
        
        # Process field: parent (XML name: parent)
        # Handle XML element: parent
        # Single element field: parent
        child_element = element.find("parent")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["parent"] = text_value
                
        
        # Process field: modifiers (XML name: modifiers)
        # Handle XML element: modifiers
        # Array field: modifiers
        # Non-wrapped array - direct elements
        # Complex items in non-wrapped array
        child_elements = element.findall("modifiers")
        items = []
        for child in child_elements:
            item_obj = ChoiceBindingDto.from_xml_element(child)
            if item_obj is not None:
                items.append(item_obj)
        data["modifiers"] = items
        
        # Process field: tax_category (XML name: taxCategory)
        # Handle XML element: tax_category
        # Single element field: tax_category
        child_element = element.find("taxCategory")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["taxCategory"] = text_value
                
        
        # Process field: category (XML name: category)
        # Handle XML element: category
        # Single element field: category
        child_element = element.find("category")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["category"] = text_value
                
        
        # Process field: accounting_category (XML name: accountingCategory)
        # Handle XML element: accounting_category
        # Single element field: accounting_category
        child_element = element.find("accountingCategory")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["accountingCategory"] = text_value
                
        
        # Process field: color (XML name: color)
        # Handle XML element: color
        # Single element field: color
        child_element = element.find("color")
        if child_element is not None:
            # Complex object field
            nested_obj = RGBColorDto.from_xml_element(child_element)
            if nested_obj is not None:
                data["color"] = nested_obj
        
        # Process field: font_color (XML name: fontColor)
        # Handle XML element: font_color
        # Single element field: font_color
        child_element = element.find("fontColor")
        if child_element is not None:
            # Complex object field
            nested_obj = RGBColorDto.from_xml_element(child_element)
            if nested_obj is not None:
                data["fontColor"] = nested_obj
        
        # Process field: front_image_id (XML name: frontImageId)
        # Handle XML element: front_image_id
        # Single element field: front_image_id
        child_element = element.find("frontImageId")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["frontImageId"] = text_value
                
        
        # Process field: position (XML name: position)
        # Handle XML element: position
        # Single element field: position
        child_element = element.find("position")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["position"] = int(text_value)
                except ValueError:
                    data["position"] = text_value
                
        
        # Process field: main_unit (XML name: mainUnit)
        # Handle XML element: main_unit
        # Single element field: main_unit
        child_element = element.find("mainUnit")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["mainUnit"] = text_value
                
        
        # Process field: excluded_sections (XML name: excludedSections)
        # Handle XML element: excluded_sections
        # Array field: excluded_sections
        # Non-wrapped array - direct elements
        # Primitive items in non-wrapped array
        child_elements = element.findall("excludedSections")
        items = []
        for child in child_elements:
            text_value = child.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                items.append(text_value)
                
        data["excludedSections"] = items
        
        # Process field: default_sale_price (XML name: defaultSalePrice)
        # Handle XML element: default_sale_price
        # Single element field: default_sale_price
        child_element = element.find("defaultSalePrice")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["defaultSalePrice"] = float(text_value)
                except ValueError:
                    data["defaultSalePrice"] = text_value
                
        
        # Process field: place_type (XML name: placeType)
        # Handle XML element: place_type
        # Single element field: place_type
        child_element = element.find("placeType")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["placeType"] = text_value
                
        
        # Process field: default_included_in_menu (XML name: defaultIncludedInMenu)
        # Handle XML element: default_included_in_menu
        # Single element field: default_included_in_menu
        child_element = element.find("defaultIncludedInMenu")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["defaultIncludedInMenu"] = text_value.lower() == 'true'
                
        
        # Process field: type (XML name: type)
        # Handle XML element: type
        # Single element field: type
        child_element = element.find("type")
        if child_element is not None:
            # Enum field
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["type"] = text_value
        
        # Process field: unit_weight (XML name: unitWeight)
        # Handle XML element: unit_weight
        # Single element field: unit_weight
        child_element = element.find("unitWeight")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["unitWeight"] = float(text_value)
                except ValueError:
                    data["unitWeight"] = text_value
                
        
        # Process field: unit_capacity (XML name: unitCapacity)
        # Handle XML element: unit_capacity
        # Single element field: unit_capacity
        child_element = element.find("unitCapacity")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["unitCapacity"] = float(text_value)
                except ValueError:
                    data["unitCapacity"] = text_value
                
        
        # Process field: not_in_store_movement (XML name: notInStoreMovement)
        # Handle XML element: not_in_store_movement
        # Single element field: not_in_store_movement
        child_element = element.find("notInStoreMovement")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["notInStoreMovement"] = text_value.lower() == 'true'
                
        
        # Process field: containers (XML name: containers)
        # Handle XML element: containers
        # Array field: containers
        # Non-wrapped array - direct elements
        # Complex items in non-wrapped array
        child_elements = element.findall("containers")
        items = []
        for child in child_elements:
            item_obj = ContainerDto.from_xml_element(child)
            if item_obj is not None:
                items.append(item_obj)
        data["containers"] = items
        
        # Process field: modifier_schema_id (XML name: modifierSchemaId)
        # Handle XML element: modifier_schema_id
        # Single element field: modifier_schema_id
        child_element = element.find("modifierSchemaId")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["modifierSchemaId"] = text_value
                
        
        # Process field: product_scale_id (XML name: productScaleId)
        # Handle XML element: product_scale_id
        # Single element field: product_scale_id
        child_element = element.find("productScaleId")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["productScaleId"] = text_value
                
        
        # Process field: cold_loss_percent (XML name: coldLossPercent)
        # Handle XML element: cold_loss_percent
        # Single element field: cold_loss_percent
        child_element = element.find("coldLossPercent")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["coldLossPercent"] = float(text_value)
                except ValueError:
                    data["coldLossPercent"] = text_value
                
        
        # Process field: hot_loss_percent (XML name: hotLossPercent)
        # Handle XML element: hot_loss_percent
        # Single element field: hot_loss_percent
        child_element = element.find("hotLossPercent")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["hotLossPercent"] = float(text_value)
                except ValueError:
                    data["hotLossPercent"] = text_value
                
        
        # Process field: allergen_groups (XML name: allergenGroups)
        # Handle XML element: allergen_groups
        # Array field: allergen_groups
        # Non-wrapped array - direct elements
        # Primitive items in non-wrapped array
        child_elements = element.findall("allergenGroups")
        items = []
        for child in child_elements:
            text_value = child.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                items.append(text_value)
                
        data["allergenGroups"] = items
        
        # Process field: can_set_open_price (XML name: canSetOpenPrice)
        # Handle XML element: can_set_open_price
        # Single element field: can_set_open_price
        child_element = element.find("canSetOpenPrice")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["canSetOpenPrice"] = text_value.lower() == 'true'
                
        
        # Process field: barcodes (XML name: barcodes)
        # Handle XML element: barcodes
        # Array field: barcodes
        # Non-wrapped array - direct elements
        # Complex items in non-wrapped array
        child_elements = element.findall("barcodes")
        items = []
        for child in child_elements:
            item_obj = BarcodeDto.from_xml_element(child)
            if item_obj is not None:
                items.append(item_obj)
        data["barcodes"] = items
        
        
        return cls.from_dict(data)

    def to_xml(self) -> str:
        """Create an XML string representation of the model"""
        root_element = Element(self.__xml_config.get("root_name", self.__class__.__name__))
        
        # Populate the XML element with model data
        self._to_xml_element_internal(root_element)
        
        # Convert to string with XML declaration
        return etree.tostring(root_element, encoding="utf-8", pretty_print=True, xml_declaration=True).decode("utf-8")

    def _to_xml_element_internal(self, element: Element) -> None:
        """Internal method to populate an XML element with model data"""
        
        # Process field: name (XML name: name)
        value = getattr(self, "name", None)
        if value is not None:  # Skip None values
            # Single element field: name
            child_element = SubElement(element, "name")
            child_element.text = value
            
        # Process field: description (XML name: description)
        value = getattr(self, "description", None)
        if value is not None:  # Skip None values
            # Single element field: description
            child_element = SubElement(element, "description")
            child_element.text = value
            
        # Process field: num (XML name: num)
        value = getattr(self, "num", None)
        if value is not None:  # Skip None values
            # Single element field: num
            child_element = SubElement(element, "num")
            child_element.text = value
            
        # Process field: code (XML name: code)
        value = getattr(self, "code", None)
        if value is not None:  # Skip None values
            # Single element field: code
            child_element = SubElement(element, "code")
            child_element.text = value
            
        # Process field: parent (XML name: parent)
        value = getattr(self, "parent", None)
        if value is not None:  # Skip None values
            # Single element field: parent
            child_element = SubElement(element, "parent")
            child_element.text = value
            
        # Process field: modifiers (XML name: modifiers)
        value = getattr(self, "modifiers", None)
        if value is not None:  # Skip None values
            # Array field: modifiers
            # Non-wrapped array - add elements directly
            for item in value:
                # Complex items in non-wrapped array
                if hasattr(item, "_to_xml_element_internal"):
                    item_element = SubElement(element, "modifiers")
                    item._to_xml_element_internal(item_element)
        # Process field: tax_category (XML name: taxCategory)
        value = getattr(self, "tax_category", None)
        if value is not None:  # Skip None values
            # Single element field: tax_category
            child_element = SubElement(element, "taxCategory")
            child_element.text = value
            
        # Process field: category (XML name: category)
        value = getattr(self, "category", None)
        if value is not None:  # Skip None values
            # Single element field: category
            child_element = SubElement(element, "category")
            child_element.text = value
            
        # Process field: accounting_category (XML name: accountingCategory)
        value = getattr(self, "accounting_category", None)
        if value is not None:  # Skip None values
            # Single element field: accounting_category
            child_element = SubElement(element, "accountingCategory")
            child_element.text = value
            
        # Process field: color (XML name: color)
        value = getattr(self, "color", None)
        if value is not None:  # Skip None values
            # Single element field: color
            child_element = SubElement(element, "color")
            # Complex object field
            if hasattr(value, "_to_xml_element_internal"):
                value._to_xml_element_internal(child_element)
        # Process field: font_color (XML name: fontColor)
        value = getattr(self, "font_color", None)
        if value is not None:  # Skip None values
            # Single element field: font_color
            child_element = SubElement(element, "fontColor")
            # Complex object field
            if hasattr(value, "_to_xml_element_internal"):
                value._to_xml_element_internal(child_element)
        # Process field: front_image_id (XML name: frontImageId)
        value = getattr(self, "front_image_id", None)
        if value is not None:  # Skip None values
            # Single element field: front_image_id
            child_element = SubElement(element, "frontImageId")
            child_element.text = value
            
        # Process field: position (XML name: position)
        value = getattr(self, "position", None)
        if value is not None:  # Skip None values
            # Single element field: position
            child_element = SubElement(element, "position")
            child_element.text = str(value)
            
        # Process field: main_unit (XML name: mainUnit)
        value = getattr(self, "main_unit", None)
        if value is not None:  # Skip None values
            # Single element field: main_unit
            child_element = SubElement(element, "mainUnit")
            child_element.text = value
            
        # Process field: excluded_sections (XML name: excludedSections)
        value = getattr(self, "excluded_sections", None)
        if value is not None:  # Skip None values
            # Array field: excluded_sections
            # Non-wrapped array - add elements directly
            for item in value:
                # Primitive items in non-wrapped array
                item_element = SubElement(element, "excludedSections")
                item_element.text = item
                
        # Process field: default_sale_price (XML name: defaultSalePrice)
        value = getattr(self, "default_sale_price", None)
        if value is not None:  # Skip None values
            # Single element field: default_sale_price
            child_element = SubElement(element, "defaultSalePrice")
            child_element.text = str(value)
            
        # Process field: place_type (XML name: placeType)
        value = getattr(self, "place_type", None)
        if value is not None:  # Skip None values
            # Single element field: place_type
            child_element = SubElement(element, "placeType")
            child_element.text = value
            
        # Process field: default_included_in_menu (XML name: defaultIncludedInMenu)
        value = getattr(self, "default_included_in_menu", None)
        if value is not None:  # Skip None values
            # Single element field: default_included_in_menu
            child_element = SubElement(element, "defaultIncludedInMenu")
            child_element.text = str(value).lower()
            
        # Process field: type (XML name: type)
        value = getattr(self, "type", None)
        if value is not None:  # Skip None values
            # Single element field: type
            child_element = SubElement(element, "type")
            # Enum field
            child_element.text = str(value)
        # Process field: unit_weight (XML name: unitWeight)
        value = getattr(self, "unit_weight", None)
        if value is not None:  # Skip None values
            # Single element field: unit_weight
            child_element = SubElement(element, "unitWeight")
            child_element.text = str(value)
            
        # Process field: unit_capacity (XML name: unitCapacity)
        value = getattr(self, "unit_capacity", None)
        if value is not None:  # Skip None values
            # Single element field: unit_capacity
            child_element = SubElement(element, "unitCapacity")
            child_element.text = str(value)
            
        # Process field: not_in_store_movement (XML name: notInStoreMovement)
        value = getattr(self, "not_in_store_movement", None)
        if value is not None:  # Skip None values
            # Single element field: not_in_store_movement
            child_element = SubElement(element, "notInStoreMovement")
            child_element.text = str(value).lower()
            
        # Process field: containers (XML name: containers)
        value = getattr(self, "containers", None)
        if value is not None:  # Skip None values
            # Array field: containers
            # Non-wrapped array - add elements directly
            for item in value:
                # Complex items in non-wrapped array
                if hasattr(item, "_to_xml_element_internal"):
                    item_element = SubElement(element, "containers")
                    item._to_xml_element_internal(item_element)
        # Process field: modifier_schema_id (XML name: modifierSchemaId)
        value = getattr(self, "modifier_schema_id", None)
        if value is not None:  # Skip None values
            # Single element field: modifier_schema_id
            child_element = SubElement(element, "modifierSchemaId")
            child_element.text = value
            
        # Process field: product_scale_id (XML name: productScaleId)
        value = getattr(self, "product_scale_id", None)
        if value is not None:  # Skip None values
            # Single element field: product_scale_id
            child_element = SubElement(element, "productScaleId")
            child_element.text = value
            
        # Process field: cold_loss_percent (XML name: coldLossPercent)
        value = getattr(self, "cold_loss_percent", None)
        if value is not None:  # Skip None values
            # Single element field: cold_loss_percent
            child_element = SubElement(element, "coldLossPercent")
            child_element.text = str(value)
            
        # Process field: hot_loss_percent (XML name: hotLossPercent)
        value = getattr(self, "hot_loss_percent", None)
        if value is not None:  # Skip None values
            # Single element field: hot_loss_percent
            child_element = SubElement(element, "hotLossPercent")
            child_element.text = str(value)
            
        # Process field: allergen_groups (XML name: allergenGroups)
        value = getattr(self, "allergen_groups", None)
        if value is not None:  # Skip None values
            # Array field: allergen_groups
            # Non-wrapped array - add elements directly
            for item in value:
                # Primitive items in non-wrapped array
                item_element = SubElement(element, "allergenGroups")
                item_element.text = item
                
        # Process field: can_set_open_price (XML name: canSetOpenPrice)
        value = getattr(self, "can_set_open_price", None)
        if value is not None:  # Skip None values
            # Single element field: can_set_open_price
            child_element = SubElement(element, "canSetOpenPrice")
            child_element.text = str(value).lower()
            
        # Process field: barcodes (XML name: barcodes)
        value = getattr(self, "barcodes", None)
        if value is not None:  # Skip None values
            # Array field: barcodes
            # Non-wrapped array - add elements directly
            for item in value:
                # Complex items in non-wrapped array
                if hasattr(item, "_to_xml_element_internal"):
                    item_element = SubElement(element, "barcodes")
                    item._to_xml_element_internal(item_element)


