# coding: utf-8

"""
    iikoServer API

    API Server позволяет сделать интеграции, которые предназначены для работы с номенклатурой, отчетами, документами, данными сотрудников и приказами.   ### Ограничения и рекомендации - Запросы должны выполняться последовательно друг за другом - Запрашивайте данные за период не длиннее одного месяца, в идеале — за один день или неделю - Для OLAPv2-отчетов рекомендуется устанавливать build-summary=false - При построении OLAPv2-отчета рекомендуется использовать не более 7 полей - Перед выполнением запросов проверяйте их на демо сервере

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set, Union
from typing_extensions import Self

# XML support imports
from lxml import etree
from lxml.etree import Element, SubElement

class LegalAddressDto(BaseModel):
    """
    LegalAddressDto
    """ # noqa: E501
    zip_code: Optional[StrictStr] = Field(default=None, description="ZIP код.", alias="zipCode")
    office: Optional[StrictStr] = Field(default=None, description="Офис.")
    building: Optional[StrictStr] = Field(default=None, description="Строение.")
    house: Optional[StrictStr] = Field(default=None, description="Дом.")
    street: Optional[StrictStr] = Field(default=None, description="Улица.")
    community: Optional[StrictStr] = Field(default=None, description="Населенный пункт.")
    city: Optional[StrictStr] = Field(default=None, description="Город.")
    district: Optional[StrictStr] = Field(default=None, description="Район.")
    region: Optional[StrictStr] = Field(default=None, description="Регион.")
    country: Optional[StrictStr] = Field(default=None, description="Страна.")
    __properties: ClassVar[List[str]] = ["zipCode", "office", "building", "house", "street", "community", "city", "district", "region", "country"]
    
    # XML metadata for the class
    __xml_config: ClassVar[Dict[str, Any]] = {
        "root_name": "legalAddressDto",
        
        
        "field_configs": {
            "zip_code": {
                "xml_name": "zipCode",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "office": {
                "xml_name": "office",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "building": {
                "xml_name": "building",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "house": {
                "xml_name": "house",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "street": {
                "xml_name": "street",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "community": {
                "xml_name": "community",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "city": {
                "xml_name": "city",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "district": {
                "xml_name": "district",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "region": {
                "xml_name": "region",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "country": {
                "xml_name": "country",
                
                
                "is_attribute": False,
                "is_wrapped": False
            }
        }
    }


    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of LegalAddressDto from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # set to None if zip_code (nullable) is None
        # and model_fields_set contains the field
        if self.zip_code is None and "zip_code" in self.model_fields_set:
            _dict['zipCode'] = None

        # set to None if office (nullable) is None
        # and model_fields_set contains the field
        if self.office is None and "office" in self.model_fields_set:
            _dict['office'] = None

        # set to None if building (nullable) is None
        # and model_fields_set contains the field
        if self.building is None and "building" in self.model_fields_set:
            _dict['building'] = None

        # set to None if house (nullable) is None
        # and model_fields_set contains the field
        if self.house is None and "house" in self.model_fields_set:
            _dict['house'] = None

        # set to None if street (nullable) is None
        # and model_fields_set contains the field
        if self.street is None and "street" in self.model_fields_set:
            _dict['street'] = None

        # set to None if community (nullable) is None
        # and model_fields_set contains the field
        if self.community is None and "community" in self.model_fields_set:
            _dict['community'] = None

        # set to None if city (nullable) is None
        # and model_fields_set contains the field
        if self.city is None and "city" in self.model_fields_set:
            _dict['city'] = None

        # set to None if district (nullable) is None
        # and model_fields_set contains the field
        if self.district is None and "district" in self.model_fields_set:
            _dict['district'] = None

        # set to None if region (nullable) is None
        # and model_fields_set contains the field
        if self.region is None and "region" in self.model_fields_set:
            _dict['region'] = None

        # set to None if country (nullable) is None
        # and model_fields_set contains the field
        if self.country is None and "country" in self.model_fields_set:
            _dict['country'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of LegalAddressDto from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "zipCode": obj.get("zipCode"),
            "office": obj.get("office"),
            "building": obj.get("building"),
            "house": obj.get("house"),
            "street": obj.get("street"),
            "community": obj.get("community"),
            "city": obj.get("city"),
            "district": obj.get("district"),
            "region": obj.get("region"),
            "country": obj.get("country")
        })
        return _obj



    @classmethod
    def from_xml(cls, xml_str: str) -> Optional[Self]:
        """Create an instance of LegalAddressDto from an XML string"""
        try:
            if isinstance(xml_str, str):
                xml_str = xml_str.encode('utf-8')
            root = etree.fromstring(xml_str)
            return cls.from_xml_element(root)
        except Exception as e:
            raise ValueError(f"Failed to parse XML: {e}")

    @classmethod
    def from_xml_element(cls, element: Element) -> Optional[Self]:
        """Create an instance of LegalAddressDto from an XML element"""
        return cls._from_xml_element_internal(element)

    @classmethod
    def _from_xml_element_internal(cls, element: Element) -> Optional[Self]:
        """Internal method to create instance from XML element"""
        if element is None:
            return None

        data = {}
        
        # Process field: zip_code (XML name: zipCode)
        # Handle XML element: zip_code
        # Single element field: zip_code
        child_element = element.find("zipCode")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["zipCode"] = text_value
                
        
        # Process field: office (XML name: office)
        # Handle XML element: office
        # Single element field: office
        child_element = element.find("office")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["office"] = text_value
                
        
        # Process field: building (XML name: building)
        # Handle XML element: building
        # Single element field: building
        child_element = element.find("building")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["building"] = text_value
                
        
        # Process field: house (XML name: house)
        # Handle XML element: house
        # Single element field: house
        child_element = element.find("house")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["house"] = text_value
                
        
        # Process field: street (XML name: street)
        # Handle XML element: street
        # Single element field: street
        child_element = element.find("street")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["street"] = text_value
                
        
        # Process field: community (XML name: community)
        # Handle XML element: community
        # Single element field: community
        child_element = element.find("community")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["community"] = text_value
                
        
        # Process field: city (XML name: city)
        # Handle XML element: city
        # Single element field: city
        child_element = element.find("city")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["city"] = text_value
                
        
        # Process field: district (XML name: district)
        # Handle XML element: district
        # Single element field: district
        child_element = element.find("district")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["district"] = text_value
                
        
        # Process field: region (XML name: region)
        # Handle XML element: region
        # Single element field: region
        child_element = element.find("region")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["region"] = text_value
                
        
        # Process field: country (XML name: country)
        # Handle XML element: country
        # Single element field: country
        child_element = element.find("country")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["country"] = text_value
                
        
        
        return cls.from_dict(data)

    def to_xml(self) -> str:
        """Create an XML string representation of the model"""
        root_element = Element(self.__xml_config.get("root_name", self.__class__.__name__))
        
        # Populate the XML element with model data
        self._to_xml_element_internal(root_element)
        
        # Convert to string with XML declaration
        return etree.tostring(root_element, encoding="utf-8", pretty_print=True, xml_declaration=True).decode("utf-8")

    def _to_xml_element_internal(self, element: Element) -> None:
        """Internal method to populate an XML element with model data"""
        
        # Process field: zip_code (XML name: zipCode)
        value = getattr(self, "zip_code", None)
        if value is not None:  # Skip None values
            # Single element field: zip_code
            child_element = SubElement(element, "zipCode")
            child_element.text = value
            
        # Process field: office (XML name: office)
        value = getattr(self, "office", None)
        if value is not None:  # Skip None values
            # Single element field: office
            child_element = SubElement(element, "office")
            child_element.text = value
            
        # Process field: building (XML name: building)
        value = getattr(self, "building", None)
        if value is not None:  # Skip None values
            # Single element field: building
            child_element = SubElement(element, "building")
            child_element.text = value
            
        # Process field: house (XML name: house)
        value = getattr(self, "house", None)
        if value is not None:  # Skip None values
            # Single element field: house
            child_element = SubElement(element, "house")
            child_element.text = value
            
        # Process field: street (XML name: street)
        value = getattr(self, "street", None)
        if value is not None:  # Skip None values
            # Single element field: street
            child_element = SubElement(element, "street")
            child_element.text = value
            
        # Process field: community (XML name: community)
        value = getattr(self, "community", None)
        if value is not None:  # Skip None values
            # Single element field: community
            child_element = SubElement(element, "community")
            child_element.text = value
            
        # Process field: city (XML name: city)
        value = getattr(self, "city", None)
        if value is not None:  # Skip None values
            # Single element field: city
            child_element = SubElement(element, "city")
            child_element.text = value
            
        # Process field: district (XML name: district)
        value = getattr(self, "district", None)
        if value is not None:  # Skip None values
            # Single element field: district
            child_element = SubElement(element, "district")
            child_element.text = value
            
        # Process field: region (XML name: region)
        value = getattr(self, "region", None)
        if value is not None:  # Skip None values
            # Single element field: region
            child_element = SubElement(element, "region")
            child_element.text = value
            
        # Process field: country (XML name: country)
        value = getattr(self, "country", None)
        if value is not None:  # Skip None values
            # Single element field: country
            child_element = SubElement(element, "country")
            child_element.text = value
            


