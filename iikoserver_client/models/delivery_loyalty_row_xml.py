# coding: utf-8

"""
    iikoServer API

    API Server позволяет сделать интеграции, которые предназначены для работы с номенклатурой, отчетами, документами, данными сотрудников и приказами.   ### Ограничения и рекомендации - Запросы должны выполняться последовательно друг за другом - Запрашивайте данные за период не длиннее одного месяца, в идеале — за один день или неделю - Для OLAPv2-отчетов рекомендуется устанавливать build-summary=false - При построении OLAPv2-отчета рекомендуется использовать не более 7 полей - Перед выполнением запросов проверяйте их на демо сервере

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictFloat, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional, Union
from iikoserver_client.models.delivery_loyalty_row_xml_regions import DeliveryLoyaltyRowXmlRegions
from iikoserver_client.models.metric_type_enum import MetricTypeEnum
from typing import Optional, Set, Union
from typing_extensions import Self

# XML support imports
from lxml import etree
from lxml.etree import Element, SubElement

class DeliveryLoyaltyRowXml(BaseModel):
    """
    DeliveryLoyaltyRowXml
    """ # noqa: E501
    var_date: Optional[StrictStr] = Field(default=None, description="Дата", alias="date")
    metric_type: Optional[MetricTypeEnum] = Field(default=None, alias="metricType")
    new_guest_count: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Количество новых гостей", alias="newGuestCount")
    order_count_per_guest: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Среднее количество заказов на гостя", alias="orderCountPerGuest")
    regions: Optional[DeliveryLoyaltyRowXmlRegions] = None
    total_order_count: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Общее количество заказов", alias="totalOrderCount")
    __properties: ClassVar[List[str]] = ["date", "metricType", "newGuestCount", "orderCountPerGuest", "regions", "totalOrderCount"]
    
    # XML metadata for the class
    __xml_config: ClassVar[Dict[str, Any]] = {
        "root_name": "row",
        
        
        "field_configs": {
            "var_date": {
                "xml_name": "date",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "metric_type": {
                "xml_name": "metricType",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "new_guest_count": {
                "xml_name": "newGuestCount",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "order_count_per_guest": {
                "xml_name": "orderCountPerGuest",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "regions": {
                "xml_name": "regions",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "total_order_count": {
                "xml_name": "totalOrderCount",
                
                
                "is_attribute": False,
                "is_wrapped": False
            }
        }
    }


    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of DeliveryLoyaltyRowXml from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of regions
        if self.regions:
            _dict['regions'] = self.regions.to_dict()
        # set to None if var_date (nullable) is None
        # and model_fields_set contains the field
        if self.var_date is None and "var_date" in self.model_fields_set:
            _dict['date'] = None

        # set to None if new_guest_count (nullable) is None
        # and model_fields_set contains the field
        if self.new_guest_count is None and "new_guest_count" in self.model_fields_set:
            _dict['newGuestCount'] = None

        # set to None if order_count_per_guest (nullable) is None
        # and model_fields_set contains the field
        if self.order_count_per_guest is None and "order_count_per_guest" in self.model_fields_set:
            _dict['orderCountPerGuest'] = None

        # set to None if total_order_count (nullable) is None
        # and model_fields_set contains the field
        if self.total_order_count is None and "total_order_count" in self.model_fields_set:
            _dict['totalOrderCount'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of DeliveryLoyaltyRowXml from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "date": obj.get("date"),
            "metricType": obj.get("metricType"),
            "newGuestCount": obj.get("newGuestCount"),
            "orderCountPerGuest": obj.get("orderCountPerGuest"),
            "regions": DeliveryLoyaltyRowXmlRegions.from_dict(obj["regions"]) if obj.get("regions") is not None else None,
            "totalOrderCount": obj.get("totalOrderCount")
        })
        return _obj



    @classmethod
    def from_xml(cls, xml_str: str) -> Optional[Self]:
        """Create an instance of DeliveryLoyaltyRowXml from an XML string"""
        try:
            if isinstance(xml_str, str):
                xml_str = xml_str.encode('utf-8')
            root = etree.fromstring(xml_str)
            return cls.from_xml_element(root)
        except Exception as e:
            raise ValueError(f"Failed to parse XML: {e}")

    @classmethod
    def from_xml_element(cls, element: Element) -> Optional[Self]:
        """Create an instance of DeliveryLoyaltyRowXml from an XML element"""
        return cls._from_xml_element_internal(element)

    @classmethod
    def _from_xml_element_internal(cls, element: Element) -> Optional[Self]:
        """Internal method to create instance from XML element"""
        if element is None:
            return None

        data = {}
        
        # Process field: var_date (XML name: date)
        # Handle XML element: var_date
        # Single element field: var_date
        child_element = element.find("date")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["date"] = text_value
                
        
        # Process field: metric_type (XML name: metricType)
        # Handle XML element: metric_type
        # Single element field: metric_type
        child_element = element.find("metricType")
        if child_element is not None:
            # Enum field
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["metricType"] = text_value
        
        # Process field: new_guest_count (XML name: newGuestCount)
        # Handle XML element: new_guest_count
        # Single element field: new_guest_count
        child_element = element.find("newGuestCount")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["newGuestCount"] = float(text_value)
                except ValueError:
                    data["newGuestCount"] = text_value
                
        
        # Process field: order_count_per_guest (XML name: orderCountPerGuest)
        # Handle XML element: order_count_per_guest
        # Single element field: order_count_per_guest
        child_element = element.find("orderCountPerGuest")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["orderCountPerGuest"] = float(text_value)
                except ValueError:
                    data["orderCountPerGuest"] = text_value
                
        
        # Process field: regions (XML name: regions)
        # Handle XML element: regions
        # Single element field: regions
        child_element = element.find("regions")
        if child_element is not None:
            # Complex object field
            nested_obj = DeliveryLoyaltyRowXmlRegions.from_xml_element(child_element)
            if nested_obj is not None:
                data["regions"] = nested_obj
        
        # Process field: total_order_count (XML name: totalOrderCount)
        # Handle XML element: total_order_count
        # Single element field: total_order_count
        child_element = element.find("totalOrderCount")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["totalOrderCount"] = float(text_value)
                except ValueError:
                    data["totalOrderCount"] = text_value
                
        
        
        return cls.from_dict(data)

    def to_xml(self) -> str:
        """Create an XML string representation of the model"""
        root_element = Element(self.__xml_config.get("root_name", self.__class__.__name__))
        
        # Populate the XML element with model data
        self._to_xml_element_internal(root_element)
        
        # Convert to string with XML declaration
        return etree.tostring(root_element, encoding="utf-8", pretty_print=True, xml_declaration=True).decode("utf-8")

    def _to_xml_element_internal(self, element: Element) -> None:
        """Internal method to populate an XML element with model data"""
        
        # Process field: var_date (XML name: date)
        value = getattr(self, "var_date", None)
        if value is not None:  # Skip None values
            # Single element field: var_date
            child_element = SubElement(element, "date")
            child_element.text = value
            
        # Process field: metric_type (XML name: metricType)
        value = getattr(self, "metric_type", None)
        if value is not None:  # Skip None values
            # Single element field: metric_type
            child_element = SubElement(element, "metricType")
            # Enum field
            child_element.text = str(value)
        # Process field: new_guest_count (XML name: newGuestCount)
        value = getattr(self, "new_guest_count", None)
        if value is not None:  # Skip None values
            # Single element field: new_guest_count
            child_element = SubElement(element, "newGuestCount")
            child_element.text = str(value)
            
        # Process field: order_count_per_guest (XML name: orderCountPerGuest)
        value = getattr(self, "order_count_per_guest", None)
        if value is not None:  # Skip None values
            # Single element field: order_count_per_guest
            child_element = SubElement(element, "orderCountPerGuest")
            child_element.text = str(value)
            
        # Process field: regions (XML name: regions)
        value = getattr(self, "regions", None)
        if value is not None:  # Skip None values
            # Single element field: regions
            child_element = SubElement(element, "regions")
            # Complex object field
            if hasattr(value, "_to_xml_element_internal"):
                value._to_xml_element_internal(child_element)
        # Process field: total_order_count (XML name: totalOrderCount)
        value = getattr(self, "total_order_count", None)
        if value is not None:  # Skip None values
            # Single element field: total_order_count
            child_element = SubElement(element, "totalOrderCount")
            child_element.text = str(value)
            


