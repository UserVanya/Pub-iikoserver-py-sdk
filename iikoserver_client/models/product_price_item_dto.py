# coding: utf-8

"""
    iikoServer API

    API Server позволяет сделать интеграции, которые предназначены для работы с номенклатурой, отчетами, документами, данными сотрудников и приказами.   ### Ограничения и рекомендации - Запросы должны выполняться последовательно друг за другом - Запрашивайте данные за период не длиннее одного месяца, в идеале — за один день или неделю - Для OLAPv2-отчетов рекомендуется устанавливать build-summary=false - При построении OLAPv2-отчета рекомендуется использовать не более 7 полей - Перед выполнением запросов проверяйте их на демо сервере

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import date
from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictFloat, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional, Union
from iikoserver_client.models.include_for_category_dto import IncludeForCategoryDto
from iikoserver_client.models.period_schedule_dto import PeriodScheduleDto
from iikoserver_client.models.price_for_category_dto import PriceForCategoryDto
from typing import Optional, Set, Union
from typing_extensions import Self

# XML support imports
from lxml import etree
from lxml.etree import Element, SubElement

class ProductPriceItemDto(BaseModel):
    """
    Элемент списка цен продукта
    """ # noqa: E501
    date_from: Optional[date] = Field(default=None, description="Начало действия приказа в формате yyyy-MM-dd", alias="dateFrom")
    date_to: Optional[date] = Field(default=None, description="Конец действия приказа в формате yyyy-MM-dd", alias="dateTo")
    price: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Цена")
    include_for_categories: Optional[List[IncludeForCategoryDto]] = Field(default=None, description="Если в этом списке присутствует идентификатор категории, то для данной ценовой категории есть своя специфика ценообразования", alias="includeForCategories")
    prices_for_categories: Optional[List[PriceForCategoryDto]] = Field(default=None, description="Цены для категорий", alias="pricesForCategories")
    included: Optional[StrictBool] = Field(default=None, description="Включен ли продукт в прайс-лист")
    dish_of_day: Optional[StrictBool] = Field(default=None, description="Является ли блюдо хитом", alias="dishOfDay")
    flyer_program: Optional[StrictBool] = Field(default=None, description="Участвует ли блюдо во флаерной программе", alias="flyerProgram")
    document_id: Optional[StrictStr] = Field(default=None, description="Идентификатор приказа", alias="documentId")
    schedule: Optional[PeriodScheduleDto] = None
    tax_category_id: Optional[StrictStr] = Field(default=None, description="Идентификатор налоговой категории (НК)", alias="taxCategoryId")
    tax_category_enabled: Optional[StrictBool] = Field(default=None, description="Включена ли НК", alias="taxCategoryEnabled")
    __properties: ClassVar[List[str]] = ["dateFrom", "dateTo", "price", "includeForCategories", "pricesForCategories", "included", "dishOfDay", "flyerProgram", "documentId", "schedule", "taxCategoryId", "taxCategoryEnabled"]
    
    # XML metadata for the class
    __xml_config: ClassVar[Dict[str, Any]] = {
        "root_name": "ProductPriceItemDto",
        
        
        "field_configs": {
            "date_from": {
                "xml_name": "dateFrom",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "date_to": {
                "xml_name": "dateTo",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "price": {
                "xml_name": "price",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "include_for_categories": {
                "xml_name": "includeForCategories",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "prices_for_categories": {
                "xml_name": "pricesForCategories",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "included": {
                "xml_name": "included",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "dish_of_day": {
                "xml_name": "dishOfDay",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "flyer_program": {
                "xml_name": "flyerProgram",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "document_id": {
                "xml_name": "documentId",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "schedule": {
                "xml_name": "schedule",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "tax_category_id": {
                "xml_name": "taxCategoryId",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "tax_category_enabled": {
                "xml_name": "taxCategoryEnabled",
                
                
                "is_attribute": False,
                "is_wrapped": False
            }
        }
    }


    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ProductPriceItemDto from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in include_for_categories (list)
        _items = []
        if self.include_for_categories:
            for _item_include_for_categories in self.include_for_categories:
                if _item_include_for_categories:
                    _items.append(_item_include_for_categories.to_dict())
            _dict['includeForCategories'] = _items
        # override the default output from pydantic by calling `to_dict()` of each item in prices_for_categories (list)
        _items = []
        if self.prices_for_categories:
            for _item_prices_for_categories in self.prices_for_categories:
                if _item_prices_for_categories:
                    _items.append(_item_prices_for_categories.to_dict())
            _dict['pricesForCategories'] = _items
        # override the default output from pydantic by calling `to_dict()` of schedule
        if self.schedule:
            _dict['schedule'] = self.schedule.to_dict()
        # set to None if price (nullable) is None
        # and model_fields_set contains the field
        if self.price is None and "price" in self.model_fields_set:
            _dict['price'] = None

        # set to None if tax_category_id (nullable) is None
        # and model_fields_set contains the field
        if self.tax_category_id is None and "tax_category_id" in self.model_fields_set:
            _dict['taxCategoryId'] = None

        # set to None if tax_category_enabled (nullable) is None
        # and model_fields_set contains the field
        if self.tax_category_enabled is None and "tax_category_enabled" in self.model_fields_set:
            _dict['taxCategoryEnabled'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ProductPriceItemDto from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "dateFrom": obj.get("dateFrom"),
            "dateTo": obj.get("dateTo"),
            "price": obj.get("price"),
            "includeForCategories": [IncludeForCategoryDto.from_dict(_item) for _item in obj["includeForCategories"]] if obj.get("includeForCategories") is not None else None,
            "pricesForCategories": [PriceForCategoryDto.from_dict(_item) for _item in obj["pricesForCategories"]] if obj.get("pricesForCategories") is not None else None,
            "included": obj.get("included"),
            "dishOfDay": obj.get("dishOfDay"),
            "flyerProgram": obj.get("flyerProgram"),
            "documentId": obj.get("documentId"),
            "schedule": PeriodScheduleDto.from_dict(obj["schedule"]) if obj.get("schedule") is not None else None,
            "taxCategoryId": obj.get("taxCategoryId"),
            "taxCategoryEnabled": obj.get("taxCategoryEnabled")
        })
        return _obj



    @classmethod
    def from_xml(cls, xml_str: str) -> Optional[Self]:
        """Create an instance of ProductPriceItemDto from an XML string"""
        try:
            if isinstance(xml_str, str):
                xml_str = xml_str.encode('utf-8')
            root = etree.fromstring(xml_str)
            return cls.from_xml_element(root)
        except Exception as e:
            raise ValueError(f"Failed to parse XML: {e}")

    @classmethod
    def from_xml_element(cls, element: Element) -> Optional[Self]:
        """Create an instance of ProductPriceItemDto from an XML element"""
        return cls._from_xml_element_internal(element)

    @classmethod
    def _from_xml_element_internal(cls, element: Element) -> Optional[Self]:
        """Internal method to create instance from XML element"""
        if element is None:
            return None

        data = {}
        
        # Process field: date_from (XML name: dateFrom)
        # Handle XML element: date_from
        # Single element field: date_from
        child_element = element.find("dateFrom")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                
                # Handle other primitive types (date, datetime, uuid, etc.)
                # Date field - handle "null" specially
                if text_value == "null":
                    data["dateFrom"] = None
                else:
                    data["dateFrom"] = text_value
                
                
        
        # Process field: date_to (XML name: dateTo)
        # Handle XML element: date_to
        # Single element field: date_to
        child_element = element.find("dateTo")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                
                # Handle other primitive types (date, datetime, uuid, etc.)
                # Date field - handle "null" specially
                if text_value == "null":
                    data["dateTo"] = None
                else:
                    data["dateTo"] = text_value
                
                
        
        # Process field: price (XML name: price)
        # Handle XML element: price
        # Single element field: price
        child_element = element.find("price")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["price"] = float(text_value)
                except ValueError:
                    data["price"] = text_value
                
        
        # Process field: include_for_categories (XML name: includeForCategories)
        # Handle XML element: include_for_categories
        # Array field: include_for_categories
        # Non-wrapped array - direct elements
        # Complex items in non-wrapped array
        child_elements = element.findall("includeForCategories")
        items = []
        for child in child_elements:
            item_obj = IncludeForCategoryDto.from_xml_element(child)
            if item_obj is not None:
                items.append(item_obj)
        data["includeForCategories"] = items
        
        # Process field: prices_for_categories (XML name: pricesForCategories)
        # Handle XML element: prices_for_categories
        # Array field: prices_for_categories
        # Non-wrapped array - direct elements
        # Complex items in non-wrapped array
        child_elements = element.findall("pricesForCategories")
        items = []
        for child in child_elements:
            item_obj = PriceForCategoryDto.from_xml_element(child)
            if item_obj is not None:
                items.append(item_obj)
        data["pricesForCategories"] = items
        
        # Process field: included (XML name: included)
        # Handle XML element: included
        # Single element field: included
        child_element = element.find("included")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["included"] = text_value.lower() == 'true'
                
        
        # Process field: dish_of_day (XML name: dishOfDay)
        # Handle XML element: dish_of_day
        # Single element field: dish_of_day
        child_element = element.find("dishOfDay")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["dishOfDay"] = text_value.lower() == 'true'
                
        
        # Process field: flyer_program (XML name: flyerProgram)
        # Handle XML element: flyer_program
        # Single element field: flyer_program
        child_element = element.find("flyerProgram")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["flyerProgram"] = text_value.lower() == 'true'
                
        
        # Process field: document_id (XML name: documentId)
        # Handle XML element: document_id
        # Single element field: document_id
        child_element = element.find("documentId")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["documentId"] = text_value
                
        
        # Process field: schedule (XML name: schedule)
        # Handle XML element: schedule
        # Single element field: schedule
        child_element = element.find("schedule")
        if child_element is not None:
            # Complex object field
            nested_obj = PeriodScheduleDto.from_xml_element(child_element)
            if nested_obj is not None:
                data["schedule"] = nested_obj
        
        # Process field: tax_category_id (XML name: taxCategoryId)
        # Handle XML element: tax_category_id
        # Single element field: tax_category_id
        child_element = element.find("taxCategoryId")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["taxCategoryId"] = text_value
                
        
        # Process field: tax_category_enabled (XML name: taxCategoryEnabled)
        # Handle XML element: tax_category_enabled
        # Single element field: tax_category_enabled
        child_element = element.find("taxCategoryEnabled")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["taxCategoryEnabled"] = text_value.lower() == 'true'
                
        
        
        return cls.from_dict(data)

    def to_xml(self) -> str:
        """Create an XML string representation of the model"""
        root_element = Element(self.__xml_config.get("root_name", self.__class__.__name__))
        
        # Populate the XML element with model data
        self._to_xml_element_internal(root_element)
        
        # Convert to string with XML declaration
        return etree.tostring(root_element, encoding="utf-8", pretty_print=True, xml_declaration=True).decode("utf-8")

    def _to_xml_element_internal(self, element: Element) -> None:
        """Internal method to populate an XML element with model data"""
        
        # Process field: date_from (XML name: dateFrom)
        value = getattr(self, "date_from", None)
        if value is not None:  # Skip None values
            # Single element field: date_from
            child_element = SubElement(element, "dateFrom")
            
            # Handle other primitive types (date, datetime, uuid, etc.)
            child_element.text = str(value)
            
        # Process field: date_to (XML name: dateTo)
        value = getattr(self, "date_to", None)
        if value is not None:  # Skip None values
            # Single element field: date_to
            child_element = SubElement(element, "dateTo")
            
            # Handle other primitive types (date, datetime, uuid, etc.)
            child_element.text = str(value)
            
        # Process field: price (XML name: price)
        value = getattr(self, "price", None)
        if value is not None:  # Skip None values
            # Single element field: price
            child_element = SubElement(element, "price")
            child_element.text = str(value)
            
        # Process field: include_for_categories (XML name: includeForCategories)
        value = getattr(self, "include_for_categories", None)
        if value is not None:  # Skip None values
            # Array field: include_for_categories
            # Non-wrapped array - add elements directly
            for item in value:
                # Complex items in non-wrapped array
                if hasattr(item, "_to_xml_element_internal"):
                    item_element = SubElement(element, "includeForCategories")
                    item._to_xml_element_internal(item_element)
        # Process field: prices_for_categories (XML name: pricesForCategories)
        value = getattr(self, "prices_for_categories", None)
        if value is not None:  # Skip None values
            # Array field: prices_for_categories
            # Non-wrapped array - add elements directly
            for item in value:
                # Complex items in non-wrapped array
                if hasattr(item, "_to_xml_element_internal"):
                    item_element = SubElement(element, "pricesForCategories")
                    item._to_xml_element_internal(item_element)
        # Process field: included (XML name: included)
        value = getattr(self, "included", None)
        if value is not None:  # Skip None values
            # Single element field: included
            child_element = SubElement(element, "included")
            child_element.text = str(value).lower()
            
        # Process field: dish_of_day (XML name: dishOfDay)
        value = getattr(self, "dish_of_day", None)
        if value is not None:  # Skip None values
            # Single element field: dish_of_day
            child_element = SubElement(element, "dishOfDay")
            child_element.text = str(value).lower()
            
        # Process field: flyer_program (XML name: flyerProgram)
        value = getattr(self, "flyer_program", None)
        if value is not None:  # Skip None values
            # Single element field: flyer_program
            child_element = SubElement(element, "flyerProgram")
            child_element.text = str(value).lower()
            
        # Process field: document_id (XML name: documentId)
        value = getattr(self, "document_id", None)
        if value is not None:  # Skip None values
            # Single element field: document_id
            child_element = SubElement(element, "documentId")
            child_element.text = value
            
        # Process field: schedule (XML name: schedule)
        value = getattr(self, "schedule", None)
        if value is not None:  # Skip None values
            # Single element field: schedule
            child_element = SubElement(element, "schedule")
            # Complex object field
            if hasattr(value, "_to_xml_element_internal"):
                value._to_xml_element_internal(child_element)
        # Process field: tax_category_id (XML name: taxCategoryId)
        value = getattr(self, "tax_category_id", None)
        if value is not None:  # Skip None values
            # Single element field: tax_category_id
            child_element = SubElement(element, "taxCategoryId")
            child_element.text = value
            
        # Process field: tax_category_enabled (XML name: taxCategoryEnabled)
        value = getattr(self, "tax_category_enabled", None)
        if value is not None:  # Skip None values
            # Single element field: tax_category_enabled
            child_element = SubElement(element, "taxCategoryEnabled")
            child_element.text = str(value).lower()
            


