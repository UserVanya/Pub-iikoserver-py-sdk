# coding: utf-8

"""
    iikoServer API

    API Server позволяет сделать интеграции, которые предназначены для работы с номенклатурой, отчетами, документами, данными сотрудников и приказами.   ### Ограничения и рекомендации - Запросы должны выполняться последовательно друг за другом - Запрашивайте данные за период не длиннее одного месяца, в идеале — за один день или неделю - Для OLAPv2-отчетов рекомендуется устанавливать build-summary=false - При построении OLAPv2-отчета рекомендуется использовать не более 7 полей - Перед выполнением запросов проверяйте их на демо сервере

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from iikoserver_client.models.document_status_enum import DocumentStatusEnum
from iikoserver_client.models.internal_transfer_item_dto import InternalTransferItemDto
from typing import Optional, Set, Union
from typing_extensions import Self

# XML support imports
from lxml import etree
from lxml.etree import Element, SubElement

class InternalTransferDto(BaseModel):
    """
    Документ внутреннего перемещения
    """ # noqa: E501
    id: Optional[StrictStr] = Field(default=None, description="Идентификатор документа.")
    date_incoming: datetime = Field(description="Дата проведения документа (учетная) в формате \"yyyy-MM-dd'T'HH:mm\".", alias="dateIncoming")
    document_number: Optional[StrictStr] = Field(default=None, description="Учетный номер документа.", alias="documentNumber")
    status: DocumentStatusEnum
    conception_id: Optional[StrictStr] = Field(default=None, description="Идентификатор концепции.", alias="conceptionId")
    comment: Optional[StrictStr] = Field(default=None, description="Комментарий к документу.")
    store_from_id: StrictStr = Field(description="Идентификатор склада, с которого происходит перемещение.", alias="storeFromId")
    store_to_id: StrictStr = Field(description="Идентификатор склада, на который происходит перемещение.", alias="storeToId")
    items: List[InternalTransferItemDto] = Field(description="Позиции документа.")
    __properties: ClassVar[List[str]] = ["id", "dateIncoming", "documentNumber", "status", "conceptionId", "comment", "storeFromId", "storeToId", "items"]
    
    # XML metadata for the class
    __xml_config: ClassVar[Dict[str, Any]] = {
        "root_name": "InternalTransferDto",
        
        
        "field_configs": {
            "id": {
                "xml_name": "id",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "date_incoming": {
                "xml_name": "dateIncoming",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "document_number": {
                "xml_name": "documentNumber",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "status": {
                "xml_name": "status",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "conception_id": {
                "xml_name": "conceptionId",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "comment": {
                "xml_name": "comment",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "store_from_id": {
                "xml_name": "storeFromId",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "store_to_id": {
                "xml_name": "storeToId",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "items": {
                "xml_name": "items",
                
                
                "is_attribute": False,
                "is_wrapped": False
            }
        }
    }


    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of InternalTransferDto from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in items (list)
        _items = []
        if self.items:
            for _item_items in self.items:
                if _item_items:
                    _items.append(_item_items.to_dict())
            _dict['items'] = _items
        # set to None if id (nullable) is None
        # and model_fields_set contains the field
        if self.id is None and "id" in self.model_fields_set:
            _dict['id'] = None

        # set to None if document_number (nullable) is None
        # and model_fields_set contains the field
        if self.document_number is None and "document_number" in self.model_fields_set:
            _dict['documentNumber'] = None

        # set to None if conception_id (nullable) is None
        # and model_fields_set contains the field
        if self.conception_id is None and "conception_id" in self.model_fields_set:
            _dict['conceptionId'] = None

        # set to None if comment (nullable) is None
        # and model_fields_set contains the field
        if self.comment is None and "comment" in self.model_fields_set:
            _dict['comment'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of InternalTransferDto from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "id": obj.get("id"),
            "dateIncoming": obj.get("dateIncoming"),
            "documentNumber": obj.get("documentNumber"),
            "status": obj.get("status"),
            "conceptionId": obj.get("conceptionId"),
            "comment": obj.get("comment"),
            "storeFromId": obj.get("storeFromId"),
            "storeToId": obj.get("storeToId"),
            "items": [InternalTransferItemDto.from_dict(_item) for _item in obj["items"]] if obj.get("items") is not None else None
        })
        return _obj



    @classmethod
    def from_xml(cls, xml_str: str) -> Optional[Self]:
        """Create an instance of InternalTransferDto from an XML string"""
        try:
            if isinstance(xml_str, str):
                xml_str = xml_str.encode('utf-8')
            root = etree.fromstring(xml_str)
            return cls.from_xml_element(root)
        except Exception as e:
            raise ValueError(f"Failed to parse XML: {e}")

    @classmethod
    def from_xml_element(cls, element: Element) -> Optional[Self]:
        """Create an instance of InternalTransferDto from an XML element"""
        return cls._from_xml_element_internal(element)

    @classmethod
    def _from_xml_element_internal(cls, element: Element) -> Optional[Self]:
        """Internal method to create instance from XML element"""
        if element is None:
            return None

        data = {}
        
        # Process field: id (XML name: id)
        # Handle XML element: id
        # Single element field: id
        child_element = element.find("id")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["id"] = text_value
                
        
        # Process field: date_incoming (XML name: dateIncoming)
        # Handle XML element: date_incoming
        # Single element field: date_incoming
        child_element = element.find("dateIncoming")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                
                # Handle other primitive types (date, datetime, uuid, etc.)
                # DateTime field - handle "null" specially  
                if text_value == "null":
                    data["dateIncoming"] = None
                else:
                    data["dateIncoming"] = text_value
                
                
        
        # Process field: document_number (XML name: documentNumber)
        # Handle XML element: document_number
        # Single element field: document_number
        child_element = element.find("documentNumber")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["documentNumber"] = text_value
                
        
        # Process field: status (XML name: status)
        # Handle XML element: status
        # Single element field: status
        child_element = element.find("status")
        if child_element is not None:
            # Enum field
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["status"] = text_value
        
        # Process field: conception_id (XML name: conceptionId)
        # Handle XML element: conception_id
        # Single element field: conception_id
        child_element = element.find("conceptionId")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["conceptionId"] = text_value
                
        
        # Process field: comment (XML name: comment)
        # Handle XML element: comment
        # Single element field: comment
        child_element = element.find("comment")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["comment"] = text_value
                
        
        # Process field: store_from_id (XML name: storeFromId)
        # Handle XML element: store_from_id
        # Single element field: store_from_id
        child_element = element.find("storeFromId")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["storeFromId"] = text_value
                
        
        # Process field: store_to_id (XML name: storeToId)
        # Handle XML element: store_to_id
        # Single element field: store_to_id
        child_element = element.find("storeToId")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["storeToId"] = text_value
                
        
        # Process field: items (XML name: items)
        # Handle XML element: items
        # Array field: items
        # Non-wrapped array - direct elements
        # Complex items in non-wrapped array
        child_elements = element.findall("items")
        items = []
        for child in child_elements:
            item_obj = InternalTransferItemDto.from_xml_element(child)
            if item_obj is not None:
                items.append(item_obj)
        data["items"] = items
        
        
        return cls.from_dict(data)

    def to_xml(self) -> str:
        """Create an XML string representation of the model"""
        root_element = Element(self.__xml_config.get("root_name", self.__class__.__name__))
        
        # Populate the XML element with model data
        self._to_xml_element_internal(root_element)
        
        # Convert to string with XML declaration
        return etree.tostring(root_element, encoding="utf-8", pretty_print=True, xml_declaration=True).decode("utf-8")

    def _to_xml_element_internal(self, element: Element) -> None:
        """Internal method to populate an XML element with model data"""
        
        # Process field: id (XML name: id)
        value = getattr(self, "id", None)
        if value is not None:  # Skip None values
            # Single element field: id
            child_element = SubElement(element, "id")
            child_element.text = value
            
        # Process field: date_incoming (XML name: dateIncoming)
        value = getattr(self, "date_incoming", None)
        if value is not None:  # Skip None values
            # Single element field: date_incoming
            child_element = SubElement(element, "dateIncoming")
            
            # Handle other primitive types (date, datetime, uuid, etc.)
            child_element.text = str(value)
            
        # Process field: document_number (XML name: documentNumber)
        value = getattr(self, "document_number", None)
        if value is not None:  # Skip None values
            # Single element field: document_number
            child_element = SubElement(element, "documentNumber")
            child_element.text = value
            
        # Process field: status (XML name: status)
        value = getattr(self, "status", None)
        if value is not None:  # Skip None values
            # Single element field: status
            child_element = SubElement(element, "status")
            # Enum field
            child_element.text = str(value)
        # Process field: conception_id (XML name: conceptionId)
        value = getattr(self, "conception_id", None)
        if value is not None:  # Skip None values
            # Single element field: conception_id
            child_element = SubElement(element, "conceptionId")
            child_element.text = value
            
        # Process field: comment (XML name: comment)
        value = getattr(self, "comment", None)
        if value is not None:  # Skip None values
            # Single element field: comment
            child_element = SubElement(element, "comment")
            child_element.text = value
            
        # Process field: store_from_id (XML name: storeFromId)
        value = getattr(self, "store_from_id", None)
        if value is not None:  # Skip None values
            # Single element field: store_from_id
            child_element = SubElement(element, "storeFromId")
            child_element.text = value
            
        # Process field: store_to_id (XML name: storeToId)
        value = getattr(self, "store_to_id", None)
        if value is not None:  # Skip None values
            # Single element field: store_to_id
            child_element = SubElement(element, "storeToId")
            child_element.text = value
            
        # Process field: items (XML name: items)
        value = getattr(self, "items", None)
        if value is not None:  # Skip None values
            # Array field: items
            # Non-wrapped array - add elements directly
            for item in value:
                # Complex items in non-wrapped array
                if hasattr(item, "_to_xml_element_internal"):
                    item_element = SubElement(element, "items")
                    item._to_xml_element_internal(item_element)


