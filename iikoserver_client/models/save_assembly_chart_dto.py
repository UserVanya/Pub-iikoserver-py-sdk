# coding: utf-8

"""
    iikoServer API

    API Server позволяет сделать интеграции, которые предназначены для работы с номенклатурой, отчетами, документами, данными сотрудников и приказами.   ### Ограничения и рекомендации - Запросы должны выполняться последовательно друг за другом - Запрашивайте данные за период не длиннее одного месяца, в идеале — за один день или неделю - Для OLAPv2-отчетов рекомендуется устанавливать build-summary=false - При построении OLAPv2-отчета рекомендуется использовать не более 7 полей - Перед выполнением запросов проверяйте их на демо сервере

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import date
from pydantic import BaseModel, ConfigDict, Field, StrictFloat, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional, Union
from typing_extensions import Annotated
from iikoserver_client.models.base_assembly_chart_item_dto import BaseAssemblyChartItemDto
from iikoserver_client.models.product_size_assembly_strategy import ProductSizeAssemblyStrategy
from iikoserver_client.models.product_writeoff_strategy import ProductWriteoffStrategy
from iikoserver_client.models.store_specification import StoreSpecification
from typing import Optional, Set, Union
from typing_extensions import Self

# XML support imports
from lxml import etree
from lxml.etree import Element, SubElement

class SaveAssemblyChartDto(BaseModel):
    """
    SaveAssemblyChartDto
    """ # noqa: E501
    assembled_product_id: StrictStr = Field(description="UUID приготавливаемого элемента номенклатуры (блюда, модификатора, заготовки) (обязательно).", alias="assembledProductId")
    date_from: date = Field(description="Учетный день начала действия технологической карты: все списания assembledProductId, начиная с 00:00 этого дня, проводятся по данной техкарте.", alias="dateFrom")
    date_to: Optional[date] = Field(default=None, description="Учетный день прекращения действия технологической карты: начиная с 00:00 этого дня списания проводятся по СЛЕДУЮЩЕЙ техкарте. Последняя техкарта действует бессрочно (null).", alias="dateTo")
    assembled_amount: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Норма закладки приготавливаемого блюда.", alias="assembledAmount")
    effective_direct_writeoff_store_specification: Optional[StoreSpecification] = Field(default=None, alias="effectiveDirectWriteoffStoreSpecification")
    product_size_assembly_strategy: Optional[ProductSizeAssemblyStrategy] = Field(default=None, alias="productSizeAssemblyStrategy")
    technology_description: Optional[StrictStr] = Field(default=None, description="Комментарий 'Технология приготовления'.", alias="technologyDescription")
    description: Optional[Annotated[str, Field(strict=True, max_length=2000)]] = Field(default=None, description="Комментарий 'Описание'.")
    appearance: Optional[Annotated[str, Field(strict=True, max_length=2000)]] = Field(default=None, description="Комментарий 'Требования к оформлению, подаче и реализации'.")
    organoleptic: Optional[Annotated[str, Field(strict=True, max_length=2000)]] = Field(default=None, description="Комментарий 'Органолептические показатели качества'.")
    output_comment: Optional[Annotated[str, Field(strict=True, max_length=2000)]] = Field(default=None, description="Суммарный выход.", alias="outputComment")
    product_writeoff_strategy: Optional[ProductWriteoffStrategy] = Field(default=None, alias="productWriteoffStrategy")
    items: List[BaseAssemblyChartItemDto] = Field(description="Массив строк технологической карты.")
    __properties: ClassVar[List[str]] = ["assembledProductId", "dateFrom", "dateTo", "assembledAmount", "effectiveDirectWriteoffStoreSpecification", "productSizeAssemblyStrategy", "technologyDescription", "description", "appearance", "organoleptic", "outputComment", "productWriteoffStrategy", "items"]
    
    # XML metadata for the class
    __xml_config: ClassVar[Dict[str, Any]] = {
        "root_name": "SaveAssemblyChartDto",
        
        
        "field_configs": {
            "assembled_product_id": {
                "xml_name": "assembledProductId",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "date_from": {
                "xml_name": "dateFrom",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "date_to": {
                "xml_name": "dateTo",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "assembled_amount": {
                "xml_name": "assembledAmount",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "effective_direct_writeoff_store_specification": {
                "xml_name": "effectiveDirectWriteoffStoreSpecification",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "product_size_assembly_strategy": {
                "xml_name": "productSizeAssemblyStrategy",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "technology_description": {
                "xml_name": "technologyDescription",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "description": {
                "xml_name": "description",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "appearance": {
                "xml_name": "appearance",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "organoleptic": {
                "xml_name": "organoleptic",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "output_comment": {
                "xml_name": "outputComment",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "product_writeoff_strategy": {
                "xml_name": "productWriteoffStrategy",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "items": {
                "xml_name": "items",
                
                
                "is_attribute": False,
                "is_wrapped": False
            }
        }
    }


    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of SaveAssemblyChartDto from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of effective_direct_writeoff_store_specification
        if self.effective_direct_writeoff_store_specification:
            _dict['effectiveDirectWriteoffStoreSpecification'] = self.effective_direct_writeoff_store_specification.to_dict()
        # override the default output from pydantic by calling `to_dict()` of each item in items (list)
        _items = []
        if self.items:
            for _item_items in self.items:
                if _item_items:
                    _items.append(_item_items.to_dict())
            _dict['items'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of SaveAssemblyChartDto from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "assembledProductId": obj.get("assembledProductId"),
            "dateFrom": obj.get("dateFrom"),
            "dateTo": obj.get("dateTo"),
            "assembledAmount": obj.get("assembledAmount"),
            "effectiveDirectWriteoffStoreSpecification": StoreSpecification.from_dict(obj["effectiveDirectWriteoffStoreSpecification"]) if obj.get("effectiveDirectWriteoffStoreSpecification") is not None else None,
            "productSizeAssemblyStrategy": obj.get("productSizeAssemblyStrategy"),
            "technologyDescription": obj.get("technologyDescription"),
            "description": obj.get("description"),
            "appearance": obj.get("appearance"),
            "organoleptic": obj.get("organoleptic"),
            "outputComment": obj.get("outputComment"),
            "productWriteoffStrategy": obj.get("productWriteoffStrategy"),
            "items": [BaseAssemblyChartItemDto.from_dict(_item) for _item in obj["items"]] if obj.get("items") is not None else None
        })
        return _obj



    @classmethod
    def from_xml(cls, xml_str: str) -> Optional[Self]:
        """Create an instance of SaveAssemblyChartDto from an XML string"""
        try:
            if isinstance(xml_str, str):
                xml_str = xml_str.encode('utf-8')
            root = etree.fromstring(xml_str)
            return cls.from_xml_element(root)
        except Exception as e:
            raise ValueError(f"Failed to parse XML: {e}")

    @classmethod
    def from_xml_element(cls, element: Element) -> Optional[Self]:
        """Create an instance of SaveAssemblyChartDto from an XML element"""
        return cls._from_xml_element_internal(element)

    @classmethod
    def _from_xml_element_internal(cls, element: Element) -> Optional[Self]:
        """Internal method to create instance from XML element"""
        if element is None:
            return None

        data = {}
        
        # Process field: assembled_product_id (XML name: assembledProductId)
        # Handle XML element: assembled_product_id
        # Single element field: assembled_product_id
        child_element = element.find("assembledProductId")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["assembledProductId"] = text_value
                
        
        # Process field: date_from (XML name: dateFrom)
        # Handle XML element: date_from
        # Single element field: date_from
        child_element = element.find("dateFrom")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                
                # Handle other primitive types (date, datetime, uuid, etc.)
                # Date field - handle "null" specially
                if text_value == "null":
                    data["dateFrom"] = None
                else:
                    data["dateFrom"] = text_value
                
                
        
        # Process field: date_to (XML name: dateTo)
        # Handle XML element: date_to
        # Single element field: date_to
        child_element = element.find("dateTo")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                
                # Handle other primitive types (date, datetime, uuid, etc.)
                # Date field - handle "null" specially
                if text_value == "null":
                    data["dateTo"] = None
                else:
                    data["dateTo"] = text_value
                
                
        
        # Process field: assembled_amount (XML name: assembledAmount)
        # Handle XML element: assembled_amount
        # Single element field: assembled_amount
        child_element = element.find("assembledAmount")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["assembledAmount"] = float(text_value)
                except ValueError:
                    data["assembledAmount"] = text_value
                
        
        # Process field: effective_direct_writeoff_store_specification (XML name: effectiveDirectWriteoffStoreSpecification)
        # Handle XML element: effective_direct_writeoff_store_specification
        # Single element field: effective_direct_writeoff_store_specification
        child_element = element.find("effectiveDirectWriteoffStoreSpecification")
        if child_element is not None:
            # Complex object field
            nested_obj = StoreSpecification.from_xml_element(child_element)
            if nested_obj is not None:
                data["effectiveDirectWriteoffStoreSpecification"] = nested_obj
        
        # Process field: product_size_assembly_strategy (XML name: productSizeAssemblyStrategy)
        # Handle XML element: product_size_assembly_strategy
        # Single element field: product_size_assembly_strategy
        child_element = element.find("productSizeAssemblyStrategy")
        if child_element is not None:
            # Enum field
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["productSizeAssemblyStrategy"] = text_value
        
        # Process field: technology_description (XML name: technologyDescription)
        # Handle XML element: technology_description
        # Single element field: technology_description
        child_element = element.find("technologyDescription")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["technologyDescription"] = text_value
                
        
        # Process field: description (XML name: description)
        # Handle XML element: description
        # Single element field: description
        child_element = element.find("description")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["description"] = text_value
                
        
        # Process field: appearance (XML name: appearance)
        # Handle XML element: appearance
        # Single element field: appearance
        child_element = element.find("appearance")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["appearance"] = text_value
                
        
        # Process field: organoleptic (XML name: organoleptic)
        # Handle XML element: organoleptic
        # Single element field: organoleptic
        child_element = element.find("organoleptic")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["organoleptic"] = text_value
                
        
        # Process field: output_comment (XML name: outputComment)
        # Handle XML element: output_comment
        # Single element field: output_comment
        child_element = element.find("outputComment")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["outputComment"] = text_value
                
        
        # Process field: product_writeoff_strategy (XML name: productWriteoffStrategy)
        # Handle XML element: product_writeoff_strategy
        # Single element field: product_writeoff_strategy
        child_element = element.find("productWriteoffStrategy")
        if child_element is not None:
            # Enum field
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["productWriteoffStrategy"] = text_value
        
        # Process field: items (XML name: items)
        # Handle XML element: items
        # Array field: items
        # Non-wrapped array - direct elements
        # Complex items in non-wrapped array
        child_elements = element.findall("items")
        items = []
        for child in child_elements:
            item_obj = BaseAssemblyChartItemDto.from_xml_element(child)
            if item_obj is not None:
                items.append(item_obj)
        data["items"] = items
        
        
        return cls.from_dict(data)

    def to_xml(self) -> str:
        """Create an XML string representation of the model"""
        root_element = Element(self.__xml_config.get("root_name", self.__class__.__name__))
        
        # Populate the XML element with model data
        self._to_xml_element_internal(root_element)
        
        # Convert to string with XML declaration
        return etree.tostring(root_element, encoding="utf-8", pretty_print=True, xml_declaration=True).decode("utf-8")

    def _to_xml_element_internal(self, element: Element) -> None:
        """Internal method to populate an XML element with model data"""
        
        # Process field: assembled_product_id (XML name: assembledProductId)
        value = getattr(self, "assembled_product_id", None)
        if value is not None:  # Skip None values
            # Single element field: assembled_product_id
            child_element = SubElement(element, "assembledProductId")
            child_element.text = value
            
        # Process field: date_from (XML name: dateFrom)
        value = getattr(self, "date_from", None)
        if value is not None:  # Skip None values
            # Single element field: date_from
            child_element = SubElement(element, "dateFrom")
            
            # Handle other primitive types (date, datetime, uuid, etc.)
            child_element.text = str(value)
            
        # Process field: date_to (XML name: dateTo)
        value = getattr(self, "date_to", None)
        if value is not None:  # Skip None values
            # Single element field: date_to
            child_element = SubElement(element, "dateTo")
            
            # Handle other primitive types (date, datetime, uuid, etc.)
            child_element.text = str(value)
            
        # Process field: assembled_amount (XML name: assembledAmount)
        value = getattr(self, "assembled_amount", None)
        if value is not None:  # Skip None values
            # Single element field: assembled_amount
            child_element = SubElement(element, "assembledAmount")
            child_element.text = str(value)
            
        # Process field: effective_direct_writeoff_store_specification (XML name: effectiveDirectWriteoffStoreSpecification)
        value = getattr(self, "effective_direct_writeoff_store_specification", None)
        if value is not None:  # Skip None values
            # Single element field: effective_direct_writeoff_store_specification
            child_element = SubElement(element, "effectiveDirectWriteoffStoreSpecification")
            # Complex object field
            if hasattr(value, "_to_xml_element_internal"):
                value._to_xml_element_internal(child_element)
        # Process field: product_size_assembly_strategy (XML name: productSizeAssemblyStrategy)
        value = getattr(self, "product_size_assembly_strategy", None)
        if value is not None:  # Skip None values
            # Single element field: product_size_assembly_strategy
            child_element = SubElement(element, "productSizeAssemblyStrategy")
            # Enum field
            child_element.text = str(value)
        # Process field: technology_description (XML name: technologyDescription)
        value = getattr(self, "technology_description", None)
        if value is not None:  # Skip None values
            # Single element field: technology_description
            child_element = SubElement(element, "technologyDescription")
            child_element.text = value
            
        # Process field: description (XML name: description)
        value = getattr(self, "description", None)
        if value is not None:  # Skip None values
            # Single element field: description
            child_element = SubElement(element, "description")
            child_element.text = value
            
        # Process field: appearance (XML name: appearance)
        value = getattr(self, "appearance", None)
        if value is not None:  # Skip None values
            # Single element field: appearance
            child_element = SubElement(element, "appearance")
            child_element.text = value
            
        # Process field: organoleptic (XML name: organoleptic)
        value = getattr(self, "organoleptic", None)
        if value is not None:  # Skip None values
            # Single element field: organoleptic
            child_element = SubElement(element, "organoleptic")
            child_element.text = value
            
        # Process field: output_comment (XML name: outputComment)
        value = getattr(self, "output_comment", None)
        if value is not None:  # Skip None values
            # Single element field: output_comment
            child_element = SubElement(element, "outputComment")
            child_element.text = value
            
        # Process field: product_writeoff_strategy (XML name: productWriteoffStrategy)
        value = getattr(self, "product_writeoff_strategy", None)
        if value is not None:  # Skip None values
            # Single element field: product_writeoff_strategy
            child_element = SubElement(element, "productWriteoffStrategy")
            # Enum field
            child_element.text = str(value)
        # Process field: items (XML name: items)
        value = getattr(self, "items", None)
        if value is not None:  # Skip None values
            # Array field: items
            # Non-wrapped array - add elements directly
            for item in value:
                # Complex items in non-wrapped array
                if hasattr(item, "_to_xml_element_internal"):
                    item_element = SubElement(element, "items")
                    item._to_xml_element_internal(item_element)


