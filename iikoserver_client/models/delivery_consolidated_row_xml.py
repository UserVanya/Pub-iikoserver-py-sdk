# coding: utf-8

"""
    iikoServer API

    API Server позволяет сделать интеграции, которые предназначены для работы с номенклатурой, отчетами, документами, данными сотрудников и приказами.   ### Ограничения и рекомендации - Запросы должны выполняться последовательно друг за другом - Запрашивайте данные за период не длиннее одного месяца, в идеале — за один день или неделю - Для OLAPv2-отчетов рекомендуется устанавливать build-summary=false - При построении OLAPv2-отчета рекомендуется использовать не более 7 полей - Перед выполнением запросов проверяйте их на демо сервере

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictFloat, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional, Union
from typing import Optional, Set, Union
from typing_extensions import Self

# XML support imports
from lxml import etree
from lxml.etree import Element, SubElement

class DeliveryConsolidatedRowXml(BaseModel):
    """
    DeliveryConsolidatedRowXml
    """ # noqa: E501
    avg_receipt: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Средний чек", alias="avgReceipt")
    var_date: Optional[StrictStr] = Field(default=None, description="Дата", alias="date")
    dish_amount: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Количество блюд", alias="dishAmount")
    dish_amount_per_order: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Количество блюд в чеке", alias="dishAmountPerOrder")
    order_count: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Количество заказов", alias="orderCount")
    order_count_courier: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Заказов курьер", alias="orderCountCourier")
    order_count_pickup: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Заказов с собой", alias="orderCountPickup")
    plan_execution_percent: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="% выполнения бюджета", alias="planExecutionPercent")
    ratio_cost_writeoff: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="% списания", alias="ratioCostWriteoff")
    revenue: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Выручка")
    __properties: ClassVar[List[str]] = ["avgReceipt", "date", "dishAmount", "dishAmountPerOrder", "orderCount", "orderCountCourier", "orderCountPickup", "planExecutionPercent", "ratioCostWriteoff", "revenue"]
    
    # XML metadata for the class
    __xml_config: ClassVar[Dict[str, Any]] = {
        "root_name": "row",
        
        
        "field_configs": {
            "avg_receipt": {
                "xml_name": "avgReceipt",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "var_date": {
                "xml_name": "date",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "dish_amount": {
                "xml_name": "dishAmount",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "dish_amount_per_order": {
                "xml_name": "dishAmountPerOrder",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "order_count": {
                "xml_name": "orderCount",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "order_count_courier": {
                "xml_name": "orderCountCourier",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "order_count_pickup": {
                "xml_name": "orderCountPickup",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "plan_execution_percent": {
                "xml_name": "planExecutionPercent",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "ratio_cost_writeoff": {
                "xml_name": "ratioCostWriteoff",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "revenue": {
                "xml_name": "revenue",
                
                
                "is_attribute": False,
                "is_wrapped": False
            }
        }
    }


    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of DeliveryConsolidatedRowXml from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # set to None if avg_receipt (nullable) is None
        # and model_fields_set contains the field
        if self.avg_receipt is None and "avg_receipt" in self.model_fields_set:
            _dict['avgReceipt'] = None

        # set to None if var_date (nullable) is None
        # and model_fields_set contains the field
        if self.var_date is None and "var_date" in self.model_fields_set:
            _dict['date'] = None

        # set to None if dish_amount (nullable) is None
        # and model_fields_set contains the field
        if self.dish_amount is None and "dish_amount" in self.model_fields_set:
            _dict['dishAmount'] = None

        # set to None if dish_amount_per_order (nullable) is None
        # and model_fields_set contains the field
        if self.dish_amount_per_order is None and "dish_amount_per_order" in self.model_fields_set:
            _dict['dishAmountPerOrder'] = None

        # set to None if order_count (nullable) is None
        # and model_fields_set contains the field
        if self.order_count is None and "order_count" in self.model_fields_set:
            _dict['orderCount'] = None

        # set to None if order_count_courier (nullable) is None
        # and model_fields_set contains the field
        if self.order_count_courier is None and "order_count_courier" in self.model_fields_set:
            _dict['orderCountCourier'] = None

        # set to None if order_count_pickup (nullable) is None
        # and model_fields_set contains the field
        if self.order_count_pickup is None and "order_count_pickup" in self.model_fields_set:
            _dict['orderCountPickup'] = None

        # set to None if plan_execution_percent (nullable) is None
        # and model_fields_set contains the field
        if self.plan_execution_percent is None and "plan_execution_percent" in self.model_fields_set:
            _dict['planExecutionPercent'] = None

        # set to None if ratio_cost_writeoff (nullable) is None
        # and model_fields_set contains the field
        if self.ratio_cost_writeoff is None and "ratio_cost_writeoff" in self.model_fields_set:
            _dict['ratioCostWriteoff'] = None

        # set to None if revenue (nullable) is None
        # and model_fields_set contains the field
        if self.revenue is None and "revenue" in self.model_fields_set:
            _dict['revenue'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of DeliveryConsolidatedRowXml from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "avgReceipt": obj.get("avgReceipt"),
            "date": obj.get("date"),
            "dishAmount": obj.get("dishAmount"),
            "dishAmountPerOrder": obj.get("dishAmountPerOrder"),
            "orderCount": obj.get("orderCount"),
            "orderCountCourier": obj.get("orderCountCourier"),
            "orderCountPickup": obj.get("orderCountPickup"),
            "planExecutionPercent": obj.get("planExecutionPercent"),
            "ratioCostWriteoff": obj.get("ratioCostWriteoff"),
            "revenue": obj.get("revenue")
        })
        return _obj



    @classmethod
    def from_xml(cls, xml_str: str) -> Optional[Self]:
        """Create an instance of DeliveryConsolidatedRowXml from an XML string"""
        try:
            if isinstance(xml_str, str):
                xml_str = xml_str.encode('utf-8')
            root = etree.fromstring(xml_str)
            return cls.from_xml_element(root)
        except Exception as e:
            raise ValueError(f"Failed to parse XML: {e}")

    @classmethod
    def from_xml_element(cls, element: Element) -> Optional[Self]:
        """Create an instance of DeliveryConsolidatedRowXml from an XML element"""
        return cls._from_xml_element_internal(element)

    @classmethod
    def _from_xml_element_internal(cls, element: Element) -> Optional[Self]:
        """Internal method to create instance from XML element"""
        if element is None:
            return None

        data = {}
        
        # Process field: avg_receipt (XML name: avgReceipt)
        # Handle XML element: avg_receipt
        # Single element field: avg_receipt
        child_element = element.find("avgReceipt")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["avgReceipt"] = float(text_value)
                except ValueError:
                    data["avgReceipt"] = text_value
                
        
        # Process field: var_date (XML name: date)
        # Handle XML element: var_date
        # Single element field: var_date
        child_element = element.find("date")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["date"] = text_value
                
        
        # Process field: dish_amount (XML name: dishAmount)
        # Handle XML element: dish_amount
        # Single element field: dish_amount
        child_element = element.find("dishAmount")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["dishAmount"] = float(text_value)
                except ValueError:
                    data["dishAmount"] = text_value
                
        
        # Process field: dish_amount_per_order (XML name: dishAmountPerOrder)
        # Handle XML element: dish_amount_per_order
        # Single element field: dish_amount_per_order
        child_element = element.find("dishAmountPerOrder")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["dishAmountPerOrder"] = float(text_value)
                except ValueError:
                    data["dishAmountPerOrder"] = text_value
                
        
        # Process field: order_count (XML name: orderCount)
        # Handle XML element: order_count
        # Single element field: order_count
        child_element = element.find("orderCount")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["orderCount"] = float(text_value)
                except ValueError:
                    data["orderCount"] = text_value
                
        
        # Process field: order_count_courier (XML name: orderCountCourier)
        # Handle XML element: order_count_courier
        # Single element field: order_count_courier
        child_element = element.find("orderCountCourier")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["orderCountCourier"] = float(text_value)
                except ValueError:
                    data["orderCountCourier"] = text_value
                
        
        # Process field: order_count_pickup (XML name: orderCountPickup)
        # Handle XML element: order_count_pickup
        # Single element field: order_count_pickup
        child_element = element.find("orderCountPickup")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["orderCountPickup"] = float(text_value)
                except ValueError:
                    data["orderCountPickup"] = text_value
                
        
        # Process field: plan_execution_percent (XML name: planExecutionPercent)
        # Handle XML element: plan_execution_percent
        # Single element field: plan_execution_percent
        child_element = element.find("planExecutionPercent")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["planExecutionPercent"] = float(text_value)
                except ValueError:
                    data["planExecutionPercent"] = text_value
                
        
        # Process field: ratio_cost_writeoff (XML name: ratioCostWriteoff)
        # Handle XML element: ratio_cost_writeoff
        # Single element field: ratio_cost_writeoff
        child_element = element.find("ratioCostWriteoff")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["ratioCostWriteoff"] = float(text_value)
                except ValueError:
                    data["ratioCostWriteoff"] = text_value
                
        
        # Process field: revenue (XML name: revenue)
        # Handle XML element: revenue
        # Single element field: revenue
        child_element = element.find("revenue")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["revenue"] = float(text_value)
                except ValueError:
                    data["revenue"] = text_value
                
        
        
        return cls.from_dict(data)

    def to_xml(self) -> str:
        """Create an XML string representation of the model"""
        root_element = Element(self.__xml_config.get("root_name", self.__class__.__name__))
        
        # Populate the XML element with model data
        self._to_xml_element_internal(root_element)
        
        # Convert to string with XML declaration
        return etree.tostring(root_element, encoding="utf-8", pretty_print=True, xml_declaration=True).decode("utf-8")

    def _to_xml_element_internal(self, element: Element) -> None:
        """Internal method to populate an XML element with model data"""
        
        # Process field: avg_receipt (XML name: avgReceipt)
        value = getattr(self, "avg_receipt", None)
        if value is not None:  # Skip None values
            # Single element field: avg_receipt
            child_element = SubElement(element, "avgReceipt")
            child_element.text = str(value)
            
        # Process field: var_date (XML name: date)
        value = getattr(self, "var_date", None)
        if value is not None:  # Skip None values
            # Single element field: var_date
            child_element = SubElement(element, "date")
            child_element.text = value
            
        # Process field: dish_amount (XML name: dishAmount)
        value = getattr(self, "dish_amount", None)
        if value is not None:  # Skip None values
            # Single element field: dish_amount
            child_element = SubElement(element, "dishAmount")
            child_element.text = str(value)
            
        # Process field: dish_amount_per_order (XML name: dishAmountPerOrder)
        value = getattr(self, "dish_amount_per_order", None)
        if value is not None:  # Skip None values
            # Single element field: dish_amount_per_order
            child_element = SubElement(element, "dishAmountPerOrder")
            child_element.text = str(value)
            
        # Process field: order_count (XML name: orderCount)
        value = getattr(self, "order_count", None)
        if value is not None:  # Skip None values
            # Single element field: order_count
            child_element = SubElement(element, "orderCount")
            child_element.text = str(value)
            
        # Process field: order_count_courier (XML name: orderCountCourier)
        value = getattr(self, "order_count_courier", None)
        if value is not None:  # Skip None values
            # Single element field: order_count_courier
            child_element = SubElement(element, "orderCountCourier")
            child_element.text = str(value)
            
        # Process field: order_count_pickup (XML name: orderCountPickup)
        value = getattr(self, "order_count_pickup", None)
        if value is not None:  # Skip None values
            # Single element field: order_count_pickup
            child_element = SubElement(element, "orderCountPickup")
            child_element.text = str(value)
            
        # Process field: plan_execution_percent (XML name: planExecutionPercent)
        value = getattr(self, "plan_execution_percent", None)
        if value is not None:  # Skip None values
            # Single element field: plan_execution_percent
            child_element = SubElement(element, "planExecutionPercent")
            child_element.text = str(value)
            
        # Process field: ratio_cost_writeoff (XML name: ratioCostWriteoff)
        value = getattr(self, "ratio_cost_writeoff", None)
        if value is not None:  # Skip None values
            # Single element field: ratio_cost_writeoff
            child_element = SubElement(element, "ratioCostWriteoff")
            child_element.text = str(value)
            
        # Process field: revenue (XML name: revenue)
        value = getattr(self, "revenue", None)
        if value is not None:  # Skip None values
            # Single element field: revenue
            child_element = SubElement(element, "revenue")
            child_element.text = str(value)
            


