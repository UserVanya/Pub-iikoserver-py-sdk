# coding: utf-8

"""
    iikoServer API

    API Server позволяет сделать интеграции, которые предназначены для работы с номенклатурой, отчетами, документами, данными сотрудников и приказами.   ### Ограничения и рекомендации - Запросы должны выполняться последовательно друг за другом - Запрашивайте данные за период не длиннее одного месяца, в идеале — за один день или неделю - Для OLAPv2-отчетов рекомендуется устанавливать build-summary=false - При построении OLAPv2-отчета рекомендуется использовать не более 7 полей - Перед выполнением запросов проверяйте их на демо сервере

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictFloat, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional, Union
from typing import Optional, Set, Union
from typing_extensions import Self

# XML support imports
from lxml import etree
from lxml.etree import Element, SubElement

class OutgoingInvoiceItemXml(BaseModel):
    """
    OutgoingInvoiceItemXml
    """ # noqa: E501
    product_id: Optional[StrictStr] = Field(default=None, description="Элемент номенклатуры (id)", alias="productId")
    product_article: Optional[StrictStr] = Field(default=None, description="Элемент номенклатуры (код/артикул)", alias="productArticle")
    store_id: Optional[StrictStr] = Field(default=None, description="Склад (id). При создании накладных с проведением обязателен. Заполняется либо в документе, либо в каждой строке отдельно, но не одновременно.", alias="storeId")
    store_code: Optional[StrictStr] = Field(default=None, description="Склад (код). При создании накладных с проведением обязателен. Заполняется либо в документе, либо в каждой строке отдельно, но не одновременно.", alias="storeCode")
    container_id: Optional[StrictStr] = Field(default=None, description="Фасовка (id)", alias="containerId")
    container_code: Optional[StrictStr] = Field(default=None, description="Фасовка (код/артикул)", alias="containerCode")
    price: Union[StrictFloat, StrictInt] = Field(description="Цена за фасовку с учетом скидки")
    price_without_vat: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Цена без НДС за фасовку с учетом скидки (только чтение) @since 6.2", alias="priceWithoutVat")
    amount: Union[StrictFloat, StrictInt] = Field(description="Количество в базовых единицах измерения")
    sum: Union[StrictFloat, StrictInt] = Field(description="Cумма строки без учета скидки. Как правило sum == amount * price / container + discountSum + vatSum")
    discount_sum: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Cумма скидки", alias="discountSum")
    vat_percent: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Величина процента НДС. Если не задана, он берется из карточки товара. @since 5.0", alias="vatPercent")
    vat_sum: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Сумма НДС для строки документа. Если не задана, она вычисляется по проценту. Нельзя задать только сумму, не задавая процент. @since 5.0", alias="vatSum")
    __properties: ClassVar[List[str]] = ["productId", "productArticle", "storeId", "storeCode", "containerId", "containerCode", "price", "priceWithoutVat", "amount", "sum", "discountSum", "vatPercent", "vatSum"]
    
    # XML metadata for the class
    __xml_config: ClassVar[Dict[str, Any]] = {
        "root_name": "item",
        
        
        "field_configs": {
            "product_id": {
                "xml_name": "productId",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "product_article": {
                "xml_name": "productArticle",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "store_id": {
                "xml_name": "storeId",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "store_code": {
                "xml_name": "storeCode",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "container_id": {
                "xml_name": "containerId",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "container_code": {
                "xml_name": "containerCode",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "price": {
                "xml_name": "price",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "price_without_vat": {
                "xml_name": "priceWithoutVat",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "amount": {
                "xml_name": "amount",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "sum": {
                "xml_name": "sum",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "discount_sum": {
                "xml_name": "discountSum",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "vat_percent": {
                "xml_name": "vatPercent",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "vat_sum": {
                "xml_name": "vatSum",
                
                
                "is_attribute": False,
                "is_wrapped": False
            }
        }
    }


    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of OutgoingInvoiceItemXml from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # set to None if product_id (nullable) is None
        # and model_fields_set contains the field
        if self.product_id is None and "product_id" in self.model_fields_set:
            _dict['productId'] = None

        # set to None if product_article (nullable) is None
        # and model_fields_set contains the field
        if self.product_article is None and "product_article" in self.model_fields_set:
            _dict['productArticle'] = None

        # set to None if store_id (nullable) is None
        # and model_fields_set contains the field
        if self.store_id is None and "store_id" in self.model_fields_set:
            _dict['storeId'] = None

        # set to None if store_code (nullable) is None
        # and model_fields_set contains the field
        if self.store_code is None and "store_code" in self.model_fields_set:
            _dict['storeCode'] = None

        # set to None if container_id (nullable) is None
        # and model_fields_set contains the field
        if self.container_id is None and "container_id" in self.model_fields_set:
            _dict['containerId'] = None

        # set to None if container_code (nullable) is None
        # and model_fields_set contains the field
        if self.container_code is None and "container_code" in self.model_fields_set:
            _dict['containerCode'] = None

        # set to None if discount_sum (nullable) is None
        # and model_fields_set contains the field
        if self.discount_sum is None and "discount_sum" in self.model_fields_set:
            _dict['discountSum'] = None

        # set to None if vat_percent (nullable) is None
        # and model_fields_set contains the field
        if self.vat_percent is None and "vat_percent" in self.model_fields_set:
            _dict['vatPercent'] = None

        # set to None if vat_sum (nullable) is None
        # and model_fields_set contains the field
        if self.vat_sum is None and "vat_sum" in self.model_fields_set:
            _dict['vatSum'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of OutgoingInvoiceItemXml from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "productId": obj.get("productId"),
            "productArticle": obj.get("productArticle"),
            "storeId": obj.get("storeId"),
            "storeCode": obj.get("storeCode"),
            "containerId": obj.get("containerId"),
            "containerCode": obj.get("containerCode"),
            "price": obj.get("price"),
            "priceWithoutVat": obj.get("priceWithoutVat"),
            "amount": obj.get("amount"),
            "sum": obj.get("sum"),
            "discountSum": obj.get("discountSum"),
            "vatPercent": obj.get("vatPercent"),
            "vatSum": obj.get("vatSum")
        })
        return _obj



    @classmethod
    def from_xml(cls, xml_str: str) -> Optional[Self]:
        """Create an instance of OutgoingInvoiceItemXml from an XML string"""
        try:
            if isinstance(xml_str, str):
                xml_str = xml_str.encode('utf-8')
            root = etree.fromstring(xml_str)
            return cls.from_xml_element(root)
        except Exception as e:
            raise ValueError(f"Failed to parse XML: {e}")

    @classmethod
    def from_xml_element(cls, element: Element) -> Optional[Self]:
        """Create an instance of OutgoingInvoiceItemXml from an XML element"""
        return cls._from_xml_element_internal(element)

    @classmethod
    def _from_xml_element_internal(cls, element: Element) -> Optional[Self]:
        """Internal method to create instance from XML element"""
        if element is None:
            return None

        data = {}
        
        # Process field: product_id (XML name: productId)
        # Handle XML element: product_id
        # Single element field: product_id
        child_element = element.find("productId")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["productId"] = text_value
                
        
        # Process field: product_article (XML name: productArticle)
        # Handle XML element: product_article
        # Single element field: product_article
        child_element = element.find("productArticle")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["productArticle"] = text_value
                
        
        # Process field: store_id (XML name: storeId)
        # Handle XML element: store_id
        # Single element field: store_id
        child_element = element.find("storeId")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["storeId"] = text_value
                
        
        # Process field: store_code (XML name: storeCode)
        # Handle XML element: store_code
        # Single element field: store_code
        child_element = element.find("storeCode")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["storeCode"] = text_value
                
        
        # Process field: container_id (XML name: containerId)
        # Handle XML element: container_id
        # Single element field: container_id
        child_element = element.find("containerId")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["containerId"] = text_value
                
        
        # Process field: container_code (XML name: containerCode)
        # Handle XML element: container_code
        # Single element field: container_code
        child_element = element.find("containerCode")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["containerCode"] = text_value
                
        
        # Process field: price (XML name: price)
        # Handle XML element: price
        # Single element field: price
        child_element = element.find("price")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["price"] = float(text_value)
                except ValueError:
                    data["price"] = text_value
                
        
        # Process field: price_without_vat (XML name: priceWithoutVat)
        # Handle XML element: price_without_vat
        # Single element field: price_without_vat
        child_element = element.find("priceWithoutVat")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["priceWithoutVat"] = float(text_value)
                except ValueError:
                    data["priceWithoutVat"] = text_value
                
        
        # Process field: amount (XML name: amount)
        # Handle XML element: amount
        # Single element field: amount
        child_element = element.find("amount")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["amount"] = float(text_value)
                except ValueError:
                    data["amount"] = text_value
                
        
        # Process field: sum (XML name: sum)
        # Handle XML element: sum
        # Single element field: sum
        child_element = element.find("sum")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["sum"] = float(text_value)
                except ValueError:
                    data["sum"] = text_value
                
        
        # Process field: discount_sum (XML name: discountSum)
        # Handle XML element: discount_sum
        # Single element field: discount_sum
        child_element = element.find("discountSum")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["discountSum"] = float(text_value)
                except ValueError:
                    data["discountSum"] = text_value
                
        
        # Process field: vat_percent (XML name: vatPercent)
        # Handle XML element: vat_percent
        # Single element field: vat_percent
        child_element = element.find("vatPercent")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["vatPercent"] = float(text_value)
                except ValueError:
                    data["vatPercent"] = text_value
                
        
        # Process field: vat_sum (XML name: vatSum)
        # Handle XML element: vat_sum
        # Single element field: vat_sum
        child_element = element.find("vatSum")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["vatSum"] = float(text_value)
                except ValueError:
                    data["vatSum"] = text_value
                
        
        
        return cls.from_dict(data)

    def to_xml(self) -> str:
        """Create an XML string representation of the model"""
        root_element = Element(self.__xml_config.get("root_name", self.__class__.__name__))
        
        # Populate the XML element with model data
        self._to_xml_element_internal(root_element)
        
        # Convert to string with XML declaration
        return etree.tostring(root_element, encoding="utf-8", pretty_print=True, xml_declaration=True).decode("utf-8")

    def _to_xml_element_internal(self, element: Element) -> None:
        """Internal method to populate an XML element with model data"""
        
        # Process field: product_id (XML name: productId)
        value = getattr(self, "product_id", None)
        if value is not None:  # Skip None values
            # Single element field: product_id
            child_element = SubElement(element, "productId")
            child_element.text = value
            
        # Process field: product_article (XML name: productArticle)
        value = getattr(self, "product_article", None)
        if value is not None:  # Skip None values
            # Single element field: product_article
            child_element = SubElement(element, "productArticle")
            child_element.text = value
            
        # Process field: store_id (XML name: storeId)
        value = getattr(self, "store_id", None)
        if value is not None:  # Skip None values
            # Single element field: store_id
            child_element = SubElement(element, "storeId")
            child_element.text = value
            
        # Process field: store_code (XML name: storeCode)
        value = getattr(self, "store_code", None)
        if value is not None:  # Skip None values
            # Single element field: store_code
            child_element = SubElement(element, "storeCode")
            child_element.text = value
            
        # Process field: container_id (XML name: containerId)
        value = getattr(self, "container_id", None)
        if value is not None:  # Skip None values
            # Single element field: container_id
            child_element = SubElement(element, "containerId")
            child_element.text = value
            
        # Process field: container_code (XML name: containerCode)
        value = getattr(self, "container_code", None)
        if value is not None:  # Skip None values
            # Single element field: container_code
            child_element = SubElement(element, "containerCode")
            child_element.text = value
            
        # Process field: price (XML name: price)
        value = getattr(self, "price", None)
        if value is not None:  # Skip None values
            # Single element field: price
            child_element = SubElement(element, "price")
            child_element.text = str(value)
            
        # Process field: price_without_vat (XML name: priceWithoutVat)
        value = getattr(self, "price_without_vat", None)
        if value is not None:  # Skip None values
            # Single element field: price_without_vat
            child_element = SubElement(element, "priceWithoutVat")
            child_element.text = str(value)
            
        # Process field: amount (XML name: amount)
        value = getattr(self, "amount", None)
        if value is not None:  # Skip None values
            # Single element field: amount
            child_element = SubElement(element, "amount")
            child_element.text = str(value)
            
        # Process field: sum (XML name: sum)
        value = getattr(self, "sum", None)
        if value is not None:  # Skip None values
            # Single element field: sum
            child_element = SubElement(element, "sum")
            child_element.text = str(value)
            
        # Process field: discount_sum (XML name: discountSum)
        value = getattr(self, "discount_sum", None)
        if value is not None:  # Skip None values
            # Single element field: discount_sum
            child_element = SubElement(element, "discountSum")
            child_element.text = str(value)
            
        # Process field: vat_percent (XML name: vatPercent)
        value = getattr(self, "vat_percent", None)
        if value is not None:  # Skip None values
            # Single element field: vat_percent
            child_element = SubElement(element, "vatPercent")
            child_element.text = str(value)
            
        # Process field: vat_sum (XML name: vatSum)
        value = getattr(self, "vat_sum", None)
        if value is not None:  # Skip None values
            # Single element field: vat_sum
            child_element = SubElement(element, "vatSum")
            child_element.text = str(value)
            


