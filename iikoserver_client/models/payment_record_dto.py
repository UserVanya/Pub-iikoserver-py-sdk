# coding: utf-8

"""
    iikoServer API

    API Server позволяет сделать интеграции, которые предназначены для работы с номенклатурой, отчетами, документами, данными сотрудников и приказами.   ### Ограничения и рекомендации - Запросы должны выполняться последовательно друг за другом - Запрашивайте данные за период не длиннее одного месяца, в идеале — за один день или неделю - Для OLAPv2-отчетов рекомендуется устанавливать build-summary=false - При построении OLAPv2-отчета рекомендуется использовать не более 7 полей - Перед выполнением запросов проверяйте их на демо сервере

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictFloat, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional, Union
from iikoserver_client.models.payment_record_info_dto import PaymentRecordInfoDto
from typing import Optional, Set, Union
from typing_extensions import Self

# XML support imports
from lxml import etree
from lxml.etree import Element, SubElement

class PaymentRecordDto(BaseModel):
    """
    PaymentRecordDto
    """ # noqa: E501
    info: PaymentRecordInfoDto
    actual_sum: Union[StrictFloat, StrictInt] = Field(description="Сумма из элемента документа закрытия кассовой смены или сумма из проводки", alias="actualSum")
    original_sum: Union[StrictFloat, StrictInt] = Field(description="Сумма проводки", alias="originalSum")
    edited_pay_account_id: Optional[StrictStr] = Field(default=None, description="Редактируемый счет из элемента документа закрытия смены", alias="editedPayAccountId")
    original_pay_account_id: Optional[StrictStr] = Field(default=None, description="Счет, значение которого совпадает с editedPayAccountId", alias="originalPayAccountId")
    pay_agent_id: Optional[StrictStr] = Field(default=None, description="Контрагент из элемента документа закрытия смены или из транзакции", alias="payAgentId")
    payment_type_id: Optional[StrictStr] = Field(default=None, description="Тип оплаты", alias="paymentTypeId")
    editable_comment: Optional[StrictStr] = Field(default=None, description="Комментарий", alias="editableComment")
    status: Optional[StrictStr] = Field(default=None, description="Статус записи")
    __properties: ClassVar[List[str]] = ["info", "actualSum", "originalSum", "editedPayAccountId", "originalPayAccountId", "payAgentId", "paymentTypeId", "editableComment", "status"]
    
    # XML metadata for the class
    __xml_config: ClassVar[Dict[str, Any]] = {
        "root_name": "PaymentRecordDto",
        
        
        "field_configs": {
            "info": {
                "xml_name": "info",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "actual_sum": {
                "xml_name": "actualSum",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "original_sum": {
                "xml_name": "originalSum",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "edited_pay_account_id": {
                "xml_name": "editedPayAccountId",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "original_pay_account_id": {
                "xml_name": "originalPayAccountId",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "pay_agent_id": {
                "xml_name": "payAgentId",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "payment_type_id": {
                "xml_name": "paymentTypeId",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "editable_comment": {
                "xml_name": "editableComment",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "status": {
                "xml_name": "status",
                
                
                "is_attribute": False,
                "is_wrapped": False
            }
        }
    }


    @field_validator('status')
    def status_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['ACCEPTED', 'UNACCEPTED', 'HASWARNINGS']):
            raise ValueError("must be one of enum values ('ACCEPTED', 'UNACCEPTED', 'HASWARNINGS')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of PaymentRecordDto from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of info
        if self.info:
            _dict['info'] = self.info.to_dict()
        # set to None if edited_pay_account_id (nullable) is None
        # and model_fields_set contains the field
        if self.edited_pay_account_id is None and "edited_pay_account_id" in self.model_fields_set:
            _dict['editedPayAccountId'] = None

        # set to None if original_pay_account_id (nullable) is None
        # and model_fields_set contains the field
        if self.original_pay_account_id is None and "original_pay_account_id" in self.model_fields_set:
            _dict['originalPayAccountId'] = None

        # set to None if pay_agent_id (nullable) is None
        # and model_fields_set contains the field
        if self.pay_agent_id is None and "pay_agent_id" in self.model_fields_set:
            _dict['payAgentId'] = None

        # set to None if payment_type_id (nullable) is None
        # and model_fields_set contains the field
        if self.payment_type_id is None and "payment_type_id" in self.model_fields_set:
            _dict['paymentTypeId'] = None

        # set to None if editable_comment (nullable) is None
        # and model_fields_set contains the field
        if self.editable_comment is None and "editable_comment" in self.model_fields_set:
            _dict['editableComment'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of PaymentRecordDto from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "info": PaymentRecordInfoDto.from_dict(obj["info"]) if obj.get("info") is not None else None,
            "actualSum": obj.get("actualSum"),
            "originalSum": obj.get("originalSum"),
            "editedPayAccountId": obj.get("editedPayAccountId"),
            "originalPayAccountId": obj.get("originalPayAccountId"),
            "payAgentId": obj.get("payAgentId"),
            "paymentTypeId": obj.get("paymentTypeId"),
            "editableComment": obj.get("editableComment"),
            "status": obj.get("status")
        })
        return _obj



    @classmethod
    def from_xml(cls, xml_str: str) -> Optional[Self]:
        """Create an instance of PaymentRecordDto from an XML string"""
        try:
            if isinstance(xml_str, str):
                xml_str = xml_str.encode('utf-8')
            root = etree.fromstring(xml_str)
            return cls.from_xml_element(root)
        except Exception as e:
            raise ValueError(f"Failed to parse XML: {e}")

    @classmethod
    def from_xml_element(cls, element: Element) -> Optional[Self]:
        """Create an instance of PaymentRecordDto from an XML element"""
        return cls._from_xml_element_internal(element)

    @classmethod
    def _from_xml_element_internal(cls, element: Element) -> Optional[Self]:
        """Internal method to create instance from XML element"""
        if element is None:
            return None

        data = {}
        
        # Process field: info (XML name: info)
        # Handle XML element: info
        # Single element field: info
        child_element = element.find("info")
        if child_element is not None:
            # Complex object field
            nested_obj = PaymentRecordInfoDto.from_xml_element(child_element)
            if nested_obj is not None:
                data["info"] = nested_obj
        
        # Process field: actual_sum (XML name: actualSum)
        # Handle XML element: actual_sum
        # Single element field: actual_sum
        child_element = element.find("actualSum")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["actualSum"] = float(text_value)
                except ValueError:
                    data["actualSum"] = text_value
                
        
        # Process field: original_sum (XML name: originalSum)
        # Handle XML element: original_sum
        # Single element field: original_sum
        child_element = element.find("originalSum")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["originalSum"] = float(text_value)
                except ValueError:
                    data["originalSum"] = text_value
                
        
        # Process field: edited_pay_account_id (XML name: editedPayAccountId)
        # Handle XML element: edited_pay_account_id
        # Single element field: edited_pay_account_id
        child_element = element.find("editedPayAccountId")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["editedPayAccountId"] = text_value
                
        
        # Process field: original_pay_account_id (XML name: originalPayAccountId)
        # Handle XML element: original_pay_account_id
        # Single element field: original_pay_account_id
        child_element = element.find("originalPayAccountId")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["originalPayAccountId"] = text_value
                
        
        # Process field: pay_agent_id (XML name: payAgentId)
        # Handle XML element: pay_agent_id
        # Single element field: pay_agent_id
        child_element = element.find("payAgentId")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["payAgentId"] = text_value
                
        
        # Process field: payment_type_id (XML name: paymentTypeId)
        # Handle XML element: payment_type_id
        # Single element field: payment_type_id
        child_element = element.find("paymentTypeId")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["paymentTypeId"] = text_value
                
        
        # Process field: editable_comment (XML name: editableComment)
        # Handle XML element: editable_comment
        # Single element field: editable_comment
        child_element = element.find("editableComment")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["editableComment"] = text_value
                
        
        # Process field: status (XML name: status)
        # Handle XML element: status
        # Single element field: status
        child_element = element.find("status")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["status"] = text_value
                
        
        
        return cls.from_dict(data)

    def to_xml(self) -> str:
        """Create an XML string representation of the model"""
        root_element = Element(self.__xml_config.get("root_name", self.__class__.__name__))
        
        # Populate the XML element with model data
        self._to_xml_element_internal(root_element)
        
        # Convert to string with XML declaration
        return etree.tostring(root_element, encoding="utf-8", pretty_print=True, xml_declaration=True).decode("utf-8")

    def _to_xml_element_internal(self, element: Element) -> None:
        """Internal method to populate an XML element with model data"""
        
        # Process field: info (XML name: info)
        value = getattr(self, "info", None)
        if value is not None:  # Skip None values
            # Single element field: info
            child_element = SubElement(element, "info")
            # Complex object field
            if hasattr(value, "_to_xml_element_internal"):
                value._to_xml_element_internal(child_element)
        # Process field: actual_sum (XML name: actualSum)
        value = getattr(self, "actual_sum", None)
        if value is not None:  # Skip None values
            # Single element field: actual_sum
            child_element = SubElement(element, "actualSum")
            child_element.text = str(value)
            
        # Process field: original_sum (XML name: originalSum)
        value = getattr(self, "original_sum", None)
        if value is not None:  # Skip None values
            # Single element field: original_sum
            child_element = SubElement(element, "originalSum")
            child_element.text = str(value)
            
        # Process field: edited_pay_account_id (XML name: editedPayAccountId)
        value = getattr(self, "edited_pay_account_id", None)
        if value is not None:  # Skip None values
            # Single element field: edited_pay_account_id
            child_element = SubElement(element, "editedPayAccountId")
            child_element.text = value
            
        # Process field: original_pay_account_id (XML name: originalPayAccountId)
        value = getattr(self, "original_pay_account_id", None)
        if value is not None:  # Skip None values
            # Single element field: original_pay_account_id
            child_element = SubElement(element, "originalPayAccountId")
            child_element.text = value
            
        # Process field: pay_agent_id (XML name: payAgentId)
        value = getattr(self, "pay_agent_id", None)
        if value is not None:  # Skip None values
            # Single element field: pay_agent_id
            child_element = SubElement(element, "payAgentId")
            child_element.text = value
            
        # Process field: payment_type_id (XML name: paymentTypeId)
        value = getattr(self, "payment_type_id", None)
        if value is not None:  # Skip None values
            # Single element field: payment_type_id
            child_element = SubElement(element, "paymentTypeId")
            child_element.text = value
            
        # Process field: editable_comment (XML name: editableComment)
        value = getattr(self, "editable_comment", None)
        if value is not None:  # Skip None values
            # Single element field: editable_comment
            child_element = SubElement(element, "editableComment")
            child_element.text = value
            
        # Process field: status (XML name: status)
        value = getattr(self, "status", None)
        if value is not None:  # Skip None values
            # Single element field: status
            child_element = SubElement(element, "status")
            child_element.text = value
            


