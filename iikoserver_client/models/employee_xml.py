# coding: utf-8

"""
    iikoServer API

    API Server позволяет сделать интеграции, которые предназначены для работы с номенклатурой, отчетами, документами, данными сотрудников и приказами.   ### Ограничения и рекомендации - Запросы должны выполняться последовательно друг за другом - Запрашивайте данные за период не длиннее одного месяца, в идеале — за один день или неделю - Для OLAPv2-отчетов рекомендуется устанавливать build-summary=false - При построении OLAPv2-отчета рекомендуется использовать не более 7 полей - Перед выполнением запросов проверяйте их на демо сервере

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import date, datetime
from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from iikoserver_client.models.public_external_data_xml import PublicExternalDataXml
from typing import Optional, Set, Union
from typing_extensions import Self

# XML support imports
from lxml import etree
from lxml.etree import Element, SubElement

class EmployeeXml(BaseModel):
    """
    EmployeeXml
    """ # noqa: E501
    id: Optional[StrictStr] = Field(default=None, description="UUID сотрудника")
    code: Optional[StrictStr] = Field(description="Табельный номер сотрудника. Пуст у системных учетных записей.")
    name: StrictStr = Field(description="Имя сотрудника")
    login: Optional[StrictStr] = Field(default=None, description="Логин для входа в бекофис")
    password: Optional[StrictStr] = Field(default=None, description="Пароль для входа в бекофис. Доступен только на изменение, не отображается.")
    main_role_code: Optional[StrictStr] = Field(default=None, description="Основная должность сотрудника. Входит в roleCodes (кроме системных учетных записей, у них не заполнено roleCodes).", alias="mainRoleCode")
    role_codes: Optional[List[Optional[StrictStr]]] = Field(default=None, description="Список кодов ролей", alias="roleCodes")
    phone: Optional[StrictStr] = Field(default=None, description="Телефон")
    cell_phone: Optional[StrictStr] = Field(default=None, description="Мобильный телефон", alias="cellPhone")
    first_name: Optional[StrictStr] = Field(default=None, description="Имя", alias="firstName")
    middle_name: Optional[StrictStr] = Field(default=None, description="Отчество", alias="middleName")
    last_name: Optional[StrictStr] = Field(default=None, description="Фамилия", alias="lastName")
    birthday: Optional[datetime] = Field(default=None, description="Дата рождения")
    email: Optional[StrictStr] = Field(default=None, description="Email")
    address: Optional[StrictStr] = Field(default=None, description="Адрес")
    hire_date: Optional[StrictStr] = Field(default=None, description="Дата найма", alias="hireDate")
    hire_document_number: Optional[StrictStr] = Field(default=None, description="Номер документа найма", alias="hireDocumentNumber")
    fire_date: Optional[date] = Field(default=None, description="Дата увольнения", alias="fireDate")
    note: Optional[StrictStr] = Field(default=None, description="Примечание")
    card_number: Optional[StrictStr] = Field(default=None, description="Slip карты сотрудника", alias="cardNumber")
    pin_code: Optional[StrictStr] = Field(default=None, description="Pin-код для входа в iikoFront. Доступен только на изменение, не отображается.", alias="pinCode")
    taxpayer_id_number: Optional[StrictStr] = Field(default=None, description="ИНН", alias="taxpayerIdNumber")
    snils: Optional[StrictStr] = Field(default=None, description="СНИЛС")
    gln: Optional[StrictStr] = Field(default=None, description="Global Location Number для поставщиков")
    activation_date: Optional[datetime] = Field(default=None, description="Дата активации", alias="activationDate")
    deactivation_date: Optional[datetime] = Field(default=None, description="Дата деактивации", alias="deactivationDate")
    preferred_department_code: Optional[StrictStr] = Field(default=None, description="Подразделение (одно из departmentCodes), в котором сотруднику назначаются смены в первую очередь.", alias="preferredDepartmentCode")
    department_codes: Optional[List[Optional[StrictStr]]] = Field(default=None, description="Назначенные подразделения. Если null - сотруднику назначены все подразделения (существующие и будущие).", alias="departmentCodes")
    responsibility_department_codes: Optional[List[Optional[StrictStr]]] = Field(default=None, description="Подразделения, в которых сотрудник является ответственным. Если null - сотруднику является ответственным во всех существующих и будущих подразделениях.", alias="responsibilityDepartmentCodes")
    deleted: Optional[StrictBool] = Field(default=None, description="Признак удаления")
    supplier: Optional[StrictBool] = Field(default=None, description="Признак поставщика")
    employee: Optional[StrictBool] = Field(default=None, description="Признак сотрудника")
    client: Optional[StrictBool] = Field(default=None, description="Признак клиента")
    personal_data_consent: Optional[StrictBool] = Field(default=None, description="Согласие на обработку персональных данных", alias="personalDataConsent")
    represents_store: Optional[StrictBool] = Field(default=None, description="Признак представляющего магазин", alias="representsStore")
    public_external_data: Optional[PublicExternalDataXml] = Field(default=None, alias="publicExternalData")
    main_role_id: Optional[StrictStr] = Field(default=None, description="ID основной должности сотрудника. Входит в rolesIds.", alias="mainRoleId")
    roles_ids: Optional[List[Optional[StrictStr]]] = Field(default=None, description="Список ID должностей сотрудника", alias="rolesIds")
    __properties: ClassVar[List[str]] = ["id", "code", "name", "login", "password", "mainRoleCode", "roleCodes", "phone", "cellPhone", "firstName", "middleName", "lastName", "birthday", "email", "address", "hireDate", "hireDocumentNumber", "fireDate", "note", "cardNumber", "pinCode", "taxpayerIdNumber", "snils", "gln", "activationDate", "deactivationDate", "preferredDepartmentCode", "departmentCodes", "responsibilityDepartmentCodes", "deleted", "supplier", "employee", "client", "personalDataConsent", "representsStore", "publicExternalData", "mainRoleId", "rolesIds"]
    
    # XML metadata for the class
    __xml_config: ClassVar[Dict[str, Any]] = {
        "root_name": "employee",
        
        
        "field_configs": {
            "id": {
                "xml_name": "id",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "code": {
                "xml_name": "code",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "name": {
                "xml_name": "name",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "login": {
                "xml_name": "login",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "password": {
                "xml_name": "password",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "main_role_code": {
                "xml_name": "mainRoleCode",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "role_codes": {
                "xml_name": "roleCodes",
                
                
                "is_attribute": False,
                "is_wrapped": True
            },
            "phone": {
                "xml_name": "phone",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "cell_phone": {
                "xml_name": "cellPhone",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "first_name": {
                "xml_name": "firstName",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "middle_name": {
                "xml_name": "middleName",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "last_name": {
                "xml_name": "lastName",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "birthday": {
                "xml_name": "birthday",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "email": {
                "xml_name": "email",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "address": {
                "xml_name": "address",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "hire_date": {
                "xml_name": "hireDate",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "hire_document_number": {
                "xml_name": "hireDocumentNumber",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "fire_date": {
                "xml_name": "fireDate",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "note": {
                "xml_name": "note",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "card_number": {
                "xml_name": "cardNumber",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "pin_code": {
                "xml_name": "pinCode",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "taxpayer_id_number": {
                "xml_name": "taxpayerIdNumber",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "snils": {
                "xml_name": "snils",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "gln": {
                "xml_name": "gln",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "activation_date": {
                "xml_name": "activationDate",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "deactivation_date": {
                "xml_name": "deactivationDate",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "preferred_department_code": {
                "xml_name": "preferredDepartmentCode",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "department_codes": {
                "xml_name": "departmentCodes",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "responsibility_department_codes": {
                "xml_name": "responsibilityDepartmentCodes",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "deleted": {
                "xml_name": "deleted",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "supplier": {
                "xml_name": "supplier",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "employee": {
                "xml_name": "employee",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "client": {
                "xml_name": "client",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "personal_data_consent": {
                "xml_name": "personalDataConsent",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "represents_store": {
                "xml_name": "representsStore",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "public_external_data": {
                "xml_name": "publicExternalData",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "main_role_id": {
                "xml_name": "mainRoleId",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "roles_ids": {
                "xml_name": "rolesIds",
                
                
                "is_attribute": False,
                "is_wrapped": True
            }
        }
    }


    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of EmployeeXml from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of public_external_data
        if self.public_external_data:
            _dict['publicExternalData'] = self.public_external_data.to_dict()
        # set to None if code (nullable) is None
        # and model_fields_set contains the field
        if self.code is None and "code" in self.model_fields_set:
            _dict['code'] = None

        # set to None if login (nullable) is None
        # and model_fields_set contains the field
        if self.login is None and "login" in self.model_fields_set:
            _dict['login'] = None

        # set to None if password (nullable) is None
        # and model_fields_set contains the field
        if self.password is None and "password" in self.model_fields_set:
            _dict['password'] = None

        # set to None if main_role_code (nullable) is None
        # and model_fields_set contains the field
        if self.main_role_code is None and "main_role_code" in self.model_fields_set:
            _dict['mainRoleCode'] = None

        # set to None if phone (nullable) is None
        # and model_fields_set contains the field
        if self.phone is None and "phone" in self.model_fields_set:
            _dict['phone'] = None

        # set to None if cell_phone (nullable) is None
        # and model_fields_set contains the field
        if self.cell_phone is None and "cell_phone" in self.model_fields_set:
            _dict['cellPhone'] = None

        # set to None if first_name (nullable) is None
        # and model_fields_set contains the field
        if self.first_name is None and "first_name" in self.model_fields_set:
            _dict['firstName'] = None

        # set to None if middle_name (nullable) is None
        # and model_fields_set contains the field
        if self.middle_name is None and "middle_name" in self.model_fields_set:
            _dict['middleName'] = None

        # set to None if last_name (nullable) is None
        # and model_fields_set contains the field
        if self.last_name is None and "last_name" in self.model_fields_set:
            _dict['lastName'] = None

        # set to None if birthday (nullable) is None
        # and model_fields_set contains the field
        if self.birthday is None and "birthday" in self.model_fields_set:
            _dict['birthday'] = None

        # set to None if email (nullable) is None
        # and model_fields_set contains the field
        if self.email is None and "email" in self.model_fields_set:
            _dict['email'] = None

        # set to None if address (nullable) is None
        # and model_fields_set contains the field
        if self.address is None and "address" in self.model_fields_set:
            _dict['address'] = None

        # set to None if hire_date (nullable) is None
        # and model_fields_set contains the field
        if self.hire_date is None and "hire_date" in self.model_fields_set:
            _dict['hireDate'] = None

        # set to None if hire_document_number (nullable) is None
        # and model_fields_set contains the field
        if self.hire_document_number is None and "hire_document_number" in self.model_fields_set:
            _dict['hireDocumentNumber'] = None

        # set to None if fire_date (nullable) is None
        # and model_fields_set contains the field
        if self.fire_date is None and "fire_date" in self.model_fields_set:
            _dict['fireDate'] = None

        # set to None if note (nullable) is None
        # and model_fields_set contains the field
        if self.note is None and "note" in self.model_fields_set:
            _dict['note'] = None

        # set to None if card_number (nullable) is None
        # and model_fields_set contains the field
        if self.card_number is None and "card_number" in self.model_fields_set:
            _dict['cardNumber'] = None

        # set to None if pin_code (nullable) is None
        # and model_fields_set contains the field
        if self.pin_code is None and "pin_code" in self.model_fields_set:
            _dict['pinCode'] = None

        # set to None if taxpayer_id_number (nullable) is None
        # and model_fields_set contains the field
        if self.taxpayer_id_number is None and "taxpayer_id_number" in self.model_fields_set:
            _dict['taxpayerIdNumber'] = None

        # set to None if snils (nullable) is None
        # and model_fields_set contains the field
        if self.snils is None and "snils" in self.model_fields_set:
            _dict['snils'] = None

        # set to None if gln (nullable) is None
        # and model_fields_set contains the field
        if self.gln is None and "gln" in self.model_fields_set:
            _dict['gln'] = None

        # set to None if activation_date (nullable) is None
        # and model_fields_set contains the field
        if self.activation_date is None and "activation_date" in self.model_fields_set:
            _dict['activationDate'] = None

        # set to None if deactivation_date (nullable) is None
        # and model_fields_set contains the field
        if self.deactivation_date is None and "deactivation_date" in self.model_fields_set:
            _dict['deactivationDate'] = None

        # set to None if preferred_department_code (nullable) is None
        # and model_fields_set contains the field
        if self.preferred_department_code is None and "preferred_department_code" in self.model_fields_set:
            _dict['preferredDepartmentCode'] = None

        # set to None if main_role_id (nullable) is None
        # and model_fields_set contains the field
        if self.main_role_id is None and "main_role_id" in self.model_fields_set:
            _dict['mainRoleId'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of EmployeeXml from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "id": obj.get("id"),
            "code": obj.get("code"),
            "name": obj.get("name"),
            "login": obj.get("login"),
            "password": obj.get("password"),
            "mainRoleCode": obj.get("mainRoleCode"),
            "roleCodes": obj.get("roleCodes"),
            "phone": obj.get("phone"),
            "cellPhone": obj.get("cellPhone"),
            "firstName": obj.get("firstName"),
            "middleName": obj.get("middleName"),
            "lastName": obj.get("lastName"),
            "birthday": obj.get("birthday"),
            "email": obj.get("email"),
            "address": obj.get("address"),
            "hireDate": obj.get("hireDate"),
            "hireDocumentNumber": obj.get("hireDocumentNumber"),
            "fireDate": obj.get("fireDate"),
            "note": obj.get("note"),
            "cardNumber": obj.get("cardNumber"),
            "pinCode": obj.get("pinCode"),
            "taxpayerIdNumber": obj.get("taxpayerIdNumber"),
            "snils": obj.get("snils"),
            "gln": obj.get("gln"),
            "activationDate": obj.get("activationDate"),
            "deactivationDate": obj.get("deactivationDate"),
            "preferredDepartmentCode": obj.get("preferredDepartmentCode"),
            "departmentCodes": obj.get("departmentCodes"),
            "responsibilityDepartmentCodes": obj.get("responsibilityDepartmentCodes"),
            "deleted": obj.get("deleted"),
            "supplier": obj.get("supplier"),
            "employee": obj.get("employee"),
            "client": obj.get("client"),
            "personalDataConsent": obj.get("personalDataConsent"),
            "representsStore": obj.get("representsStore"),
            "publicExternalData": PublicExternalDataXml.from_dict(obj["publicExternalData"]) if obj.get("publicExternalData") is not None else None,
            "mainRoleId": obj.get("mainRoleId"),
            "rolesIds": obj.get("rolesIds")
        })
        return _obj



    @classmethod
    def from_xml(cls, xml_str: str) -> Optional[Self]:
        """Create an instance of EmployeeXml from an XML string"""
        try:
            if isinstance(xml_str, str):
                xml_str = xml_str.encode('utf-8')
            root = etree.fromstring(xml_str)
            return cls.from_xml_element(root)
        except Exception as e:
            raise ValueError(f"Failed to parse XML: {e}")

    @classmethod
    def from_xml_element(cls, element: Element) -> Optional[Self]:
        """Create an instance of EmployeeXml from an XML element"""
        return cls._from_xml_element_internal(element)

    @classmethod
    def _from_xml_element_internal(cls, element: Element) -> Optional[Self]:
        """Internal method to create instance from XML element"""
        if element is None:
            return None

        data = {}
        
        # Process field: id (XML name: id)
        # Handle XML element: id
        # Single element field: id
        child_element = element.find("id")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["id"] = text_value
                
        
        # Process field: code (XML name: code)
        # Handle XML element: code
        # Single element field: code
        child_element = element.find("code")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["code"] = text_value
                
        
        # Process field: name (XML name: name)
        # Handle XML element: name
        # Single element field: name
        child_element = element.find("name")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["name"] = text_value
                
        
        # Process field: login (XML name: login)
        # Handle XML element: login
        # Single element field: login
        child_element = element.find("login")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["login"] = text_value
                
        
        # Process field: password (XML name: password)
        # Handle XML element: password
        # Single element field: password
        child_element = element.find("password")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["password"] = text_value
                
        
        # Process field: main_role_code (XML name: mainRoleCode)
        # Handle XML element: main_role_code
        # Single element field: main_role_code
        child_element = element.find("mainRoleCode")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["mainRoleCode"] = text_value
                
        
        # Process field: role_codes (XML name: roleCodes)
        # Handle XML element: role_codes
        # Array field: role_codes
        # Wrapped array - current element is the wrapper, look for items directly
        items = []
        # Primitive items in wrapped array
        child_elements = element.findall("roleCodes")
        for child in child_elements:
            text_value = child.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                items.append(text_value)
                
        data["roleCodes"] = items
        
        # Process field: phone (XML name: phone)
        # Handle XML element: phone
        # Single element field: phone
        child_element = element.find("phone")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["phone"] = text_value
                
        
        # Process field: cell_phone (XML name: cellPhone)
        # Handle XML element: cell_phone
        # Single element field: cell_phone
        child_element = element.find("cellPhone")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["cellPhone"] = text_value
                
        
        # Process field: first_name (XML name: firstName)
        # Handle XML element: first_name
        # Single element field: first_name
        child_element = element.find("firstName")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["firstName"] = text_value
                
        
        # Process field: middle_name (XML name: middleName)
        # Handle XML element: middle_name
        # Single element field: middle_name
        child_element = element.find("middleName")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["middleName"] = text_value
                
        
        # Process field: last_name (XML name: lastName)
        # Handle XML element: last_name
        # Single element field: last_name
        child_element = element.find("lastName")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["lastName"] = text_value
                
        
        # Process field: birthday (XML name: birthday)
        # Handle XML element: birthday
        # Single element field: birthday
        child_element = element.find("birthday")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                
                # Handle other primitive types (date, datetime, uuid, etc.)
                # DateTime field - handle "null" specially  
                if text_value == "null":
                    data["birthday"] = None
                else:
                    data["birthday"] = text_value
                
                
        
        # Process field: email (XML name: email)
        # Handle XML element: email
        # Single element field: email
        child_element = element.find("email")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["email"] = text_value
                
        
        # Process field: address (XML name: address)
        # Handle XML element: address
        # Single element field: address
        child_element = element.find("address")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["address"] = text_value
                
        
        # Process field: hire_date (XML name: hireDate)
        # Handle XML element: hire_date
        # Single element field: hire_date
        child_element = element.find("hireDate")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["hireDate"] = text_value
                
        
        # Process field: hire_document_number (XML name: hireDocumentNumber)
        # Handle XML element: hire_document_number
        # Single element field: hire_document_number
        child_element = element.find("hireDocumentNumber")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["hireDocumentNumber"] = text_value
                
        
        # Process field: fire_date (XML name: fireDate)
        # Handle XML element: fire_date
        # Single element field: fire_date
        child_element = element.find("fireDate")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                
                # Handle other primitive types (date, datetime, uuid, etc.)
                # Date field - handle "null" specially
                if text_value == "null":
                    data["fireDate"] = None
                else:
                    data["fireDate"] = text_value
                
                
        
        # Process field: note (XML name: note)
        # Handle XML element: note
        # Single element field: note
        child_element = element.find("note")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["note"] = text_value
                
        
        # Process field: card_number (XML name: cardNumber)
        # Handle XML element: card_number
        # Single element field: card_number
        child_element = element.find("cardNumber")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["cardNumber"] = text_value
                
        
        # Process field: pin_code (XML name: pinCode)
        # Handle XML element: pin_code
        # Single element field: pin_code
        child_element = element.find("pinCode")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["pinCode"] = text_value
                
        
        # Process field: taxpayer_id_number (XML name: taxpayerIdNumber)
        # Handle XML element: taxpayer_id_number
        # Single element field: taxpayer_id_number
        child_element = element.find("taxpayerIdNumber")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["taxpayerIdNumber"] = text_value
                
        
        # Process field: snils (XML name: snils)
        # Handle XML element: snils
        # Single element field: snils
        child_element = element.find("snils")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["snils"] = text_value
                
        
        # Process field: gln (XML name: gln)
        # Handle XML element: gln
        # Single element field: gln
        child_element = element.find("gln")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["gln"] = text_value
                
        
        # Process field: activation_date (XML name: activationDate)
        # Handle XML element: activation_date
        # Single element field: activation_date
        child_element = element.find("activationDate")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                
                # Handle other primitive types (date, datetime, uuid, etc.)
                # DateTime field - handle "null" specially  
                if text_value == "null":
                    data["activationDate"] = None
                else:
                    data["activationDate"] = text_value
                
                
        
        # Process field: deactivation_date (XML name: deactivationDate)
        # Handle XML element: deactivation_date
        # Single element field: deactivation_date
        child_element = element.find("deactivationDate")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                
                # Handle other primitive types (date, datetime, uuid, etc.)
                # DateTime field - handle "null" specially  
                if text_value == "null":
                    data["deactivationDate"] = None
                else:
                    data["deactivationDate"] = text_value
                
                
        
        # Process field: preferred_department_code (XML name: preferredDepartmentCode)
        # Handle XML element: preferred_department_code
        # Single element field: preferred_department_code
        child_element = element.find("preferredDepartmentCode")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["preferredDepartmentCode"] = text_value
                
        
        # Process field: department_codes (XML name: departmentCodes)
        # Handle XML element: department_codes
        # Array field: department_codes
        # Non-wrapped array - direct elements
        # Primitive items in non-wrapped array
        child_elements = element.findall("departmentCodes")
        items = []
        for child in child_elements:
            text_value = child.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                items.append(text_value)
                
        data["departmentCodes"] = items
        
        # Process field: responsibility_department_codes (XML name: responsibilityDepartmentCodes)
        # Handle XML element: responsibility_department_codes
        # Array field: responsibility_department_codes
        # Non-wrapped array - direct elements
        # Primitive items in non-wrapped array
        child_elements = element.findall("responsibilityDepartmentCodes")
        items = []
        for child in child_elements:
            text_value = child.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                items.append(text_value)
                
        data["responsibilityDepartmentCodes"] = items
        
        # Process field: deleted (XML name: deleted)
        # Handle XML element: deleted
        # Single element field: deleted
        child_element = element.find("deleted")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["deleted"] = text_value.lower() == 'true'
                
        
        # Process field: supplier (XML name: supplier)
        # Handle XML element: supplier
        # Single element field: supplier
        child_element = element.find("supplier")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["supplier"] = text_value.lower() == 'true'
                
        
        # Process field: employee (XML name: employee)
        # Handle XML element: employee
        # Single element field: employee
        child_element = element.find("employee")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["employee"] = text_value.lower() == 'true'
                
        
        # Process field: client (XML name: client)
        # Handle XML element: client
        # Single element field: client
        child_element = element.find("client")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["client"] = text_value.lower() == 'true'
                
        
        # Process field: personal_data_consent (XML name: personalDataConsent)
        # Handle XML element: personal_data_consent
        # Single element field: personal_data_consent
        child_element = element.find("personalDataConsent")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["personalDataConsent"] = text_value.lower() == 'true'
                
        
        # Process field: represents_store (XML name: representsStore)
        # Handle XML element: represents_store
        # Single element field: represents_store
        child_element = element.find("representsStore")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["representsStore"] = text_value.lower() == 'true'
                
        
        # Process field: public_external_data (XML name: publicExternalData)
        # Handle XML element: public_external_data
        # Single element field: public_external_data
        child_element = element.find("publicExternalData")
        if child_element is not None:
            # Complex object field
            nested_obj = PublicExternalDataXml.from_xml_element(child_element)
            if nested_obj is not None:
                data["publicExternalData"] = nested_obj
        
        # Process field: main_role_id (XML name: mainRoleId)
        # Handle XML element: main_role_id
        # Single element field: main_role_id
        child_element = element.find("mainRoleId")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["mainRoleId"] = text_value
                
        
        # Process field: roles_ids (XML name: rolesIds)
        # Handle XML element: roles_ids
        # Array field: roles_ids
        # Wrapped array - current element is the wrapper, look for items directly
        items = []
        # Primitive items in wrapped array
        child_elements = element.findall("rolesIds")
        for child in child_elements:
            text_value = child.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                items.append(text_value)
                
        data["rolesIds"] = items
        
        
        return cls.from_dict(data)

    def to_xml(self) -> str:
        """Create an XML string representation of the model"""
        root_element = Element(self.__xml_config.get("root_name", self.__class__.__name__))
        
        # Populate the XML element with model data
        self._to_xml_element_internal(root_element)
        
        # Convert to string with XML declaration
        return etree.tostring(root_element, encoding="utf-8", pretty_print=True, xml_declaration=True).decode("utf-8")

    def _to_xml_element_internal(self, element: Element) -> None:
        """Internal method to populate an XML element with model data"""
        
        # Process field: id (XML name: id)
        value = getattr(self, "id", None)
        if value is not None:  # Skip None values
            # Single element field: id
            child_element = SubElement(element, "id")
            child_element.text = value
            
        # Process field: code (XML name: code)
        value = getattr(self, "code", None)
        if value is not None:  # Skip None values
            # Single element field: code
            child_element = SubElement(element, "code")
            child_element.text = value
            
        # Process field: name (XML name: name)
        value = getattr(self, "name", None)
        if value is not None:  # Skip None values
            # Single element field: name
            child_element = SubElement(element, "name")
            child_element.text = value
            
        # Process field: login (XML name: login)
        value = getattr(self, "login", None)
        if value is not None:  # Skip None values
            # Single element field: login
            child_element = SubElement(element, "login")
            child_element.text = value
            
        # Process field: password (XML name: password)
        value = getattr(self, "password", None)
        if value is not None:  # Skip None values
            # Single element field: password
            child_element = SubElement(element, "password")
            child_element.text = value
            
        # Process field: main_role_code (XML name: mainRoleCode)
        value = getattr(self, "main_role_code", None)
        if value is not None:  # Skip None values
            # Single element field: main_role_code
            child_element = SubElement(element, "mainRoleCode")
            child_element.text = value
            
        # Process field: role_codes (XML name: roleCodes)
        value = getattr(self, "role_codes", None)
        if value is not None:  # Skip None values
            # Array field: role_codes
            # Wrapped array - elements added directly to parent (wrapper already exists)
            for item in value:
                # Primitive items in wrapped array
                item_element = SubElement(element, "roleCodes")
                item_element.text = item
                
        # Process field: phone (XML name: phone)
        value = getattr(self, "phone", None)
        if value is not None:  # Skip None values
            # Single element field: phone
            child_element = SubElement(element, "phone")
            child_element.text = value
            
        # Process field: cell_phone (XML name: cellPhone)
        value = getattr(self, "cell_phone", None)
        if value is not None:  # Skip None values
            # Single element field: cell_phone
            child_element = SubElement(element, "cellPhone")
            child_element.text = value
            
        # Process field: first_name (XML name: firstName)
        value = getattr(self, "first_name", None)
        if value is not None:  # Skip None values
            # Single element field: first_name
            child_element = SubElement(element, "firstName")
            child_element.text = value
            
        # Process field: middle_name (XML name: middleName)
        value = getattr(self, "middle_name", None)
        if value is not None:  # Skip None values
            # Single element field: middle_name
            child_element = SubElement(element, "middleName")
            child_element.text = value
            
        # Process field: last_name (XML name: lastName)
        value = getattr(self, "last_name", None)
        if value is not None:  # Skip None values
            # Single element field: last_name
            child_element = SubElement(element, "lastName")
            child_element.text = value
            
        # Process field: birthday (XML name: birthday)
        value = getattr(self, "birthday", None)
        if value is not None:  # Skip None values
            # Single element field: birthday
            child_element = SubElement(element, "birthday")
            
            # Handle other primitive types (date, datetime, uuid, etc.)
            child_element.text = str(value)
            
        # Process field: email (XML name: email)
        value = getattr(self, "email", None)
        if value is not None:  # Skip None values
            # Single element field: email
            child_element = SubElement(element, "email")
            child_element.text = value
            
        # Process field: address (XML name: address)
        value = getattr(self, "address", None)
        if value is not None:  # Skip None values
            # Single element field: address
            child_element = SubElement(element, "address")
            child_element.text = value
            
        # Process field: hire_date (XML name: hireDate)
        value = getattr(self, "hire_date", None)
        if value is not None:  # Skip None values
            # Single element field: hire_date
            child_element = SubElement(element, "hireDate")
            child_element.text = value
            
        # Process field: hire_document_number (XML name: hireDocumentNumber)
        value = getattr(self, "hire_document_number", None)
        if value is not None:  # Skip None values
            # Single element field: hire_document_number
            child_element = SubElement(element, "hireDocumentNumber")
            child_element.text = value
            
        # Process field: fire_date (XML name: fireDate)
        value = getattr(self, "fire_date", None)
        if value is not None:  # Skip None values
            # Single element field: fire_date
            child_element = SubElement(element, "fireDate")
            
            # Handle other primitive types (date, datetime, uuid, etc.)
            child_element.text = str(value)
            
        # Process field: note (XML name: note)
        value = getattr(self, "note", None)
        if value is not None:  # Skip None values
            # Single element field: note
            child_element = SubElement(element, "note")
            child_element.text = value
            
        # Process field: card_number (XML name: cardNumber)
        value = getattr(self, "card_number", None)
        if value is not None:  # Skip None values
            # Single element field: card_number
            child_element = SubElement(element, "cardNumber")
            child_element.text = value
            
        # Process field: pin_code (XML name: pinCode)
        value = getattr(self, "pin_code", None)
        if value is not None:  # Skip None values
            # Single element field: pin_code
            child_element = SubElement(element, "pinCode")
            child_element.text = value
            
        # Process field: taxpayer_id_number (XML name: taxpayerIdNumber)
        value = getattr(self, "taxpayer_id_number", None)
        if value is not None:  # Skip None values
            # Single element field: taxpayer_id_number
            child_element = SubElement(element, "taxpayerIdNumber")
            child_element.text = value
            
        # Process field: snils (XML name: snils)
        value = getattr(self, "snils", None)
        if value is not None:  # Skip None values
            # Single element field: snils
            child_element = SubElement(element, "snils")
            child_element.text = value
            
        # Process field: gln (XML name: gln)
        value = getattr(self, "gln", None)
        if value is not None:  # Skip None values
            # Single element field: gln
            child_element = SubElement(element, "gln")
            child_element.text = value
            
        # Process field: activation_date (XML name: activationDate)
        value = getattr(self, "activation_date", None)
        if value is not None:  # Skip None values
            # Single element field: activation_date
            child_element = SubElement(element, "activationDate")
            
            # Handle other primitive types (date, datetime, uuid, etc.)
            child_element.text = str(value)
            
        # Process field: deactivation_date (XML name: deactivationDate)
        value = getattr(self, "deactivation_date", None)
        if value is not None:  # Skip None values
            # Single element field: deactivation_date
            child_element = SubElement(element, "deactivationDate")
            
            # Handle other primitive types (date, datetime, uuid, etc.)
            child_element.text = str(value)
            
        # Process field: preferred_department_code (XML name: preferredDepartmentCode)
        value = getattr(self, "preferred_department_code", None)
        if value is not None:  # Skip None values
            # Single element field: preferred_department_code
            child_element = SubElement(element, "preferredDepartmentCode")
            child_element.text = value
            
        # Process field: department_codes (XML name: departmentCodes)
        value = getattr(self, "department_codes", None)
        if value is not None:  # Skip None values
            # Array field: department_codes
            # Non-wrapped array - add elements directly
            for item in value:
                # Primitive items in non-wrapped array
                item_element = SubElement(element, "departmentCodes")
                item_element.text = item
                
        # Process field: responsibility_department_codes (XML name: responsibilityDepartmentCodes)
        value = getattr(self, "responsibility_department_codes", None)
        if value is not None:  # Skip None values
            # Array field: responsibility_department_codes
            # Non-wrapped array - add elements directly
            for item in value:
                # Primitive items in non-wrapped array
                item_element = SubElement(element, "responsibilityDepartmentCodes")
                item_element.text = item
                
        # Process field: deleted (XML name: deleted)
        value = getattr(self, "deleted", None)
        if value is not None:  # Skip None values
            # Single element field: deleted
            child_element = SubElement(element, "deleted")
            child_element.text = str(value).lower()
            
        # Process field: supplier (XML name: supplier)
        value = getattr(self, "supplier", None)
        if value is not None:  # Skip None values
            # Single element field: supplier
            child_element = SubElement(element, "supplier")
            child_element.text = str(value).lower()
            
        # Process field: employee (XML name: employee)
        value = getattr(self, "employee", None)
        if value is not None:  # Skip None values
            # Single element field: employee
            child_element = SubElement(element, "employee")
            child_element.text = str(value).lower()
            
        # Process field: client (XML name: client)
        value = getattr(self, "client", None)
        if value is not None:  # Skip None values
            # Single element field: client
            child_element = SubElement(element, "client")
            child_element.text = str(value).lower()
            
        # Process field: personal_data_consent (XML name: personalDataConsent)
        value = getattr(self, "personal_data_consent", None)
        if value is not None:  # Skip None values
            # Single element field: personal_data_consent
            child_element = SubElement(element, "personalDataConsent")
            child_element.text = str(value).lower()
            
        # Process field: represents_store (XML name: representsStore)
        value = getattr(self, "represents_store", None)
        if value is not None:  # Skip None values
            # Single element field: represents_store
            child_element = SubElement(element, "representsStore")
            child_element.text = str(value).lower()
            
        # Process field: public_external_data (XML name: publicExternalData)
        value = getattr(self, "public_external_data", None)
        if value is not None:  # Skip None values
            # Single element field: public_external_data
            child_element = SubElement(element, "publicExternalData")
            # Complex object field
            if hasattr(value, "_to_xml_element_internal"):
                value._to_xml_element_internal(child_element)
        # Process field: main_role_id (XML name: mainRoleId)
        value = getattr(self, "main_role_id", None)
        if value is not None:  # Skip None values
            # Single element field: main_role_id
            child_element = SubElement(element, "mainRoleId")
            child_element.text = value
            
        # Process field: roles_ids (XML name: rolesIds)
        value = getattr(self, "roles_ids", None)
        if value is not None:  # Skip None values
            # Array field: roles_ids
            # Wrapped array - elements added directly to parent (wrapper already exists)
            for item in value:
                # Primitive items in wrapped array
                item_element = SubElement(element, "rolesIds")
                item_element.text = item
                


