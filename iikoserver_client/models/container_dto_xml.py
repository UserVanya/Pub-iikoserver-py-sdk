# coding: utf-8

"""
    iikoServer API

    API Server позволяет сделать интеграции, которые предназначены для работы с номенклатурой, отчетами, документами, данными сотрудников и приказами.   ### Ограничения и рекомендации - Запросы должны выполняться последовательно друг за другом - Запрашивайте данные за период не длиннее одного месяца, в идеале — за один день или неделю - Для OLAPv2-отчетов рекомендуется устанавливать build-summary=false - При построении OLAPv2-отчета рекомендуется использовать не более 7 полей - Перед выполнением запросов проверяйте их на демо сервере

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictFloat, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional, Union
from typing import Optional, Set, Union
from typing_extensions import Self

# XML support imports
from lxml import etree
from lxml.etree import Element, SubElement

class ContainerDtoXml(BaseModel):
    """
    ContainerDtoXml
    """ # noqa: E501
    id: Optional[StrictStr] = Field(default=None, description="id фасовки (guid)")
    name: Optional[StrictStr] = Field(default=None, description="Название фасовки")
    count: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Количество базовых единиц измерения товара в фасовке")
    container_weight: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Вес тары (если товар продается на вес)", alias="containerWeight")
    full_container_weight: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Вес с тарой (если товар продается на вес)", alias="fullContainerWeight")
    backward_recalculation: Optional[StrictBool] = Field(default=None, description="Обратный пересчет (true/false)", alias="backwardRecalculation")
    deleted: Optional[StrictBool] = Field(default=None, description="Признак удаления (true/false)")
    use_in_front: Optional[StrictBool] = Field(default=None, description="Использовать во фронте (true/false)", alias="useInFront")
    __properties: ClassVar[List[str]] = ["id", "name", "count", "containerWeight", "fullContainerWeight", "backwardRecalculation", "deleted", "useInFront"]
    
    # XML metadata for the class
    __xml_config: ClassVar[Dict[str, Any]] = {
        "root_name": "containerDto",
        
        
        "field_configs": {
            "id": {
                "xml_name": "id",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "name": {
                "xml_name": "name",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "count": {
                "xml_name": "count",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "container_weight": {
                "xml_name": "containerWeight",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "full_container_weight": {
                "xml_name": "fullContainerWeight",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "backward_recalculation": {
                "xml_name": "backwardRecalculation",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "deleted": {
                "xml_name": "deleted",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "use_in_front": {
                "xml_name": "useInFront",
                
                
                "is_attribute": False,
                "is_wrapped": False
            }
        }
    }


    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ContainerDtoXml from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # set to None if id (nullable) is None
        # and model_fields_set contains the field
        if self.id is None and "id" in self.model_fields_set:
            _dict['id'] = None

        # set to None if name (nullable) is None
        # and model_fields_set contains the field
        if self.name is None and "name" in self.model_fields_set:
            _dict['name'] = None

        # set to None if count (nullable) is None
        # and model_fields_set contains the field
        if self.count is None and "count" in self.model_fields_set:
            _dict['count'] = None

        # set to None if container_weight (nullable) is None
        # and model_fields_set contains the field
        if self.container_weight is None and "container_weight" in self.model_fields_set:
            _dict['containerWeight'] = None

        # set to None if full_container_weight (nullable) is None
        # and model_fields_set contains the field
        if self.full_container_weight is None and "full_container_weight" in self.model_fields_set:
            _dict['fullContainerWeight'] = None

        # set to None if backward_recalculation (nullable) is None
        # and model_fields_set contains the field
        if self.backward_recalculation is None and "backward_recalculation" in self.model_fields_set:
            _dict['backwardRecalculation'] = None

        # set to None if deleted (nullable) is None
        # and model_fields_set contains the field
        if self.deleted is None and "deleted" in self.model_fields_set:
            _dict['deleted'] = None

        # set to None if use_in_front (nullable) is None
        # and model_fields_set contains the field
        if self.use_in_front is None and "use_in_front" in self.model_fields_set:
            _dict['useInFront'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ContainerDtoXml from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "id": obj.get("id"),
            "name": obj.get("name"),
            "count": obj.get("count"),
            "containerWeight": obj.get("containerWeight"),
            "fullContainerWeight": obj.get("fullContainerWeight"),
            "backwardRecalculation": obj.get("backwardRecalculation"),
            "deleted": obj.get("deleted"),
            "useInFront": obj.get("useInFront")
        })
        return _obj



    @classmethod
    def from_xml(cls, xml_str: str) -> Optional[Self]:
        """Create an instance of ContainerDtoXml from an XML string"""
        try:
            if isinstance(xml_str, str):
                xml_str = xml_str.encode('utf-8')
            root = etree.fromstring(xml_str)
            return cls.from_xml_element(root)
        except Exception as e:
            raise ValueError(f"Failed to parse XML: {e}")

    @classmethod
    def from_xml_element(cls, element: Element) -> Optional[Self]:
        """Create an instance of ContainerDtoXml from an XML element"""
        return cls._from_xml_element_internal(element)

    @classmethod
    def _from_xml_element_internal(cls, element: Element) -> Optional[Self]:
        """Internal method to create instance from XML element"""
        if element is None:
            return None

        data = {}
        
        # Process field: id (XML name: id)
        # Handle XML element: id
        # Single element field: id
        child_element = element.find("id")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["id"] = text_value
                
        
        # Process field: name (XML name: name)
        # Handle XML element: name
        # Single element field: name
        child_element = element.find("name")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["name"] = text_value
                
        
        # Process field: count (XML name: count)
        # Handle XML element: count
        # Single element field: count
        child_element = element.find("count")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["count"] = float(text_value)
                except ValueError:
                    data["count"] = text_value
                
        
        # Process field: container_weight (XML name: containerWeight)
        # Handle XML element: container_weight
        # Single element field: container_weight
        child_element = element.find("containerWeight")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["containerWeight"] = float(text_value)
                except ValueError:
                    data["containerWeight"] = text_value
                
        
        # Process field: full_container_weight (XML name: fullContainerWeight)
        # Handle XML element: full_container_weight
        # Single element field: full_container_weight
        child_element = element.find("fullContainerWeight")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["fullContainerWeight"] = float(text_value)
                except ValueError:
                    data["fullContainerWeight"] = text_value
                
        
        # Process field: backward_recalculation (XML name: backwardRecalculation)
        # Handle XML element: backward_recalculation
        # Single element field: backward_recalculation
        child_element = element.find("backwardRecalculation")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["backwardRecalculation"] = text_value.lower() == 'true'
                
        
        # Process field: deleted (XML name: deleted)
        # Handle XML element: deleted
        # Single element field: deleted
        child_element = element.find("deleted")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["deleted"] = text_value.lower() == 'true'
                
        
        # Process field: use_in_front (XML name: useInFront)
        # Handle XML element: use_in_front
        # Single element field: use_in_front
        child_element = element.find("useInFront")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["useInFront"] = text_value.lower() == 'true'
                
        
        
        return cls.from_dict(data)

    def to_xml(self) -> str:
        """Create an XML string representation of the model"""
        root_element = Element(self.__xml_config.get("root_name", self.__class__.__name__))
        
        # Populate the XML element with model data
        self._to_xml_element_internal(root_element)
        
        # Convert to string with XML declaration
        return etree.tostring(root_element, encoding="utf-8", pretty_print=True, xml_declaration=True).decode("utf-8")

    def _to_xml_element_internal(self, element: Element) -> None:
        """Internal method to populate an XML element with model data"""
        
        # Process field: id (XML name: id)
        value = getattr(self, "id", None)
        if value is not None:  # Skip None values
            # Single element field: id
            child_element = SubElement(element, "id")
            child_element.text = value
            
        # Process field: name (XML name: name)
        value = getattr(self, "name", None)
        if value is not None:  # Skip None values
            # Single element field: name
            child_element = SubElement(element, "name")
            child_element.text = value
            
        # Process field: count (XML name: count)
        value = getattr(self, "count", None)
        if value is not None:  # Skip None values
            # Single element field: count
            child_element = SubElement(element, "count")
            child_element.text = str(value)
            
        # Process field: container_weight (XML name: containerWeight)
        value = getattr(self, "container_weight", None)
        if value is not None:  # Skip None values
            # Single element field: container_weight
            child_element = SubElement(element, "containerWeight")
            child_element.text = str(value)
            
        # Process field: full_container_weight (XML name: fullContainerWeight)
        value = getattr(self, "full_container_weight", None)
        if value is not None:  # Skip None values
            # Single element field: full_container_weight
            child_element = SubElement(element, "fullContainerWeight")
            child_element.text = str(value)
            
        # Process field: backward_recalculation (XML name: backwardRecalculation)
        value = getattr(self, "backward_recalculation", None)
        if value is not None:  # Skip None values
            # Single element field: backward_recalculation
            child_element = SubElement(element, "backwardRecalculation")
            child_element.text = str(value).lower()
            
        # Process field: deleted (XML name: deleted)
        value = getattr(self, "deleted", None)
        if value is not None:  # Skip None values
            # Single element field: deleted
            child_element = SubElement(element, "deleted")
            child_element.text = str(value).lower()
            
        # Process field: use_in_front (XML name: useInFront)
        value = getattr(self, "use_in_front", None)
        if value is not None:  # Skip None values
            # Single element field: use_in_front
            child_element = SubElement(element, "useInFront")
            child_element.text = str(value).lower()
            


