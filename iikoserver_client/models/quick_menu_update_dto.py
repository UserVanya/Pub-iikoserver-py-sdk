# coding: utf-8

"""
    iikoServer API

    API Server позволяет сделать интеграции, которые предназначены для работы с номенклатурой, отчетами, документами, данными сотрудников и приказами.   ### Ограничения и рекомендации - Запросы должны выполняться последовательно друг за другом - Запрашивайте данные за период не длиннее одного месяца, в идеале — за один день или неделю - Для OLAPv2-отчетов рекомендуется устанавливать build-summary=false - При построении OLAPv2-отчета рекомендуется использовать не более 7 полей - Перед выполнением запросов проверяйте их на демо сервере

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from iikoserver_client.models.quick_label_create_dto import QuickLabelCreateDto
from typing import Optional, Set, Union
from typing_extensions import Self

# XML support imports
from lxml import etree
from lxml.etree import Element, SubElement

class QuickMenuUpdateDto(BaseModel):
    """
    Данные для обновления быстрого меню
    """ # noqa: E501
    id: StrictStr = Field(description="UUID быстрого меню")
    depends_on_week_day: StrictBool = Field(description="Зависит ли меню от дня недели", alias="dependsOnWeekDay")
    department_id: StrictStr = Field(description="Подразделение, для которого действует данное быстрое меню", alias="departmentId")
    section_id: Optional[StrictStr] = Field(default=None, description="Отделение, для которого действует данное быстрое меню. Если null, то быстрое меню для всего подразделения", alias="sectionId")
    page_names: Annotated[List[StrictStr], Field(min_length=3, max_length=3)] = Field(description="Названия страниц (3 штуки)", alias="pageNames")
    labels: List[QuickLabelCreateDto] = Field(description="Список ячеек")
    __properties: ClassVar[List[str]] = ["id", "dependsOnWeekDay", "departmentId", "sectionId", "pageNames", "labels"]
    
    # XML metadata for the class
    __xml_config: ClassVar[Dict[str, Any]] = {
        "root_name": "QuickMenuUpdateDto",
        
        
        "field_configs": {
            "id": {
                "xml_name": "id",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "depends_on_week_day": {
                "xml_name": "dependsOnWeekDay",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "department_id": {
                "xml_name": "departmentId",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "section_id": {
                "xml_name": "sectionId",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "page_names": {
                "xml_name": "pageNames",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "labels": {
                "xml_name": "labels",
                
                
                "is_attribute": False,
                "is_wrapped": False
            }
        }
    }


    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of QuickMenuUpdateDto from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in labels (list)
        _items = []
        if self.labels:
            for _item_labels in self.labels:
                if _item_labels:
                    _items.append(_item_labels.to_dict())
            _dict['labels'] = _items
        # set to None if section_id (nullable) is None
        # and model_fields_set contains the field
        if self.section_id is None and "section_id" in self.model_fields_set:
            _dict['sectionId'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of QuickMenuUpdateDto from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "id": obj.get("id"),
            "dependsOnWeekDay": obj.get("dependsOnWeekDay"),
            "departmentId": obj.get("departmentId"),
            "sectionId": obj.get("sectionId"),
            "pageNames": obj.get("pageNames"),
            "labels": [QuickLabelCreateDto.from_dict(_item) for _item in obj["labels"]] if obj.get("labels") is not None else None
        })
        return _obj



    @classmethod
    def from_xml(cls, xml_str: str) -> Optional[Self]:
        """Create an instance of QuickMenuUpdateDto from an XML string"""
        try:
            if isinstance(xml_str, str):
                xml_str = xml_str.encode('utf-8')
            root = etree.fromstring(xml_str)
            return cls.from_xml_element(root)
        except Exception as e:
            raise ValueError(f"Failed to parse XML: {e}")

    @classmethod
    def from_xml_element(cls, element: Element) -> Optional[Self]:
        """Create an instance of QuickMenuUpdateDto from an XML element"""
        return cls._from_xml_element_internal(element)

    @classmethod
    def _from_xml_element_internal(cls, element: Element) -> Optional[Self]:
        """Internal method to create instance from XML element"""
        if element is None:
            return None

        data = {}
        
        # Process field: id (XML name: id)
        # Handle XML element: id
        # Single element field: id
        child_element = element.find("id")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["id"] = text_value
                
        
        # Process field: depends_on_week_day (XML name: dependsOnWeekDay)
        # Handle XML element: depends_on_week_day
        # Single element field: depends_on_week_day
        child_element = element.find("dependsOnWeekDay")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["dependsOnWeekDay"] = text_value.lower() == 'true'
                
        
        # Process field: department_id (XML name: departmentId)
        # Handle XML element: department_id
        # Single element field: department_id
        child_element = element.find("departmentId")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["departmentId"] = text_value
                
        
        # Process field: section_id (XML name: sectionId)
        # Handle XML element: section_id
        # Single element field: section_id
        child_element = element.find("sectionId")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["sectionId"] = text_value
                
        
        # Process field: page_names (XML name: pageNames)
        # Handle XML element: page_names
        # Array field: page_names
        # Non-wrapped array - direct elements
        # Primitive items in non-wrapped array
        child_elements = element.findall("pageNames")
        items = []
        for child in child_elements:
            text_value = child.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                items.append(text_value)
                
        data["pageNames"] = items
        
        # Process field: labels (XML name: labels)
        # Handle XML element: labels
        # Array field: labels
        # Non-wrapped array - direct elements
        # Complex items in non-wrapped array
        child_elements = element.findall("labels")
        items = []
        for child in child_elements:
            item_obj = QuickLabelCreateDto.from_xml_element(child)
            if item_obj is not None:
                items.append(item_obj)
        data["labels"] = items
        
        
        return cls.from_dict(data)

    def to_xml(self) -> str:
        """Create an XML string representation of the model"""
        root_element = Element(self.__xml_config.get("root_name", self.__class__.__name__))
        
        # Populate the XML element with model data
        self._to_xml_element_internal(root_element)
        
        # Convert to string with XML declaration
        return etree.tostring(root_element, encoding="utf-8", pretty_print=True, xml_declaration=True).decode("utf-8")

    def _to_xml_element_internal(self, element: Element) -> None:
        """Internal method to populate an XML element with model data"""
        
        # Process field: id (XML name: id)
        value = getattr(self, "id", None)
        if value is not None:  # Skip None values
            # Single element field: id
            child_element = SubElement(element, "id")
            child_element.text = value
            
        # Process field: depends_on_week_day (XML name: dependsOnWeekDay)
        value = getattr(self, "depends_on_week_day", None)
        if value is not None:  # Skip None values
            # Single element field: depends_on_week_day
            child_element = SubElement(element, "dependsOnWeekDay")
            child_element.text = str(value).lower()
            
        # Process field: department_id (XML name: departmentId)
        value = getattr(self, "department_id", None)
        if value is not None:  # Skip None values
            # Single element field: department_id
            child_element = SubElement(element, "departmentId")
            child_element.text = value
            
        # Process field: section_id (XML name: sectionId)
        value = getattr(self, "section_id", None)
        if value is not None:  # Skip None values
            # Single element field: section_id
            child_element = SubElement(element, "sectionId")
            child_element.text = value
            
        # Process field: page_names (XML name: pageNames)
        value = getattr(self, "page_names", None)
        if value is not None:  # Skip None values
            # Array field: page_names
            # Non-wrapped array - add elements directly
            for item in value:
                # Primitive items in non-wrapped array
                item_element = SubElement(element, "pageNames")
                item_element.text = item
                
        # Process field: labels (XML name: labels)
        value = getattr(self, "labels", None)
        if value is not None:  # Skip None values
            # Array field: labels
            # Non-wrapped array - add elements directly
            for item in value:
                # Complex items in non-wrapped array
                if hasattr(item, "_to_xml_element_internal"):
                    item_element = SubElement(element, "labels")
                    item._to_xml_element_internal(item_element)


