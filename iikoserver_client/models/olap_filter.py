# coding: utf-8

"""
    iikoServer API

    API Server позволяет сделать интеграции, которые предназначены для работы с номенклатурой, отчетами, документами, данными сотрудников и приказами.   ### Ограничения и рекомендации - Запросы должны выполняться последовательно друг за другом - Запрашивайте данные за период не длиннее одного месяца, в идеале — за один день или неделю - Для OLAPv2-отчетов рекомендуется устанавливать build-summary=false - При построении OLAPv2-отчета рекомендуется использовать не более 7 полей - Перед выполнением запросов проверяйте их на демо сервере

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from iikoserver_client.models.olap_period_type_enum import OlapPeriodTypeEnum
from typing import Optional, Set, Union
from typing_extensions import Self

# XML support imports
from lxml import etree
from lxml.etree import Element, SubElement

class OlapFilter(BaseModel):
    """
    OlapFilter
    """ # noqa: E501
    filter_type: StrictStr = Field(description="Тип фильтра: IncludeValues, ExcludeValues, Range, DateRange", alias="filterType")
    values: Optional[List[Any]] = Field(default=None, description="Массив значений для фильтрации (для типов IncludeValues, ExcludeValues)")
    var_from: Optional[Any] = Field(default=None, description="Начальное значение диапазона (любой тип: число, строка, дата и т.д.)", alias="from")
    to: Optional[Any] = Field(default=None, description="Конечное значение диапазона (любой тип: число, строка, дата и т.д.)")
    include_low: Optional[StrictBool] = Field(default=True, description="Включать ли нижнюю границу диапазона", alias="includeLow")
    include_high: Optional[StrictBool] = Field(default=False, description="Включать ли верхнюю границу диапазона", alias="includeHigh")
    period_type: Optional[OlapPeriodTypeEnum] = Field(default=None, alias="periodType")
    __properties: ClassVar[List[str]] = ["filterType", "values", "from", "to", "includeLow", "includeHigh", "periodType"]
    
    # XML metadata for the class
    __xml_config: ClassVar[Dict[str, Any]] = {
        "root_name": "OlapFilter",
        
        
        "field_configs": {
            "filter_type": {
                "xml_name": "filterType",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "values": {
                "xml_name": "values",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "var_from": {
                "xml_name": "from",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "to": {
                "xml_name": "to",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "include_low": {
                "xml_name": "includeLow",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "include_high": {
                "xml_name": "includeHigh",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "period_type": {
                "xml_name": "periodType",
                
                
                "is_attribute": False,
                "is_wrapped": False
            }
        }
    }


    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of OlapFilter from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # set to None if values (nullable) is None
        # and model_fields_set contains the field
        if self.values is None and "values" in self.model_fields_set:
            _dict['values'] = None

        # set to None if var_from (nullable) is None
        # and model_fields_set contains the field
        if self.var_from is None and "var_from" in self.model_fields_set:
            _dict['from'] = None

        # set to None if to (nullable) is None
        # and model_fields_set contains the field
        if self.to is None and "to" in self.model_fields_set:
            _dict['to'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of OlapFilter from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "filterType": obj.get("filterType"),
            "values": obj.get("values"),
            "from": obj.get("from"),
            "to": obj.get("to"),
            "includeLow": obj.get("includeLow") if obj.get("includeLow") is not None else True,
            "includeHigh": obj.get("includeHigh") if obj.get("includeHigh") is not None else False,
            "periodType": obj.get("periodType")
        })
        return _obj



    @classmethod
    def from_xml(cls, xml_str: str) -> Optional[Self]:
        """Create an instance of OlapFilter from an XML string"""
        try:
            if isinstance(xml_str, str):
                xml_str = xml_str.encode('utf-8')
            root = etree.fromstring(xml_str)
            return cls.from_xml_element(root)
        except Exception as e:
            raise ValueError(f"Failed to parse XML: {e}")

    @classmethod
    def from_xml_element(cls, element: Element) -> Optional[Self]:
        """Create an instance of OlapFilter from an XML element"""
        return cls._from_xml_element_internal(element)

    @classmethod
    def _from_xml_element_internal(cls, element: Element) -> Optional[Self]:
        """Internal method to create instance from XML element"""
        if element is None:
            return None

        data = {}
        
        # Process field: filter_type (XML name: filterType)
        # Handle XML element: filter_type
        # Single element field: filter_type
        child_element = element.find("filterType")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["filterType"] = text_value
                
        
        # Process field: values (XML name: values)
        # Handle XML element: values
        # Array field: values
        # Non-wrapped array - direct elements
        # Primitive items in non-wrapped array
        child_elements = element.findall("values")
        items = []
        for child in child_elements:
            text_value = child.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                
                # Handle other primitive types in non-wrapped array items
                items.append(text_value)
            
        data["values"] = items
        
        # Process field: var_from (XML name: from)
        # Handle XML element: var_from
        # Single element field: var_from
        child_element = element.find("from")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                
                # Handle other primitive types (date, datetime, uuid, etc.)
                
                data["from"] = text_value
                
                
        
        # Process field: to (XML name: to)
        # Handle XML element: to
        # Single element field: to
        child_element = element.find("to")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                
                # Handle other primitive types (date, datetime, uuid, etc.)
                
                data["to"] = text_value
                
                
        
        # Process field: include_low (XML name: includeLow)
        # Handle XML element: include_low
        # Single element field: include_low
        child_element = element.find("includeLow")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["includeLow"] = text_value.lower() == 'true'
                
        
        # Process field: include_high (XML name: includeHigh)
        # Handle XML element: include_high
        # Single element field: include_high
        child_element = element.find("includeHigh")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["includeHigh"] = text_value.lower() == 'true'
                
        
        # Process field: period_type (XML name: periodType)
        # Handle XML element: period_type
        # Single element field: period_type
        child_element = element.find("periodType")
        if child_element is not None:
            # Enum field
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["periodType"] = text_value
        
        
        return cls.from_dict(data)

    def to_xml(self) -> str:
        """Create an XML string representation of the model"""
        root_element = Element(self.__xml_config.get("root_name", self.__class__.__name__))
        
        # Populate the XML element with model data
        self._to_xml_element_internal(root_element)
        
        # Convert to string with XML declaration
        return etree.tostring(root_element, encoding="utf-8", pretty_print=True, xml_declaration=True).decode("utf-8")

    def _to_xml_element_internal(self, element: Element) -> None:
        """Internal method to populate an XML element with model data"""
        
        # Process field: filter_type (XML name: filterType)
        value = getattr(self, "filter_type", None)
        if value is not None:  # Skip None values
            # Single element field: filter_type
            child_element = SubElement(element, "filterType")
            child_element.text = value
            
        # Process field: values (XML name: values)
        value = getattr(self, "values", None)
        if value is not None:  # Skip None values
            # Array field: values
            # Non-wrapped array - add elements directly
            for item in value:
                # Primitive items in non-wrapped array
                item_element = SubElement(element, "values")
                
                # Handle other primitive types in non-wrapped array items
                item_element.text = str(item)
                
        # Process field: var_from (XML name: from)
        value = getattr(self, "var_from", None)
        if value is not None:  # Skip None values
            # Single element field: var_from
            child_element = SubElement(element, "from")
            
            # Handle other primitive types (date, datetime, uuid, etc.)
            child_element.text = str(value)
            
        # Process field: to (XML name: to)
        value = getattr(self, "to", None)
        if value is not None:  # Skip None values
            # Single element field: to
            child_element = SubElement(element, "to")
            
            # Handle other primitive types (date, datetime, uuid, etc.)
            child_element.text = str(value)
            
        # Process field: include_low (XML name: includeLow)
        value = getattr(self, "include_low", None)
        if value is not None:  # Skip None values
            # Single element field: include_low
            child_element = SubElement(element, "includeLow")
            child_element.text = str(value).lower()
            
        # Process field: include_high (XML name: includeHigh)
        value = getattr(self, "include_high", None)
        if value is not None:  # Skip None values
            # Single element field: include_high
            child_element = SubElement(element, "includeHigh")
            child_element.text = str(value).lower()
            
        # Process field: period_type (XML name: periodType)
        value = getattr(self, "period_type", None)
        if value is not None:  # Skip None values
            # Single element field: period_type
            child_element = SubElement(element, "periodType")
            # Enum field
            child_element.text = str(value)


