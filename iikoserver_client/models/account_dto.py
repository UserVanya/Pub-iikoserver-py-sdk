# coding: utf-8

"""
    iikoServer API

    API Server позволяет сделать интеграции, которые предназначены для работы с номенклатурой, отчетами, документами, данными сотрудников и приказами.   ### Ограничения и рекомендации - Запросы должны выполняться последовательно друг за другом - Запрашивайте данные за период не длиннее одного месяца, в идеале — за один день или неделю - Для OLAPv2-отчетов рекомендуется устанавливать build-summary=false - При построении OLAPv2-отчета рекомендуется использовать не более 7 полей - Перед выполнением запросов проверяйте их на демо сервере

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from iikoserver_client.models.account_type_enum import AccountTypeEnum
from typing import Optional, Set, Union
from typing_extensions import Self

# XML support imports
from lxml import etree
from lxml.etree import Element, SubElement

class AccountDto(BaseModel):
    """
    Счет
    """ # noqa: E501
    root_type: StrictStr = Field(description="Тип корневой сущности", alias="rootType")
    id: StrictStr = Field(description="UUID счета")
    account_parent_id: Optional[StrictStr] = Field(default=None, description="UUID родительского счета", alias="accountParentId")
    parent_corporate_id: Optional[StrictStr] = Field(default=None, description="UUID объекта структуры корпорации, которой принадлежит счет", alias="parentCorporateId")
    code: StrictStr = Field(description="Код счета")
    deleted: StrictBool = Field(description="Удален ли счет")
    name: StrictStr = Field(description="Название счета")
    type: AccountTypeEnum
    system: StrictBool = Field(description="Является ли счет предустановленным системным")
    custom_transactions_allowed: StrictBool = Field(description="Можно ли делать ручные проводки", alias="customTransactionsAllowed")
    __properties: ClassVar[List[str]] = ["rootType", "id", "accountParentId", "parentCorporateId", "code", "deleted", "name", "type", "system", "customTransactionsAllowed"]
    
    # XML metadata for the class
    __xml_config: ClassVar[Dict[str, Any]] = {
        "root_name": "AccountDto",
        
        
        "field_configs": {
            "root_type": {
                "xml_name": "rootType",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "id": {
                "xml_name": "id",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "account_parent_id": {
                "xml_name": "accountParentId",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "parent_corporate_id": {
                "xml_name": "parentCorporateId",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "code": {
                "xml_name": "code",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "deleted": {
                "xml_name": "deleted",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "name": {
                "xml_name": "name",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "type": {
                "xml_name": "type",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "system": {
                "xml_name": "system",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "custom_transactions_allowed": {
                "xml_name": "customTransactionsAllowed",
                
                
                "is_attribute": False,
                "is_wrapped": False
            }
        }
    }


    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of AccountDto from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # set to None if account_parent_id (nullable) is None
        # and model_fields_set contains the field
        if self.account_parent_id is None and "account_parent_id" in self.model_fields_set:
            _dict['accountParentId'] = None

        # set to None if parent_corporate_id (nullable) is None
        # and model_fields_set contains the field
        if self.parent_corporate_id is None and "parent_corporate_id" in self.model_fields_set:
            _dict['parentCorporateId'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of AccountDto from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "rootType": obj.get("rootType"),
            "id": obj.get("id"),
            "accountParentId": obj.get("accountParentId"),
            "parentCorporateId": obj.get("parentCorporateId"),
            "code": obj.get("code"),
            "deleted": obj.get("deleted"),
            "name": obj.get("name"),
            "type": obj.get("type"),
            "system": obj.get("system"),
            "customTransactionsAllowed": obj.get("customTransactionsAllowed")
        })
        return _obj



    @classmethod
    def from_xml(cls, xml_str: str) -> Optional[Self]:
        """Create an instance of AccountDto from an XML string"""
        try:
            if isinstance(xml_str, str):
                xml_str = xml_str.encode('utf-8')
            root = etree.fromstring(xml_str)
            return cls.from_xml_element(root)
        except Exception as e:
            raise ValueError(f"Failed to parse XML: {e}")

    @classmethod
    def from_xml_element(cls, element: Element) -> Optional[Self]:
        """Create an instance of AccountDto from an XML element"""
        return cls._from_xml_element_internal(element)

    @classmethod
    def _from_xml_element_internal(cls, element: Element) -> Optional[Self]:
        """Internal method to create instance from XML element"""
        if element is None:
            return None

        data = {}
        
        # Process field: root_type (XML name: rootType)
        # Handle XML element: root_type
        # Single element field: root_type
        child_element = element.find("rootType")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["rootType"] = text_value
                
        
        # Process field: id (XML name: id)
        # Handle XML element: id
        # Single element field: id
        child_element = element.find("id")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["id"] = text_value
                
        
        # Process field: account_parent_id (XML name: accountParentId)
        # Handle XML element: account_parent_id
        # Single element field: account_parent_id
        child_element = element.find("accountParentId")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["accountParentId"] = text_value
                
        
        # Process field: parent_corporate_id (XML name: parentCorporateId)
        # Handle XML element: parent_corporate_id
        # Single element field: parent_corporate_id
        child_element = element.find("parentCorporateId")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["parentCorporateId"] = text_value
                
        
        # Process field: code (XML name: code)
        # Handle XML element: code
        # Single element field: code
        child_element = element.find("code")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["code"] = text_value
                
        
        # Process field: deleted (XML name: deleted)
        # Handle XML element: deleted
        # Single element field: deleted
        child_element = element.find("deleted")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["deleted"] = text_value.lower() == 'true'
                
        
        # Process field: name (XML name: name)
        # Handle XML element: name
        # Single element field: name
        child_element = element.find("name")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["name"] = text_value
                
        
        # Process field: type (XML name: type)
        # Handle XML element: type
        # Single element field: type
        child_element = element.find("type")
        if child_element is not None:
            # Enum field
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["type"] = text_value
        
        # Process field: system (XML name: system)
        # Handle XML element: system
        # Single element field: system
        child_element = element.find("system")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["system"] = text_value.lower() == 'true'
                
        
        # Process field: custom_transactions_allowed (XML name: customTransactionsAllowed)
        # Handle XML element: custom_transactions_allowed
        # Single element field: custom_transactions_allowed
        child_element = element.find("customTransactionsAllowed")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["customTransactionsAllowed"] = text_value.lower() == 'true'
                
        
        
        return cls.from_dict(data)

    def to_xml(self) -> str:
        """Create an XML string representation of the model"""
        root_element = Element(self.__xml_config.get("root_name", self.__class__.__name__))
        
        # Populate the XML element with model data
        self._to_xml_element_internal(root_element)
        
        # Convert to string with XML declaration
        return etree.tostring(root_element, encoding="utf-8", pretty_print=True, xml_declaration=True).decode("utf-8")

    def _to_xml_element_internal(self, element: Element) -> None:
        """Internal method to populate an XML element with model data"""
        
        # Process field: root_type (XML name: rootType)
        value = getattr(self, "root_type", None)
        if value is not None:  # Skip None values
            # Single element field: root_type
            child_element = SubElement(element, "rootType")
            child_element.text = value
            
        # Process field: id (XML name: id)
        value = getattr(self, "id", None)
        if value is not None:  # Skip None values
            # Single element field: id
            child_element = SubElement(element, "id")
            child_element.text = value
            
        # Process field: account_parent_id (XML name: accountParentId)
        value = getattr(self, "account_parent_id", None)
        if value is not None:  # Skip None values
            # Single element field: account_parent_id
            child_element = SubElement(element, "accountParentId")
            child_element.text = value
            
        # Process field: parent_corporate_id (XML name: parentCorporateId)
        value = getattr(self, "parent_corporate_id", None)
        if value is not None:  # Skip None values
            # Single element field: parent_corporate_id
            child_element = SubElement(element, "parentCorporateId")
            child_element.text = value
            
        # Process field: code (XML name: code)
        value = getattr(self, "code", None)
        if value is not None:  # Skip None values
            # Single element field: code
            child_element = SubElement(element, "code")
            child_element.text = value
            
        # Process field: deleted (XML name: deleted)
        value = getattr(self, "deleted", None)
        if value is not None:  # Skip None values
            # Single element field: deleted
            child_element = SubElement(element, "deleted")
            child_element.text = str(value).lower()
            
        # Process field: name (XML name: name)
        value = getattr(self, "name", None)
        if value is not None:  # Skip None values
            # Single element field: name
            child_element = SubElement(element, "name")
            child_element.text = value
            
        # Process field: type (XML name: type)
        value = getattr(self, "type", None)
        if value is not None:  # Skip None values
            # Single element field: type
            child_element = SubElement(element, "type")
            # Enum field
            child_element.text = str(value)
        # Process field: system (XML name: system)
        value = getattr(self, "system", None)
        if value is not None:  # Skip None values
            # Single element field: system
            child_element = SubElement(element, "system")
            child_element.text = str(value).lower()
            
        # Process field: custom_transactions_allowed (XML name: customTransactionsAllowed)
        value = getattr(self, "custom_transactions_allowed", None)
        if value is not None:  # Skip None values
            # Single element field: custom_transactions_allowed
            child_element = SubElement(element, "customTransactionsAllowed")
            child_element.text = str(value).lower()
            


