# coding: utf-8

"""
    iikoServer API

    API Server позволяет сделать интеграции, которые предназначены для работы с номенклатурой, отчетами, документами, данными сотрудников и приказами.   ### Ограничения и рекомендации - Запросы должны выполняться последовательно друг за другом - Запрашивайте данные за период не длиннее одного месяца, в идеале — за один день или неделю - Для OLAPv2-отчетов рекомендуется устанавливать build-summary=false - При построении OLAPv2-отчета рекомендуется использовать не более 7 полей - Перед выполнением запросов проверяйте их на демо сервере

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictFloat, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional, Union
from typing import Optional, Set, Union
from typing_extensions import Self

# XML support imports
from lxml import etree
from lxml.etree import Element, SubElement

class PaymentDetailsXml(BaseModel):
    """
    PaymentDetailsXml
    """ # noqa: E501
    salary_department_id: Optional[StrictStr] = Field(default=None, description="Идентификатор подразделения, в котором начислена заработная плата", alias="salaryDepartmentId")
    salary_department_name: Optional[StrictStr] = Field(default=None, description="Название подразделения, в котором начислена заработная плата", alias="salaryDepartmentName")
    regular_payed_minutes: StrictInt = Field(description="Зачтенное отработанное время в минутах", alias="regularPayedMinutes")
    regular_payment_sum: Union[StrictFloat, StrictInt] = Field(description="Начисленная за смену или явку заработная плата", alias="regularPaymentSum")
    overtime_payed_minutes: StrictInt = Field(description="Зачтенное время переработки в минутах", alias="overtimePayedMinutes")
    overtime_payment_sum: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Начисленная за переработку заработная плата", alias="overtimePaymentSum")
    other_payments_sum: Union[StrictFloat, StrictInt] = Field(description="Общая сумма начисленных и не отмененных автоматических штрафов за нарушение расписания", alias="otherPaymentsSum")
    __properties: ClassVar[List[str]] = ["salaryDepartmentId", "salaryDepartmentName", "regularPayedMinutes", "regularPaymentSum", "overtimePayedMinutes", "overtimePaymentSum", "otherPaymentsSum"]
    
    # XML metadata for the class
    __xml_config: ClassVar[Dict[str, Any]] = {
        "root_name": "paymentDetails",
        
        
        "field_configs": {
            "salary_department_id": {
                "xml_name": "salaryDepartmentId",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "salary_department_name": {
                "xml_name": "salaryDepartmentName",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "regular_payed_minutes": {
                "xml_name": "regularPayedMinutes",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "regular_payment_sum": {
                "xml_name": "regularPaymentSum",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "overtime_payed_minutes": {
                "xml_name": "overtimePayedMinutes",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "overtime_payment_sum": {
                "xml_name": "overtimePaymentSum",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "other_payments_sum": {
                "xml_name": "otherPaymentsSum",
                
                
                "is_attribute": False,
                "is_wrapped": False
            }
        }
    }


    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of PaymentDetailsXml from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of PaymentDetailsXml from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "salaryDepartmentId": obj.get("salaryDepartmentId"),
            "salaryDepartmentName": obj.get("salaryDepartmentName"),
            "regularPayedMinutes": obj.get("regularPayedMinutes"),
            "regularPaymentSum": obj.get("regularPaymentSum"),
            "overtimePayedMinutes": obj.get("overtimePayedMinutes"),
            "overtimePaymentSum": obj.get("overtimePaymentSum"),
            "otherPaymentsSum": obj.get("otherPaymentsSum")
        })
        return _obj



    @classmethod
    def from_xml(cls, xml_str: str) -> Optional[Self]:
        """Create an instance of PaymentDetailsXml from an XML string"""
        try:
            if isinstance(xml_str, str):
                xml_str = xml_str.encode('utf-8')
            root = etree.fromstring(xml_str)
            return cls.from_xml_element(root)
        except Exception as e:
            raise ValueError(f"Failed to parse XML: {e}")

    @classmethod
    def from_xml_element(cls, element: Element) -> Optional[Self]:
        """Create an instance of PaymentDetailsXml from an XML element"""
        return cls._from_xml_element_internal(element)

    @classmethod
    def _from_xml_element_internal(cls, element: Element) -> Optional[Self]:
        """Internal method to create instance from XML element"""
        if element is None:
            return None

        data = {}
        
        # Process field: salary_department_id (XML name: salaryDepartmentId)
        # Handle XML element: salary_department_id
        # Single element field: salary_department_id
        child_element = element.find("salaryDepartmentId")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["salaryDepartmentId"] = text_value
                
        
        # Process field: salary_department_name (XML name: salaryDepartmentName)
        # Handle XML element: salary_department_name
        # Single element field: salary_department_name
        child_element = element.find("salaryDepartmentName")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["salaryDepartmentName"] = text_value
                
        
        # Process field: regular_payed_minutes (XML name: regularPayedMinutes)
        # Handle XML element: regular_payed_minutes
        # Single element field: regular_payed_minutes
        child_element = element.find("regularPayedMinutes")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["regularPayedMinutes"] = int(text_value)
                except ValueError:
                    data["regularPayedMinutes"] = text_value
                
        
        # Process field: regular_payment_sum (XML name: regularPaymentSum)
        # Handle XML element: regular_payment_sum
        # Single element field: regular_payment_sum
        child_element = element.find("regularPaymentSum")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["regularPaymentSum"] = float(text_value)
                except ValueError:
                    data["regularPaymentSum"] = text_value
                
        
        # Process field: overtime_payed_minutes (XML name: overtimePayedMinutes)
        # Handle XML element: overtime_payed_minutes
        # Single element field: overtime_payed_minutes
        child_element = element.find("overtimePayedMinutes")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["overtimePayedMinutes"] = int(text_value)
                except ValueError:
                    data["overtimePayedMinutes"] = text_value
                
        
        # Process field: overtime_payment_sum (XML name: overtimePaymentSum)
        # Handle XML element: overtime_payment_sum
        # Single element field: overtime_payment_sum
        child_element = element.find("overtimePaymentSum")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["overtimePaymentSum"] = float(text_value)
                except ValueError:
                    data["overtimePaymentSum"] = text_value
                
        
        # Process field: other_payments_sum (XML name: otherPaymentsSum)
        # Handle XML element: other_payments_sum
        # Single element field: other_payments_sum
        child_element = element.find("otherPaymentsSum")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["otherPaymentsSum"] = float(text_value)
                except ValueError:
                    data["otherPaymentsSum"] = text_value
                
        
        
        return cls.from_dict(data)

    def to_xml(self) -> str:
        """Create an XML string representation of the model"""
        root_element = Element(self.__xml_config.get("root_name", self.__class__.__name__))
        
        # Populate the XML element with model data
        self._to_xml_element_internal(root_element)
        
        # Convert to string with XML declaration
        return etree.tostring(root_element, encoding="utf-8", pretty_print=True, xml_declaration=True).decode("utf-8")

    def _to_xml_element_internal(self, element: Element) -> None:
        """Internal method to populate an XML element with model data"""
        
        # Process field: salary_department_id (XML name: salaryDepartmentId)
        value = getattr(self, "salary_department_id", None)
        if value is not None:  # Skip None values
            # Single element field: salary_department_id
            child_element = SubElement(element, "salaryDepartmentId")
            child_element.text = value
            
        # Process field: salary_department_name (XML name: salaryDepartmentName)
        value = getattr(self, "salary_department_name", None)
        if value is not None:  # Skip None values
            # Single element field: salary_department_name
            child_element = SubElement(element, "salaryDepartmentName")
            child_element.text = value
            
        # Process field: regular_payed_minutes (XML name: regularPayedMinutes)
        value = getattr(self, "regular_payed_minutes", None)
        if value is not None:  # Skip None values
            # Single element field: regular_payed_minutes
            child_element = SubElement(element, "regularPayedMinutes")
            child_element.text = str(value)
            
        # Process field: regular_payment_sum (XML name: regularPaymentSum)
        value = getattr(self, "regular_payment_sum", None)
        if value is not None:  # Skip None values
            # Single element field: regular_payment_sum
            child_element = SubElement(element, "regularPaymentSum")
            child_element.text = str(value)
            
        # Process field: overtime_payed_minutes (XML name: overtimePayedMinutes)
        value = getattr(self, "overtime_payed_minutes", None)
        if value is not None:  # Skip None values
            # Single element field: overtime_payed_minutes
            child_element = SubElement(element, "overtimePayedMinutes")
            child_element.text = str(value)
            
        # Process field: overtime_payment_sum (XML name: overtimePaymentSum)
        value = getattr(self, "overtime_payment_sum", None)
        if value is not None:  # Skip None values
            # Single element field: overtime_payment_sum
            child_element = SubElement(element, "overtimePaymentSum")
            child_element.text = str(value)
            
        # Process field: other_payments_sum (XML name: otherPaymentsSum)
        value = getattr(self, "other_payments_sum", None)
        if value is not None:  # Skip None values
            # Single element field: other_payments_sum
            child_element = SubElement(element, "otherPaymentsSum")
            child_element.text = str(value)
            


