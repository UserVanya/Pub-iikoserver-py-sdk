# coding: utf-8

"""
    iikoServer API

    API Server позволяет сделать интеграции, которые предназначены для работы с номенклатурой, отчетами, документами, данными сотрудников и приказами.   ### Ограничения и рекомендации - Запросы должны выполняться последовательно друг за другом - Запрашивайте данные за период не длиннее одного месяца, в идеале — за один день или неделю - Для OLAPv2-отчетов рекомендуется устанавливать build-summary=false - При построении OLAPv2-отчета рекомендуется использовать не более 7 полей - Перед выполнением запросов проверяйте их на демо сервере

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictFloat, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional, Union
from iikoserver_client.models.auth_info_dto import AuthInfoDto
from iikoserver_client.models.cash_flow_category_dto import CashFlowCategoryDto
from typing import Optional, Set, Union
from typing_extensions import Self

# XML support imports
from lxml import etree
from lxml.etree import Element, SubElement

class PaymentRecordInfoDto(BaseModel):
    """
    PaymentRecordInfoDto
    """ # noqa: E501
    id: StrictStr = Field(description="UUID проводки")
    var_date: datetime = Field(description="Дата создания в формате yyyy-MM-dd'T'HH:MM:SS", alias="date")
    creation_date: datetime = Field(description="Дата создания с привязкой ко времени", alias="creationDate")
    group: StrictStr = Field(description="Группа проводок")
    account_id: Optional[StrictStr] = Field(default=None, description="Редактируемый счет", alias="accountId")
    counteragent_id: Optional[StrictStr] = Field(default=None, description="Контрагент", alias="counteragentId")
    payment_type_id: Optional[StrictStr] = Field(default=None, description="Тип оплаты", alias="paymentTypeId")
    type: StrictStr = Field(description="Тип проводки")
    sum: Union[StrictFloat, StrictInt] = Field(description="Сумма")
    comment: Optional[StrictStr] = Field(default=None, description="Комментарий")
    auth: Optional[AuthInfoDto] = None
    cause_event_id: Optional[StrictStr] = Field(default=None, description="UUID события оплаты заказа", alias="causeEventId")
    cashier_id: Optional[StrictStr] = Field(default=None, description="UUID кассира, совершившего проводку", alias="cashierId")
    department_id: Optional[StrictStr] = Field(default=None, description="UUID торгового предприятия", alias="departmentId")
    cash_flow_category: Optional[CashFlowCategoryDto] = Field(default=None, alias="cashFlowCategory")
    __properties: ClassVar[List[str]] = ["id", "date", "creationDate", "group", "accountId", "counteragentId", "paymentTypeId", "type", "sum", "comment", "auth", "causeEventId", "cashierId", "departmentId", "cashFlowCategory"]
    
    # XML metadata for the class
    __xml_config: ClassVar[Dict[str, Any]] = {
        "root_name": "PaymentRecordInfoDto",
        
        
        "field_configs": {
            "id": {
                "xml_name": "id",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "var_date": {
                "xml_name": "date",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "creation_date": {
                "xml_name": "creationDate",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "group": {
                "xml_name": "group",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "account_id": {
                "xml_name": "accountId",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "counteragent_id": {
                "xml_name": "counteragentId",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "payment_type_id": {
                "xml_name": "paymentTypeId",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "type": {
                "xml_name": "type",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "sum": {
                "xml_name": "sum",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "comment": {
                "xml_name": "comment",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "auth": {
                "xml_name": "auth",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "cause_event_id": {
                "xml_name": "causeEventId",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "cashier_id": {
                "xml_name": "cashierId",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "department_id": {
                "xml_name": "departmentId",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "cash_flow_category": {
                "xml_name": "cashFlowCategory",
                
                
                "is_attribute": False,
                "is_wrapped": False
            }
        }
    }


    @field_validator('group')
    def group_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['CARD', 'CREDIT', 'PAYOUT', 'PAYIN']):
            raise ValueError("must be one of enum values ('CARD', 'CREDIT', 'PAYOUT', 'PAYIN')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of PaymentRecordInfoDto from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of auth
        if self.auth:
            _dict['auth'] = self.auth.to_dict()
        # override the default output from pydantic by calling `to_dict()` of cash_flow_category
        if self.cash_flow_category:
            _dict['cashFlowCategory'] = self.cash_flow_category.to_dict()
        # set to None if account_id (nullable) is None
        # and model_fields_set contains the field
        if self.account_id is None and "account_id" in self.model_fields_set:
            _dict['accountId'] = None

        # set to None if counteragent_id (nullable) is None
        # and model_fields_set contains the field
        if self.counteragent_id is None and "counteragent_id" in self.model_fields_set:
            _dict['counteragentId'] = None

        # set to None if payment_type_id (nullable) is None
        # and model_fields_set contains the field
        if self.payment_type_id is None and "payment_type_id" in self.model_fields_set:
            _dict['paymentTypeId'] = None

        # set to None if comment (nullable) is None
        # and model_fields_set contains the field
        if self.comment is None and "comment" in self.model_fields_set:
            _dict['comment'] = None

        # set to None if cause_event_id (nullable) is None
        # and model_fields_set contains the field
        if self.cause_event_id is None and "cause_event_id" in self.model_fields_set:
            _dict['causeEventId'] = None

        # set to None if cashier_id (nullable) is None
        # and model_fields_set contains the field
        if self.cashier_id is None and "cashier_id" in self.model_fields_set:
            _dict['cashierId'] = None

        # set to None if department_id (nullable) is None
        # and model_fields_set contains the field
        if self.department_id is None and "department_id" in self.model_fields_set:
            _dict['departmentId'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of PaymentRecordInfoDto from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "id": obj.get("id"),
            "date": obj.get("date"),
            "creationDate": obj.get("creationDate"),
            "group": obj.get("group"),
            "accountId": obj.get("accountId"),
            "counteragentId": obj.get("counteragentId"),
            "paymentTypeId": obj.get("paymentTypeId"),
            "type": obj.get("type"),
            "sum": obj.get("sum"),
            "comment": obj.get("comment"),
            "auth": AuthInfoDto.from_dict(obj["auth"]) if obj.get("auth") is not None else None,
            "causeEventId": obj.get("causeEventId"),
            "cashierId": obj.get("cashierId"),
            "departmentId": obj.get("departmentId"),
            "cashFlowCategory": CashFlowCategoryDto.from_dict(obj["cashFlowCategory"]) if obj.get("cashFlowCategory") is not None else None
        })
        return _obj



    @classmethod
    def from_xml(cls, xml_str: str) -> Optional[Self]:
        """Create an instance of PaymentRecordInfoDto from an XML string"""
        try:
            if isinstance(xml_str, str):
                xml_str = xml_str.encode('utf-8')
            root = etree.fromstring(xml_str)
            return cls.from_xml_element(root)
        except Exception as e:
            raise ValueError(f"Failed to parse XML: {e}")

    @classmethod
    def from_xml_element(cls, element: Element) -> Optional[Self]:
        """Create an instance of PaymentRecordInfoDto from an XML element"""
        return cls._from_xml_element_internal(element)

    @classmethod
    def _from_xml_element_internal(cls, element: Element) -> Optional[Self]:
        """Internal method to create instance from XML element"""
        if element is None:
            return None

        data = {}
        
        # Process field: id (XML name: id)
        # Handle XML element: id
        # Single element field: id
        child_element = element.find("id")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["id"] = text_value
                
        
        # Process field: var_date (XML name: date)
        # Handle XML element: var_date
        # Single element field: var_date
        child_element = element.find("date")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                
                # Handle other primitive types (date, datetime, uuid, etc.)
                # DateTime field - handle "null" specially  
                if text_value == "null":
                    data["date"] = None
                else:
                    data["date"] = text_value
                
                
        
        # Process field: creation_date (XML name: creationDate)
        # Handle XML element: creation_date
        # Single element field: creation_date
        child_element = element.find("creationDate")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                
                # Handle other primitive types (date, datetime, uuid, etc.)
                # DateTime field - handle "null" specially  
                if text_value == "null":
                    data["creationDate"] = None
                else:
                    data["creationDate"] = text_value
                
                
        
        # Process field: group (XML name: group)
        # Handle XML element: group
        # Single element field: group
        child_element = element.find("group")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["group"] = text_value
                
        
        # Process field: account_id (XML name: accountId)
        # Handle XML element: account_id
        # Single element field: account_id
        child_element = element.find("accountId")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["accountId"] = text_value
                
        
        # Process field: counteragent_id (XML name: counteragentId)
        # Handle XML element: counteragent_id
        # Single element field: counteragent_id
        child_element = element.find("counteragentId")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["counteragentId"] = text_value
                
        
        # Process field: payment_type_id (XML name: paymentTypeId)
        # Handle XML element: payment_type_id
        # Single element field: payment_type_id
        child_element = element.find("paymentTypeId")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["paymentTypeId"] = text_value
                
        
        # Process field: type (XML name: type)
        # Handle XML element: type
        # Single element field: type
        child_element = element.find("type")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["type"] = text_value
                
        
        # Process field: sum (XML name: sum)
        # Handle XML element: sum
        # Single element field: sum
        child_element = element.find("sum")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["sum"] = float(text_value)
                except ValueError:
                    data["sum"] = text_value
                
        
        # Process field: comment (XML name: comment)
        # Handle XML element: comment
        # Single element field: comment
        child_element = element.find("comment")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["comment"] = text_value
                
        
        # Process field: auth (XML name: auth)
        # Handle XML element: auth
        # Single element field: auth
        child_element = element.find("auth")
        if child_element is not None:
            # Complex object field
            nested_obj = AuthInfoDto.from_xml_element(child_element)
            if nested_obj is not None:
                data["auth"] = nested_obj
        
        # Process field: cause_event_id (XML name: causeEventId)
        # Handle XML element: cause_event_id
        # Single element field: cause_event_id
        child_element = element.find("causeEventId")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["causeEventId"] = text_value
                
        
        # Process field: cashier_id (XML name: cashierId)
        # Handle XML element: cashier_id
        # Single element field: cashier_id
        child_element = element.find("cashierId")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["cashierId"] = text_value
                
        
        # Process field: department_id (XML name: departmentId)
        # Handle XML element: department_id
        # Single element field: department_id
        child_element = element.find("departmentId")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["departmentId"] = text_value
                
        
        # Process field: cash_flow_category (XML name: cashFlowCategory)
        # Handle XML element: cash_flow_category
        # Single element field: cash_flow_category
        child_element = element.find("cashFlowCategory")
        if child_element is not None:
            # Complex object field
            nested_obj = CashFlowCategoryDto.from_xml_element(child_element)
            if nested_obj is not None:
                data["cashFlowCategory"] = nested_obj
        
        
        return cls.from_dict(data)

    def to_xml(self) -> str:
        """Create an XML string representation of the model"""
        root_element = Element(self.__xml_config.get("root_name", self.__class__.__name__))
        
        # Populate the XML element with model data
        self._to_xml_element_internal(root_element)
        
        # Convert to string with XML declaration
        return etree.tostring(root_element, encoding="utf-8", pretty_print=True, xml_declaration=True).decode("utf-8")

    def _to_xml_element_internal(self, element: Element) -> None:
        """Internal method to populate an XML element with model data"""
        
        # Process field: id (XML name: id)
        value = getattr(self, "id", None)
        if value is not None:  # Skip None values
            # Single element field: id
            child_element = SubElement(element, "id")
            child_element.text = value
            
        # Process field: var_date (XML name: date)
        value = getattr(self, "var_date", None)
        if value is not None:  # Skip None values
            # Single element field: var_date
            child_element = SubElement(element, "date")
            
            # Handle other primitive types (date, datetime, uuid, etc.)
            child_element.text = str(value)
            
        # Process field: creation_date (XML name: creationDate)
        value = getattr(self, "creation_date", None)
        if value is not None:  # Skip None values
            # Single element field: creation_date
            child_element = SubElement(element, "creationDate")
            
            # Handle other primitive types (date, datetime, uuid, etc.)
            child_element.text = str(value)
            
        # Process field: group (XML name: group)
        value = getattr(self, "group", None)
        if value is not None:  # Skip None values
            # Single element field: group
            child_element = SubElement(element, "group")
            child_element.text = value
            
        # Process field: account_id (XML name: accountId)
        value = getattr(self, "account_id", None)
        if value is not None:  # Skip None values
            # Single element field: account_id
            child_element = SubElement(element, "accountId")
            child_element.text = value
            
        # Process field: counteragent_id (XML name: counteragentId)
        value = getattr(self, "counteragent_id", None)
        if value is not None:  # Skip None values
            # Single element field: counteragent_id
            child_element = SubElement(element, "counteragentId")
            child_element.text = value
            
        # Process field: payment_type_id (XML name: paymentTypeId)
        value = getattr(self, "payment_type_id", None)
        if value is not None:  # Skip None values
            # Single element field: payment_type_id
            child_element = SubElement(element, "paymentTypeId")
            child_element.text = value
            
        # Process field: type (XML name: type)
        value = getattr(self, "type", None)
        if value is not None:  # Skip None values
            # Single element field: type
            child_element = SubElement(element, "type")
            child_element.text = value
            
        # Process field: sum (XML name: sum)
        value = getattr(self, "sum", None)
        if value is not None:  # Skip None values
            # Single element field: sum
            child_element = SubElement(element, "sum")
            child_element.text = str(value)
            
        # Process field: comment (XML name: comment)
        value = getattr(self, "comment", None)
        if value is not None:  # Skip None values
            # Single element field: comment
            child_element = SubElement(element, "comment")
            child_element.text = value
            
        # Process field: auth (XML name: auth)
        value = getattr(self, "auth", None)
        if value is not None:  # Skip None values
            # Single element field: auth
            child_element = SubElement(element, "auth")
            # Complex object field
            if hasattr(value, "_to_xml_element_internal"):
                value._to_xml_element_internal(child_element)
        # Process field: cause_event_id (XML name: causeEventId)
        value = getattr(self, "cause_event_id", None)
        if value is not None:  # Skip None values
            # Single element field: cause_event_id
            child_element = SubElement(element, "causeEventId")
            child_element.text = value
            
        # Process field: cashier_id (XML name: cashierId)
        value = getattr(self, "cashier_id", None)
        if value is not None:  # Skip None values
            # Single element field: cashier_id
            child_element = SubElement(element, "cashierId")
            child_element.text = value
            
        # Process field: department_id (XML name: departmentId)
        value = getattr(self, "department_id", None)
        if value is not None:  # Skip None values
            # Single element field: department_id
            child_element = SubElement(element, "departmentId")
            child_element.text = value
            
        # Process field: cash_flow_category (XML name: cashFlowCategory)
        value = getattr(self, "cash_flow_category", None)
        if value is not None:  # Skip None values
            # Single element field: cash_flow_category
            child_element = SubElement(element, "cashFlowCategory")
            # Complex object field
            if hasattr(value, "_to_xml_element_internal"):
                value._to_xml_element_internal(child_element)


