# coding: utf-8

"""
    iikoServer API

    API Server позволяет сделать интеграции, которые предназначены для работы с номенклатурой, отчетами, документами, данными сотрудников и приказами.   ### Ограничения и рекомендации - Запросы должны выполняться последовательно друг за другом - Запрашивайте данные за период не длиннее одного месяца, в идеале — за один день или неделю - Для OLAPv2-отчетов рекомендуется устанавливать build-summary=false - При построении OLAPv2-отчета рекомендуется использовать не более 7 полей - Перед выполнением запросов проверяйте их на демо сервере

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictFloat, StrictInt
from typing import Any, ClassVar, Dict, List, Union
from iikoserver_client.models.id_code_name_dto import IdCodeNameDto
from typing import Optional, Set, Union
from typing_extensions import Self

# XML support imports
from lxml import etree
from lxml.etree import Element, SubElement

class IncomingInventoryValidationResultItemXml(BaseModel):
    """
    IncomingInventoryValidationResultItemXml
    """ # noqa: E501
    product: IdCodeNameDto
    expected_amount: Union[StrictFloat, StrictInt] = Field(description="Расчетное количество товара. Может иметь точность выше, чем допустима в документах.", alias="expectedAmount")
    expected_sum: Union[StrictFloat, StrictInt] = Field(description="Расчетная сумма товара. Строго два знака после запятой.", alias="expectedSum")
    actual_amount: Union[StrictFloat, StrictInt] = Field(description="Фактическое количество в базовой единице измерения товара.", alias="actualAmount")
    difference_amount: Union[StrictFloat, StrictInt] = Field(description="Количество излишка (+) или недостатка (-) в базовой единице измерения товара.", alias="differenceAmount")
    difference_sum: Union[StrictFloat, StrictInt] = Field(description="Сумма излишка или недостатка проведенной инвентаризации.", alias="differenceSum")
    __properties: ClassVar[List[str]] = ["product", "expectedAmount", "expectedSum", "actualAmount", "differenceAmount", "differenceSum"]
    
    # XML metadata for the class
    __xml_config: ClassVar[Dict[str, Any]] = {
        "root_name": "IncomingInventoryValidationResultItemXml",
        
        
        "field_configs": {
            "product": {
                "xml_name": "product",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "expected_amount": {
                "xml_name": "expectedAmount",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "expected_sum": {
                "xml_name": "expectedSum",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "actual_amount": {
                "xml_name": "actualAmount",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "difference_amount": {
                "xml_name": "differenceAmount",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "difference_sum": {
                "xml_name": "differenceSum",
                
                
                "is_attribute": False,
                "is_wrapped": False
            }
        }
    }


    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of IncomingInventoryValidationResultItemXml from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of product
        if self.product:
            _dict['product'] = self.product.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of IncomingInventoryValidationResultItemXml from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "product": IdCodeNameDto.from_dict(obj["product"]) if obj.get("product") is not None else None,
            "expectedAmount": obj.get("expectedAmount"),
            "expectedSum": obj.get("expectedSum"),
            "actualAmount": obj.get("actualAmount"),
            "differenceAmount": obj.get("differenceAmount"),
            "differenceSum": obj.get("differenceSum")
        })
        return _obj



    @classmethod
    def from_xml(cls, xml_str: str) -> Optional[Self]:
        """Create an instance of IncomingInventoryValidationResultItemXml from an XML string"""
        try:
            if isinstance(xml_str, str):
                xml_str = xml_str.encode('utf-8')
            root = etree.fromstring(xml_str)
            return cls.from_xml_element(root)
        except Exception as e:
            raise ValueError(f"Failed to parse XML: {e}")

    @classmethod
    def from_xml_element(cls, element: Element) -> Optional[Self]:
        """Create an instance of IncomingInventoryValidationResultItemXml from an XML element"""
        return cls._from_xml_element_internal(element)

    @classmethod
    def _from_xml_element_internal(cls, element: Element) -> Optional[Self]:
        """Internal method to create instance from XML element"""
        if element is None:
            return None

        data = {}
        
        # Process field: product (XML name: product)
        # Handle XML element: product
        # Single element field: product
        child_element = element.find("product")
        if child_element is not None:
            # Complex object field
            nested_obj = IdCodeNameDto.from_xml_element(child_element)
            if nested_obj is not None:
                data["product"] = nested_obj
        
        # Process field: expected_amount (XML name: expectedAmount)
        # Handle XML element: expected_amount
        # Single element field: expected_amount
        child_element = element.find("expectedAmount")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["expectedAmount"] = float(text_value)
                except ValueError:
                    data["expectedAmount"] = text_value
                
        
        # Process field: expected_sum (XML name: expectedSum)
        # Handle XML element: expected_sum
        # Single element field: expected_sum
        child_element = element.find("expectedSum")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["expectedSum"] = float(text_value)
                except ValueError:
                    data["expectedSum"] = text_value
                
        
        # Process field: actual_amount (XML name: actualAmount)
        # Handle XML element: actual_amount
        # Single element field: actual_amount
        child_element = element.find("actualAmount")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["actualAmount"] = float(text_value)
                except ValueError:
                    data["actualAmount"] = text_value
                
        
        # Process field: difference_amount (XML name: differenceAmount)
        # Handle XML element: difference_amount
        # Single element field: difference_amount
        child_element = element.find("differenceAmount")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["differenceAmount"] = float(text_value)
                except ValueError:
                    data["differenceAmount"] = text_value
                
        
        # Process field: difference_sum (XML name: differenceSum)
        # Handle XML element: difference_sum
        # Single element field: difference_sum
        child_element = element.find("differenceSum")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["differenceSum"] = float(text_value)
                except ValueError:
                    data["differenceSum"] = text_value
                
        
        
        return cls.from_dict(data)

    def to_xml(self) -> str:
        """Create an XML string representation of the model"""
        root_element = Element(self.__xml_config.get("root_name", self.__class__.__name__))
        
        # Populate the XML element with model data
        self._to_xml_element_internal(root_element)
        
        # Convert to string with XML declaration
        return etree.tostring(root_element, encoding="utf-8", pretty_print=True, xml_declaration=True).decode("utf-8")

    def _to_xml_element_internal(self, element: Element) -> None:
        """Internal method to populate an XML element with model data"""
        
        # Process field: product (XML name: product)
        value = getattr(self, "product", None)
        if value is not None:  # Skip None values
            # Single element field: product
            child_element = SubElement(element, "product")
            # Complex object field
            if hasattr(value, "_to_xml_element_internal"):
                value._to_xml_element_internal(child_element)
        # Process field: expected_amount (XML name: expectedAmount)
        value = getattr(self, "expected_amount", None)
        if value is not None:  # Skip None values
            # Single element field: expected_amount
            child_element = SubElement(element, "expectedAmount")
            child_element.text = str(value)
            
        # Process field: expected_sum (XML name: expectedSum)
        value = getattr(self, "expected_sum", None)
        if value is not None:  # Skip None values
            # Single element field: expected_sum
            child_element = SubElement(element, "expectedSum")
            child_element.text = str(value)
            
        # Process field: actual_amount (XML name: actualAmount)
        value = getattr(self, "actual_amount", None)
        if value is not None:  # Skip None values
            # Single element field: actual_amount
            child_element = SubElement(element, "actualAmount")
            child_element.text = str(value)
            
        # Process field: difference_amount (XML name: differenceAmount)
        value = getattr(self, "difference_amount", None)
        if value is not None:  # Skip None values
            # Single element field: difference_amount
            child_element = SubElement(element, "differenceAmount")
            child_element.text = str(value)
            
        # Process field: difference_sum (XML name: differenceSum)
        value = getattr(self, "difference_sum", None)
        if value is not None:  # Skip None values
            # Single element field: difference_sum
            child_element = SubElement(element, "differenceSum")
            child_element.text = str(value)
            


