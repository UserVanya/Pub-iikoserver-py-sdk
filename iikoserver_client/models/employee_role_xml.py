# coding: utf-8

"""
    iikoServer API

    API Server позволяет сделать интеграции, которые предназначены для работы с номенклатурой, отчетами, документами, данными сотрудников и приказами.   ### Ограничения и рекомендации - Запросы должны выполняться последовательно друг за другом - Запрашивайте данные за период не длиннее одного месяца, в идеале — за один день или неделю - Для OLAPv2-отчетов рекомендуется устанавливать build-summary=false - При построении OLAPv2-отчета рекомендуется использовать не более 7 полей - Перед выполнением запросов проверяйте их на демо сервере

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictFloat, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional, Union
from iikoserver_client.models.payment_scheme_enum import PaymentSchemeEnum
from typing import Optional, Set, Union
from typing_extensions import Self

# XML support imports
from lxml import etree
from lxml.etree import Element, SubElement

class EmployeeRoleXml(BaseModel):
    """
    EmployeeRoleXml
    """ # noqa: E501
    id: StrictStr = Field(description="Идентификатор должности")
    code: StrictStr = Field(description="Код должности")
    name: StrictStr = Field(description="Название должности")
    payment_per_hour: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Зарплата в час", alias="paymentPerHour")
    steady_salary: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Зарплата в месяц", alias="steadySalary")
    schedule_type: Optional[PaymentSchemeEnum] = Field(default=None, alias="scheduleType")
    deleted: Optional[StrictBool] = Field(default=None, description="Удалена ли должность")
    __properties: ClassVar[List[str]] = ["id", "code", "name", "paymentPerHour", "steadySalary", "scheduleType", "deleted"]
    
    # XML metadata for the class
    __xml_config: ClassVar[Dict[str, Any]] = {
        "root_name": "role",
        
        
        "field_configs": {
            "id": {
                "xml_name": "id",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "code": {
                "xml_name": "code",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "name": {
                "xml_name": "name",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "payment_per_hour": {
                "xml_name": "paymentPerHour",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "steady_salary": {
                "xml_name": "steadySalary",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "schedule_type": {
                "xml_name": "scheduleType",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "deleted": {
                "xml_name": "deleted",
                
                
                "is_attribute": False,
                "is_wrapped": False
            }
        }
    }


    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of EmployeeRoleXml from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # set to None if payment_per_hour (nullable) is None
        # and model_fields_set contains the field
        if self.payment_per_hour is None and "payment_per_hour" in self.model_fields_set:
            _dict['paymentPerHour'] = None

        # set to None if steady_salary (nullable) is None
        # and model_fields_set contains the field
        if self.steady_salary is None and "steady_salary" in self.model_fields_set:
            _dict['steadySalary'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of EmployeeRoleXml from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "id": obj.get("id"),
            "code": obj.get("code"),
            "name": obj.get("name"),
            "paymentPerHour": obj.get("paymentPerHour"),
            "steadySalary": obj.get("steadySalary"),
            "scheduleType": obj.get("scheduleType"),
            "deleted": obj.get("deleted")
        })
        return _obj



    @classmethod
    def from_xml(cls, xml_str: str) -> Optional[Self]:
        """Create an instance of EmployeeRoleXml from an XML string"""
        try:
            if isinstance(xml_str, str):
                xml_str = xml_str.encode('utf-8')
            root = etree.fromstring(xml_str)
            return cls.from_xml_element(root)
        except Exception as e:
            raise ValueError(f"Failed to parse XML: {e}")

    @classmethod
    def from_xml_element(cls, element: Element) -> Optional[Self]:
        """Create an instance of EmployeeRoleXml from an XML element"""
        return cls._from_xml_element_internal(element)

    @classmethod
    def _from_xml_element_internal(cls, element: Element) -> Optional[Self]:
        """Internal method to create instance from XML element"""
        if element is None:
            return None

        data = {}
        
        # Process field: id (XML name: id)
        # Handle XML element: id
        # Single element field: id
        child_element = element.find("id")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["id"] = text_value
                
        
        # Process field: code (XML name: code)
        # Handle XML element: code
        # Single element field: code
        child_element = element.find("code")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["code"] = text_value
                
        
        # Process field: name (XML name: name)
        # Handle XML element: name
        # Single element field: name
        child_element = element.find("name")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["name"] = text_value
                
        
        # Process field: payment_per_hour (XML name: paymentPerHour)
        # Handle XML element: payment_per_hour
        # Single element field: payment_per_hour
        child_element = element.find("paymentPerHour")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["paymentPerHour"] = float(text_value)
                except ValueError:
                    data["paymentPerHour"] = text_value
                
        
        # Process field: steady_salary (XML name: steadySalary)
        # Handle XML element: steady_salary
        # Single element field: steady_salary
        child_element = element.find("steadySalary")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["steadySalary"] = float(text_value)
                except ValueError:
                    data["steadySalary"] = text_value
                
        
        # Process field: schedule_type (XML name: scheduleType)
        # Handle XML element: schedule_type
        # Single element field: schedule_type
        child_element = element.find("scheduleType")
        if child_element is not None:
            # Enum field
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["scheduleType"] = text_value
        
        # Process field: deleted (XML name: deleted)
        # Handle XML element: deleted
        # Single element field: deleted
        child_element = element.find("deleted")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["deleted"] = text_value.lower() == 'true'
                
        
        
        return cls.from_dict(data)

    def to_xml(self) -> str:
        """Create an XML string representation of the model"""
        root_element = Element(self.__xml_config.get("root_name", self.__class__.__name__))
        
        # Populate the XML element with model data
        self._to_xml_element_internal(root_element)
        
        # Convert to string with XML declaration
        return etree.tostring(root_element, encoding="utf-8", pretty_print=True, xml_declaration=True).decode("utf-8")

    def _to_xml_element_internal(self, element: Element) -> None:
        """Internal method to populate an XML element with model data"""
        
        # Process field: id (XML name: id)
        value = getattr(self, "id", None)
        if value is not None:  # Skip None values
            # Single element field: id
            child_element = SubElement(element, "id")
            child_element.text = value
            
        # Process field: code (XML name: code)
        value = getattr(self, "code", None)
        if value is not None:  # Skip None values
            # Single element field: code
            child_element = SubElement(element, "code")
            child_element.text = value
            
        # Process field: name (XML name: name)
        value = getattr(self, "name", None)
        if value is not None:  # Skip None values
            # Single element field: name
            child_element = SubElement(element, "name")
            child_element.text = value
            
        # Process field: payment_per_hour (XML name: paymentPerHour)
        value = getattr(self, "payment_per_hour", None)
        if value is not None:  # Skip None values
            # Single element field: payment_per_hour
            child_element = SubElement(element, "paymentPerHour")
            child_element.text = str(value)
            
        # Process field: steady_salary (XML name: steadySalary)
        value = getattr(self, "steady_salary", None)
        if value is not None:  # Skip None values
            # Single element field: steady_salary
            child_element = SubElement(element, "steadySalary")
            child_element.text = str(value)
            
        # Process field: schedule_type (XML name: scheduleType)
        value = getattr(self, "schedule_type", None)
        if value is not None:  # Skip None values
            # Single element field: schedule_type
            child_element = SubElement(element, "scheduleType")
            # Enum field
            child_element.text = str(value)
        # Process field: deleted (XML name: deleted)
        value = getattr(self, "deleted", None)
        if value is not None:  # Skip None values
            # Single element field: deleted
            child_element = SubElement(element, "deleted")
            child_element.text = str(value).lower()
            


