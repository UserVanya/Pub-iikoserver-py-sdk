# coding: utf-8

"""
    iikoServer API

    API Server позволяет сделать интеграции, которые предназначены для работы с номенклатурой, отчетами, документами, данными сотрудников и приказами.   ### Ограничения и рекомендации - Запросы должны выполняться последовательно друг за другом - Запрашивайте данные за период не длиннее одного месяца, в идеале — за один день или неделю - Для OLAPv2-отчетов рекомендуется устанавливать build-summary=false - При построении OLAPv2-отчета рекомендуется использовать не более 7 полей - Перед выполнением запросов проверяйте их на демо сервере

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictFloat, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional, Union
from iikoserver_client.models.cash_shift_status_enum import CashShiftStatusEnum
from typing import Optional, Set, Union
from typing_extensions import Self

# XML support imports
from lxml import etree
from lxml.etree import Element, SubElement

class CashShiftDto(BaseModel):
    """
    CashShiftDto
    """ # noqa: E501
    id: StrictStr = Field(description="Id смены")
    session_number: StrictInt = Field(description="Номер кассовой смены (в нумерации фронта)", alias="sessionNumber")
    fiscal_number: Optional[StrictInt] = Field(default=None, description="Фискальный номер смены (с ФРа)", alias="fiscalNumber")
    cash_reg_number: StrictInt = Field(description="Номер ФРа (в нумерации iiko)", alias="cashRegNumber")
    cash_reg_serial: Optional[StrictStr] = Field(default=None, description="Серийный номер ФРа", alias="cashRegSerial")
    open_date: datetime = Field(description="Дата открытия смены", alias="openDate")
    close_date: Optional[datetime] = Field(default=None, description="Дата закрытия смены", alias="closeDate")
    accept_date: Optional[datetime] = Field(default=None, description="Дата принятия смены. null - смена не принята", alias="acceptDate")
    manager_id: Optional[StrictStr] = Field(default=None, description="Ответственный менеджер", alias="managerId")
    responsible_user: Optional[StrictStr] = Field(default=None, description="Ответственный кассир", alias="responsibleUser")
    session_start_cash: Union[StrictFloat, StrictInt] = Field(description="Остаток в кассе на начало дня", alias="sessionStartCash")
    pay_orders: Union[StrictFloat, StrictInt] = Field(description="Сумма всех заказов с учётом скидки", alias="payOrders")
    sum_writeoff_orders: Union[StrictFloat, StrictInt] = Field(description="Сумма заказов, закрытых за счет заведения", alias="sumWriteoffOrders")
    sales_cash: Union[StrictFloat, StrictInt] = Field(description="Сумма продаж за наличные", alias="salesCash")
    sales_credit: Union[StrictFloat, StrictInt] = Field(description="Сумма продаж в кредит", alias="salesCredit")
    sales_card: Union[StrictFloat, StrictInt] = Field(description="Сумма продаж по картам", alias="salesCard")
    pay_in: Union[StrictFloat, StrictInt] = Field(description="Сумма всех внесений", alias="payIn")
    pay_out: Union[StrictFloat, StrictInt] = Field(description="Сумма всех изъятий, без учета изъятий в конце смены", alias="payOut")
    pay_income: Union[StrictFloat, StrictInt] = Field(description="Сумма изъятия в конце смены", alias="payIncome")
    cash_remain: Optional[Union[StrictFloat, StrictInt]] = Field(description="Остаток в кассе после закрытия смены", alias="cashRemain")
    cash_diff: Union[StrictFloat, StrictInt] = Field(description="Общее расхождение сумм книжных и фактических", alias="cashDiff")
    session_status: CashShiftStatusEnum = Field(alias="sessionStatus")
    conception: Optional[StrictStr] = Field(default=None, description="Концепция, которой принадлежит данная кассовая смена")
    point_of_sale: Optional[StrictStr] = Field(default=None, description="Точка продаж данной кассовой смены", alias="pointOfSale")
    __properties: ClassVar[List[str]] = ["id", "sessionNumber", "fiscalNumber", "cashRegNumber", "cashRegSerial", "openDate", "closeDate", "acceptDate", "managerId", "responsibleUser", "sessionStartCash", "payOrders", "sumWriteoffOrders", "salesCash", "salesCredit", "salesCard", "payIn", "payOut", "payIncome", "cashRemain", "cashDiff", "sessionStatus", "conception", "pointOfSale"]
    
    # XML metadata for the class
    __xml_config: ClassVar[Dict[str, Any]] = {
        "root_name": "CashShiftDto",
        
        
        "field_configs": {
            "id": {
                "xml_name": "id",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "session_number": {
                "xml_name": "sessionNumber",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "fiscal_number": {
                "xml_name": "fiscalNumber",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "cash_reg_number": {
                "xml_name": "cashRegNumber",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "cash_reg_serial": {
                "xml_name": "cashRegSerial",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "open_date": {
                "xml_name": "openDate",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "close_date": {
                "xml_name": "closeDate",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "accept_date": {
                "xml_name": "acceptDate",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "manager_id": {
                "xml_name": "managerId",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "responsible_user": {
                "xml_name": "responsibleUser",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "session_start_cash": {
                "xml_name": "sessionStartCash",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "pay_orders": {
                "xml_name": "payOrders",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "sum_writeoff_orders": {
                "xml_name": "sumWriteoffOrders",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "sales_cash": {
                "xml_name": "salesCash",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "sales_credit": {
                "xml_name": "salesCredit",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "sales_card": {
                "xml_name": "salesCard",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "pay_in": {
                "xml_name": "payIn",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "pay_out": {
                "xml_name": "payOut",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "pay_income": {
                "xml_name": "payIncome",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "cash_remain": {
                "xml_name": "cashRemain",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "cash_diff": {
                "xml_name": "cashDiff",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "session_status": {
                "xml_name": "sessionStatus",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "conception": {
                "xml_name": "conception",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "point_of_sale": {
                "xml_name": "pointOfSale",
                
                
                "is_attribute": False,
                "is_wrapped": False
            }
        }
    }


    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of CashShiftDto from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # set to None if fiscal_number (nullable) is None
        # and model_fields_set contains the field
        if self.fiscal_number is None and "fiscal_number" in self.model_fields_set:
            _dict['fiscalNumber'] = None

        # set to None if cash_reg_serial (nullable) is None
        # and model_fields_set contains the field
        if self.cash_reg_serial is None and "cash_reg_serial" in self.model_fields_set:
            _dict['cashRegSerial'] = None

        # set to None if close_date (nullable) is None
        # and model_fields_set contains the field
        if self.close_date is None and "close_date" in self.model_fields_set:
            _dict['closeDate'] = None

        # set to None if accept_date (nullable) is None
        # and model_fields_set contains the field
        if self.accept_date is None and "accept_date" in self.model_fields_set:
            _dict['acceptDate'] = None

        # set to None if manager_id (nullable) is None
        # and model_fields_set contains the field
        if self.manager_id is None and "manager_id" in self.model_fields_set:
            _dict['managerId'] = None

        # set to None if responsible_user (nullable) is None
        # and model_fields_set contains the field
        if self.responsible_user is None and "responsible_user" in self.model_fields_set:
            _dict['responsibleUser'] = None

        # set to None if cash_remain (nullable) is None
        # and model_fields_set contains the field
        if self.cash_remain is None and "cash_remain" in self.model_fields_set:
            _dict['cashRemain'] = None

        # set to None if conception (nullable) is None
        # and model_fields_set contains the field
        if self.conception is None and "conception" in self.model_fields_set:
            _dict['conception'] = None

        # set to None if point_of_sale (nullable) is None
        # and model_fields_set contains the field
        if self.point_of_sale is None and "point_of_sale" in self.model_fields_set:
            _dict['pointOfSale'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of CashShiftDto from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "id": obj.get("id"),
            "sessionNumber": obj.get("sessionNumber"),
            "fiscalNumber": obj.get("fiscalNumber"),
            "cashRegNumber": obj.get("cashRegNumber"),
            "cashRegSerial": obj.get("cashRegSerial"),
            "openDate": obj.get("openDate"),
            "closeDate": obj.get("closeDate"),
            "acceptDate": obj.get("acceptDate"),
            "managerId": obj.get("managerId"),
            "responsibleUser": obj.get("responsibleUser"),
            "sessionStartCash": obj.get("sessionStartCash"),
            "payOrders": obj.get("payOrders"),
            "sumWriteoffOrders": obj.get("sumWriteoffOrders"),
            "salesCash": obj.get("salesCash"),
            "salesCredit": obj.get("salesCredit"),
            "salesCard": obj.get("salesCard"),
            "payIn": obj.get("payIn"),
            "payOut": obj.get("payOut"),
            "payIncome": obj.get("payIncome"),
            "cashRemain": obj.get("cashRemain"),
            "cashDiff": obj.get("cashDiff"),
            "sessionStatus": obj.get("sessionStatus"),
            "conception": obj.get("conception"),
            "pointOfSale": obj.get("pointOfSale")
        })
        return _obj



    @classmethod
    def from_xml(cls, xml_str: str) -> Optional[Self]:
        """Create an instance of CashShiftDto from an XML string"""
        try:
            if isinstance(xml_str, str):
                xml_str = xml_str.encode('utf-8')
            root = etree.fromstring(xml_str)
            return cls.from_xml_element(root)
        except Exception as e:
            raise ValueError(f"Failed to parse XML: {e}")

    @classmethod
    def from_xml_element(cls, element: Element) -> Optional[Self]:
        """Create an instance of CashShiftDto from an XML element"""
        return cls._from_xml_element_internal(element)

    @classmethod
    def _from_xml_element_internal(cls, element: Element) -> Optional[Self]:
        """Internal method to create instance from XML element"""
        if element is None:
            return None

        data = {}
        
        # Process field: id (XML name: id)
        # Handle XML element: id
        # Single element field: id
        child_element = element.find("id")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["id"] = text_value
                
        
        # Process field: session_number (XML name: sessionNumber)
        # Handle XML element: session_number
        # Single element field: session_number
        child_element = element.find("sessionNumber")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["sessionNumber"] = int(text_value)
                except ValueError:
                    data["sessionNumber"] = text_value
                
        
        # Process field: fiscal_number (XML name: fiscalNumber)
        # Handle XML element: fiscal_number
        # Single element field: fiscal_number
        child_element = element.find("fiscalNumber")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["fiscalNumber"] = int(text_value)
                except ValueError:
                    data["fiscalNumber"] = text_value
                
        
        # Process field: cash_reg_number (XML name: cashRegNumber)
        # Handle XML element: cash_reg_number
        # Single element field: cash_reg_number
        child_element = element.find("cashRegNumber")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["cashRegNumber"] = int(text_value)
                except ValueError:
                    data["cashRegNumber"] = text_value
                
        
        # Process field: cash_reg_serial (XML name: cashRegSerial)
        # Handle XML element: cash_reg_serial
        # Single element field: cash_reg_serial
        child_element = element.find("cashRegSerial")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["cashRegSerial"] = text_value
                
        
        # Process field: open_date (XML name: openDate)
        # Handle XML element: open_date
        # Single element field: open_date
        child_element = element.find("openDate")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                
                # Handle other primitive types (date, datetime, uuid, etc.)
                # DateTime field - handle "null" specially  
                if text_value == "null":
                    data["openDate"] = None
                else:
                    data["openDate"] = text_value
                
                
        
        # Process field: close_date (XML name: closeDate)
        # Handle XML element: close_date
        # Single element field: close_date
        child_element = element.find("closeDate")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                
                # Handle other primitive types (date, datetime, uuid, etc.)
                # DateTime field - handle "null" specially  
                if text_value == "null":
                    data["closeDate"] = None
                else:
                    data["closeDate"] = text_value
                
                
        
        # Process field: accept_date (XML name: acceptDate)
        # Handle XML element: accept_date
        # Single element field: accept_date
        child_element = element.find("acceptDate")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                
                # Handle other primitive types (date, datetime, uuid, etc.)
                # DateTime field - handle "null" specially  
                if text_value == "null":
                    data["acceptDate"] = None
                else:
                    data["acceptDate"] = text_value
                
                
        
        # Process field: manager_id (XML name: managerId)
        # Handle XML element: manager_id
        # Single element field: manager_id
        child_element = element.find("managerId")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["managerId"] = text_value
                
        
        # Process field: responsible_user (XML name: responsibleUser)
        # Handle XML element: responsible_user
        # Single element field: responsible_user
        child_element = element.find("responsibleUser")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["responsibleUser"] = text_value
                
        
        # Process field: session_start_cash (XML name: sessionStartCash)
        # Handle XML element: session_start_cash
        # Single element field: session_start_cash
        child_element = element.find("sessionStartCash")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["sessionStartCash"] = float(text_value)
                except ValueError:
                    data["sessionStartCash"] = text_value
                
        
        # Process field: pay_orders (XML name: payOrders)
        # Handle XML element: pay_orders
        # Single element field: pay_orders
        child_element = element.find("payOrders")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["payOrders"] = float(text_value)
                except ValueError:
                    data["payOrders"] = text_value
                
        
        # Process field: sum_writeoff_orders (XML name: sumWriteoffOrders)
        # Handle XML element: sum_writeoff_orders
        # Single element field: sum_writeoff_orders
        child_element = element.find("sumWriteoffOrders")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["sumWriteoffOrders"] = float(text_value)
                except ValueError:
                    data["sumWriteoffOrders"] = text_value
                
        
        # Process field: sales_cash (XML name: salesCash)
        # Handle XML element: sales_cash
        # Single element field: sales_cash
        child_element = element.find("salesCash")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["salesCash"] = float(text_value)
                except ValueError:
                    data["salesCash"] = text_value
                
        
        # Process field: sales_credit (XML name: salesCredit)
        # Handle XML element: sales_credit
        # Single element field: sales_credit
        child_element = element.find("salesCredit")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["salesCredit"] = float(text_value)
                except ValueError:
                    data["salesCredit"] = text_value
                
        
        # Process field: sales_card (XML name: salesCard)
        # Handle XML element: sales_card
        # Single element field: sales_card
        child_element = element.find("salesCard")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["salesCard"] = float(text_value)
                except ValueError:
                    data["salesCard"] = text_value
                
        
        # Process field: pay_in (XML name: payIn)
        # Handle XML element: pay_in
        # Single element field: pay_in
        child_element = element.find("payIn")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["payIn"] = float(text_value)
                except ValueError:
                    data["payIn"] = text_value
                
        
        # Process field: pay_out (XML name: payOut)
        # Handle XML element: pay_out
        # Single element field: pay_out
        child_element = element.find("payOut")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["payOut"] = float(text_value)
                except ValueError:
                    data["payOut"] = text_value
                
        
        # Process field: pay_income (XML name: payIncome)
        # Handle XML element: pay_income
        # Single element field: pay_income
        child_element = element.find("payIncome")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["payIncome"] = float(text_value)
                except ValueError:
                    data["payIncome"] = text_value
                
        
        # Process field: cash_remain (XML name: cashRemain)
        # Handle XML element: cash_remain
        # Single element field: cash_remain
        child_element = element.find("cashRemain")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["cashRemain"] = float(text_value)
                except ValueError:
                    data["cashRemain"] = text_value
                
        
        # Process field: cash_diff (XML name: cashDiff)
        # Handle XML element: cash_diff
        # Single element field: cash_diff
        child_element = element.find("cashDiff")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["cashDiff"] = float(text_value)
                except ValueError:
                    data["cashDiff"] = text_value
                
        
        # Process field: session_status (XML name: sessionStatus)
        # Handle XML element: session_status
        # Single element field: session_status
        child_element = element.find("sessionStatus")
        if child_element is not None:
            # Enum field
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["sessionStatus"] = text_value
        
        # Process field: conception (XML name: conception)
        # Handle XML element: conception
        # Single element field: conception
        child_element = element.find("conception")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["conception"] = text_value
                
        
        # Process field: point_of_sale (XML name: pointOfSale)
        # Handle XML element: point_of_sale
        # Single element field: point_of_sale
        child_element = element.find("pointOfSale")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["pointOfSale"] = text_value
                
        
        
        return cls.from_dict(data)

    def to_xml(self) -> str:
        """Create an XML string representation of the model"""
        root_element = Element(self.__xml_config.get("root_name", self.__class__.__name__))
        
        # Populate the XML element with model data
        self._to_xml_element_internal(root_element)
        
        # Convert to string with XML declaration
        return etree.tostring(root_element, encoding="utf-8", pretty_print=True, xml_declaration=True).decode("utf-8")

    def _to_xml_element_internal(self, element: Element) -> None:
        """Internal method to populate an XML element with model data"""
        
        # Process field: id (XML name: id)
        value = getattr(self, "id", None)
        if value is not None:  # Skip None values
            # Single element field: id
            child_element = SubElement(element, "id")
            child_element.text = value
            
        # Process field: session_number (XML name: sessionNumber)
        value = getattr(self, "session_number", None)
        if value is not None:  # Skip None values
            # Single element field: session_number
            child_element = SubElement(element, "sessionNumber")
            child_element.text = str(value)
            
        # Process field: fiscal_number (XML name: fiscalNumber)
        value = getattr(self, "fiscal_number", None)
        if value is not None:  # Skip None values
            # Single element field: fiscal_number
            child_element = SubElement(element, "fiscalNumber")
            child_element.text = str(value)
            
        # Process field: cash_reg_number (XML name: cashRegNumber)
        value = getattr(self, "cash_reg_number", None)
        if value is not None:  # Skip None values
            # Single element field: cash_reg_number
            child_element = SubElement(element, "cashRegNumber")
            child_element.text = str(value)
            
        # Process field: cash_reg_serial (XML name: cashRegSerial)
        value = getattr(self, "cash_reg_serial", None)
        if value is not None:  # Skip None values
            # Single element field: cash_reg_serial
            child_element = SubElement(element, "cashRegSerial")
            child_element.text = value
            
        # Process field: open_date (XML name: openDate)
        value = getattr(self, "open_date", None)
        if value is not None:  # Skip None values
            # Single element field: open_date
            child_element = SubElement(element, "openDate")
            
            # Handle other primitive types (date, datetime, uuid, etc.)
            child_element.text = str(value)
            
        # Process field: close_date (XML name: closeDate)
        value = getattr(self, "close_date", None)
        if value is not None:  # Skip None values
            # Single element field: close_date
            child_element = SubElement(element, "closeDate")
            
            # Handle other primitive types (date, datetime, uuid, etc.)
            child_element.text = str(value)
            
        # Process field: accept_date (XML name: acceptDate)
        value = getattr(self, "accept_date", None)
        if value is not None:  # Skip None values
            # Single element field: accept_date
            child_element = SubElement(element, "acceptDate")
            
            # Handle other primitive types (date, datetime, uuid, etc.)
            child_element.text = str(value)
            
        # Process field: manager_id (XML name: managerId)
        value = getattr(self, "manager_id", None)
        if value is not None:  # Skip None values
            # Single element field: manager_id
            child_element = SubElement(element, "managerId")
            child_element.text = value
            
        # Process field: responsible_user (XML name: responsibleUser)
        value = getattr(self, "responsible_user", None)
        if value is not None:  # Skip None values
            # Single element field: responsible_user
            child_element = SubElement(element, "responsibleUser")
            child_element.text = value
            
        # Process field: session_start_cash (XML name: sessionStartCash)
        value = getattr(self, "session_start_cash", None)
        if value is not None:  # Skip None values
            # Single element field: session_start_cash
            child_element = SubElement(element, "sessionStartCash")
            child_element.text = str(value)
            
        # Process field: pay_orders (XML name: payOrders)
        value = getattr(self, "pay_orders", None)
        if value is not None:  # Skip None values
            # Single element field: pay_orders
            child_element = SubElement(element, "payOrders")
            child_element.text = str(value)
            
        # Process field: sum_writeoff_orders (XML name: sumWriteoffOrders)
        value = getattr(self, "sum_writeoff_orders", None)
        if value is not None:  # Skip None values
            # Single element field: sum_writeoff_orders
            child_element = SubElement(element, "sumWriteoffOrders")
            child_element.text = str(value)
            
        # Process field: sales_cash (XML name: salesCash)
        value = getattr(self, "sales_cash", None)
        if value is not None:  # Skip None values
            # Single element field: sales_cash
            child_element = SubElement(element, "salesCash")
            child_element.text = str(value)
            
        # Process field: sales_credit (XML name: salesCredit)
        value = getattr(self, "sales_credit", None)
        if value is not None:  # Skip None values
            # Single element field: sales_credit
            child_element = SubElement(element, "salesCredit")
            child_element.text = str(value)
            
        # Process field: sales_card (XML name: salesCard)
        value = getattr(self, "sales_card", None)
        if value is not None:  # Skip None values
            # Single element field: sales_card
            child_element = SubElement(element, "salesCard")
            child_element.text = str(value)
            
        # Process field: pay_in (XML name: payIn)
        value = getattr(self, "pay_in", None)
        if value is not None:  # Skip None values
            # Single element field: pay_in
            child_element = SubElement(element, "payIn")
            child_element.text = str(value)
            
        # Process field: pay_out (XML name: payOut)
        value = getattr(self, "pay_out", None)
        if value is not None:  # Skip None values
            # Single element field: pay_out
            child_element = SubElement(element, "payOut")
            child_element.text = str(value)
            
        # Process field: pay_income (XML name: payIncome)
        value = getattr(self, "pay_income", None)
        if value is not None:  # Skip None values
            # Single element field: pay_income
            child_element = SubElement(element, "payIncome")
            child_element.text = str(value)
            
        # Process field: cash_remain (XML name: cashRemain)
        value = getattr(self, "cash_remain", None)
        if value is not None:  # Skip None values
            # Single element field: cash_remain
            child_element = SubElement(element, "cashRemain")
            child_element.text = str(value)
            
        # Process field: cash_diff (XML name: cashDiff)
        value = getattr(self, "cash_diff", None)
        if value is not None:  # Skip None values
            # Single element field: cash_diff
            child_element = SubElement(element, "cashDiff")
            child_element.text = str(value)
            
        # Process field: session_status (XML name: sessionStatus)
        value = getattr(self, "session_status", None)
        if value is not None:  # Skip None values
            # Single element field: session_status
            child_element = SubElement(element, "sessionStatus")
            # Enum field
            child_element.text = str(value)
        # Process field: conception (XML name: conception)
        value = getattr(self, "conception", None)
        if value is not None:  # Skip None values
            # Single element field: conception
            child_element = SubElement(element, "conception")
            child_element.text = value
            
        # Process field: point_of_sale (XML name: pointOfSale)
        value = getattr(self, "point_of_sale", None)
        if value is not None:  # Skip None values
            # Single element field: point_of_sale
            child_element = SubElement(element, "pointOfSale")
            child_element.text = value
            


