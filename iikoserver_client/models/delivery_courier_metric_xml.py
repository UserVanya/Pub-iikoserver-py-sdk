# coding: utf-8

"""
    iikoServer API

    API Server позволяет сделать интеграции, которые предназначены для работы с номенклатурой, отчетами, документами, данными сотрудников и приказами.   ### Ограничения и рекомендации - Запросы должны выполняться последовательно друг за другом - Запрашивайте данные за период не длиннее одного месяца, в идеале — за один день или неделю - Для OLAPv2-отчетов рекомендуется устанавливать build-summary=false - При построении OLAPv2-отчета рекомендуется использовать не более 7 полей - Перед выполнением запросов проверяйте их на демо сервере

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictFloat, StrictInt
from typing import Any, ClassVar, Dict, List, Optional, Union
from iikoserver_client.models.metric_type_enum import MetricTypeEnum
from typing import Optional, Set, Union
from typing_extensions import Self

# XML support imports
from lxml import etree
from lxml.etree import Element, SubElement

class DeliveryCourierMetricXml(BaseModel):
    """
    DeliveryCourierMetricXml
    """ # noqa: E501
    doubled_orders: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Сдвоенные заказы", alias="doubledOrders")
    metric_type: Optional[MetricTypeEnum] = Field(default=None, alias="metricType")
    on_the_way_time: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Время в пути", alias="onTheWayTime")
    order_count: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Количество заказов", alias="orderCount")
    total_time: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Общее время", alias="totalTime")
    tripled_orders: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Строенные заказы", alias="tripledOrders")
    __properties: ClassVar[List[str]] = ["doubledOrders", "metricType", "onTheWayTime", "orderCount", "totalTime", "tripledOrders"]
    
    # XML metadata for the class
    __xml_config: ClassVar[Dict[str, Any]] = {
        "root_name": "metric",
        
        
        "field_configs": {
            "doubled_orders": {
                "xml_name": "doubledOrders",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "metric_type": {
                "xml_name": "metricType",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "on_the_way_time": {
                "xml_name": "onTheWayTime",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "order_count": {
                "xml_name": "orderCount",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "total_time": {
                "xml_name": "totalTime",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "tripled_orders": {
                "xml_name": "tripledOrders",
                
                
                "is_attribute": False,
                "is_wrapped": False
            }
        }
    }


    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of DeliveryCourierMetricXml from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # set to None if doubled_orders (nullable) is None
        # and model_fields_set contains the field
        if self.doubled_orders is None and "doubled_orders" in self.model_fields_set:
            _dict['doubledOrders'] = None

        # set to None if on_the_way_time (nullable) is None
        # and model_fields_set contains the field
        if self.on_the_way_time is None and "on_the_way_time" in self.model_fields_set:
            _dict['onTheWayTime'] = None

        # set to None if order_count (nullable) is None
        # and model_fields_set contains the field
        if self.order_count is None and "order_count" in self.model_fields_set:
            _dict['orderCount'] = None

        # set to None if total_time (nullable) is None
        # and model_fields_set contains the field
        if self.total_time is None and "total_time" in self.model_fields_set:
            _dict['totalTime'] = None

        # set to None if tripled_orders (nullable) is None
        # and model_fields_set contains the field
        if self.tripled_orders is None and "tripled_orders" in self.model_fields_set:
            _dict['tripledOrders'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of DeliveryCourierMetricXml from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "doubledOrders": obj.get("doubledOrders"),
            "metricType": obj.get("metricType"),
            "onTheWayTime": obj.get("onTheWayTime"),
            "orderCount": obj.get("orderCount"),
            "totalTime": obj.get("totalTime"),
            "tripledOrders": obj.get("tripledOrders")
        })
        return _obj



    @classmethod
    def from_xml(cls, xml_str: str) -> Optional[Self]:
        """Create an instance of DeliveryCourierMetricXml from an XML string"""
        try:
            if isinstance(xml_str, str):
                xml_str = xml_str.encode('utf-8')
            root = etree.fromstring(xml_str)
            return cls.from_xml_element(root)
        except Exception as e:
            raise ValueError(f"Failed to parse XML: {e}")

    @classmethod
    def from_xml_element(cls, element: Element) -> Optional[Self]:
        """Create an instance of DeliveryCourierMetricXml from an XML element"""
        return cls._from_xml_element_internal(element)

    @classmethod
    def _from_xml_element_internal(cls, element: Element) -> Optional[Self]:
        """Internal method to create instance from XML element"""
        if element is None:
            return None

        data = {}
        
        # Process field: doubled_orders (XML name: doubledOrders)
        # Handle XML element: doubled_orders
        # Single element field: doubled_orders
        child_element = element.find("doubledOrders")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["doubledOrders"] = float(text_value)
                except ValueError:
                    data["doubledOrders"] = text_value
                
        
        # Process field: metric_type (XML name: metricType)
        # Handle XML element: metric_type
        # Single element field: metric_type
        child_element = element.find("metricType")
        if child_element is not None:
            # Enum field
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["metricType"] = text_value
        
        # Process field: on_the_way_time (XML name: onTheWayTime)
        # Handle XML element: on_the_way_time
        # Single element field: on_the_way_time
        child_element = element.find("onTheWayTime")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["onTheWayTime"] = float(text_value)
                except ValueError:
                    data["onTheWayTime"] = text_value
                
        
        # Process field: order_count (XML name: orderCount)
        # Handle XML element: order_count
        # Single element field: order_count
        child_element = element.find("orderCount")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["orderCount"] = float(text_value)
                except ValueError:
                    data["orderCount"] = text_value
                
        
        # Process field: total_time (XML name: totalTime)
        # Handle XML element: total_time
        # Single element field: total_time
        child_element = element.find("totalTime")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["totalTime"] = float(text_value)
                except ValueError:
                    data["totalTime"] = text_value
                
        
        # Process field: tripled_orders (XML name: tripledOrders)
        # Handle XML element: tripled_orders
        # Single element field: tripled_orders
        child_element = element.find("tripledOrders")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["tripledOrders"] = float(text_value)
                except ValueError:
                    data["tripledOrders"] = text_value
                
        
        
        return cls.from_dict(data)

    def to_xml(self) -> str:
        """Create an XML string representation of the model"""
        root_element = Element(self.__xml_config.get("root_name", self.__class__.__name__))
        
        # Populate the XML element with model data
        self._to_xml_element_internal(root_element)
        
        # Convert to string with XML declaration
        return etree.tostring(root_element, encoding="utf-8", pretty_print=True, xml_declaration=True).decode("utf-8")

    def _to_xml_element_internal(self, element: Element) -> None:
        """Internal method to populate an XML element with model data"""
        
        # Process field: doubled_orders (XML name: doubledOrders)
        value = getattr(self, "doubled_orders", None)
        if value is not None:  # Skip None values
            # Single element field: doubled_orders
            child_element = SubElement(element, "doubledOrders")
            child_element.text = str(value)
            
        # Process field: metric_type (XML name: metricType)
        value = getattr(self, "metric_type", None)
        if value is not None:  # Skip None values
            # Single element field: metric_type
            child_element = SubElement(element, "metricType")
            # Enum field
            child_element.text = str(value)
        # Process field: on_the_way_time (XML name: onTheWayTime)
        value = getattr(self, "on_the_way_time", None)
        if value is not None:  # Skip None values
            # Single element field: on_the_way_time
            child_element = SubElement(element, "onTheWayTime")
            child_element.text = str(value)
            
        # Process field: order_count (XML name: orderCount)
        value = getattr(self, "order_count", None)
        if value is not None:  # Skip None values
            # Single element field: order_count
            child_element = SubElement(element, "orderCount")
            child_element.text = str(value)
            
        # Process field: total_time (XML name: totalTime)
        value = getattr(self, "total_time", None)
        if value is not None:  # Skip None values
            # Single element field: total_time
            child_element = SubElement(element, "totalTime")
            child_element.text = str(value)
            
        # Process field: tripled_orders (XML name: tripledOrders)
        value = getattr(self, "tripled_orders", None)
        if value is not None:  # Skip None values
            # Single element field: tripled_orders
            child_element = SubElement(element, "tripledOrders")
            child_element.text = str(value)
            


