# coding: utf-8

"""
    iikoServer API

    API Server позволяет сделать интеграции, которые предназначены для работы с номенклатурой, отчетами, документами, данными сотрудников и приказами.   ### Ограничения и рекомендации - Запросы должны выполняться последовательно друг за другом - Запрашивайте данные за период не длиннее одного месяца, в идеале — за один день или неделю - Для OLAPv2-отчетов рекомендуется устанавливать build-summary=false - При построении OLAPv2-отчета рекомендуется использовать не более 7 полей - Перед выполнением запросов проверяйте их на демо сервере

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from iikoserver_client.models.production_document_xml_items import ProductionDocumentXmlItems
from typing import Optional, Set, Union
from typing_extensions import Self

# XML support imports
from lxml import etree
from lxml.etree import Element, SubElement

class ProductionDocumentXml(BaseModel):
    """
    ProductionDocumentXml
    """ # noqa: E501
    items: Optional[ProductionDocumentXmlItems] = None
    conception: Optional[StrictStr] = Field(default=None, description="Концепция (guid)")
    conception_code: Optional[StrictStr] = Field(default=None, description="Код концепции. @since 7.8", alias="conceptionCode")
    comment: Optional[StrictStr] = Field(default=None, description="Комментарий")
    document_number: Optional[StrictStr] = Field(default=None, description="Номер документа", alias="documentNumber")
    date_incoming: Optional[StrictStr] = Field(default=None, description="Дата документа. Поддерживаемые форматы: yyyy-MM-ddTHH:mm:ss, yyyy-MM-dd (dd.MM.yyyy не рекомендуется).", alias="dateIncoming")
    store_to: Optional[StrictStr] = Field(default=None, description="На склад (guid)", alias="storeTo")
    store_from: Optional[StrictStr] = Field(default=None, description="Со склада (guid)", alias="storeFrom")
    __properties: ClassVar[List[str]] = ["items", "conception", "conceptionCode", "comment", "documentNumber", "dateIncoming", "storeTo", "storeFrom"]
    
    # XML metadata for the class
    __xml_config: ClassVar[Dict[str, Any]] = {
        "root_name": "document",
        
        
        "field_configs": {
            "items": {
                "xml_name": "items",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "conception": {
                "xml_name": "conception",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "conception_code": {
                "xml_name": "conceptionCode",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "comment": {
                "xml_name": "comment",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "document_number": {
                "xml_name": "documentNumber",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "date_incoming": {
                "xml_name": "dateIncoming",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "store_to": {
                "xml_name": "storeTo",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "store_from": {
                "xml_name": "storeFrom",
                
                
                "is_attribute": False,
                "is_wrapped": False
            }
        }
    }


    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ProductionDocumentXml from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of items
        if self.items:
            _dict['items'] = self.items.to_dict()
        # set to None if conception (nullable) is None
        # and model_fields_set contains the field
        if self.conception is None and "conception" in self.model_fields_set:
            _dict['conception'] = None

        # set to None if conception_code (nullable) is None
        # and model_fields_set contains the field
        if self.conception_code is None and "conception_code" in self.model_fields_set:
            _dict['conceptionCode'] = None

        # set to None if comment (nullable) is None
        # and model_fields_set contains the field
        if self.comment is None and "comment" in self.model_fields_set:
            _dict['comment'] = None

        # set to None if document_number (nullable) is None
        # and model_fields_set contains the field
        if self.document_number is None and "document_number" in self.model_fields_set:
            _dict['documentNumber'] = None

        # set to None if date_incoming (nullable) is None
        # and model_fields_set contains the field
        if self.date_incoming is None and "date_incoming" in self.model_fields_set:
            _dict['dateIncoming'] = None

        # set to None if store_to (nullable) is None
        # and model_fields_set contains the field
        if self.store_to is None and "store_to" in self.model_fields_set:
            _dict['storeTo'] = None

        # set to None if store_from (nullable) is None
        # and model_fields_set contains the field
        if self.store_from is None and "store_from" in self.model_fields_set:
            _dict['storeFrom'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ProductionDocumentXml from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "items": ProductionDocumentXmlItems.from_dict(obj["items"]) if obj.get("items") is not None else None,
            "conception": obj.get("conception"),
            "conceptionCode": obj.get("conceptionCode"),
            "comment": obj.get("comment"),
            "documentNumber": obj.get("documentNumber"),
            "dateIncoming": obj.get("dateIncoming"),
            "storeTo": obj.get("storeTo"),
            "storeFrom": obj.get("storeFrom")
        })
        return _obj



    @classmethod
    def from_xml(cls, xml_str: str) -> Optional[Self]:
        """Create an instance of ProductionDocumentXml from an XML string"""
        try:
            if isinstance(xml_str, str):
                xml_str = xml_str.encode('utf-8')
            root = etree.fromstring(xml_str)
            return cls.from_xml_element(root)
        except Exception as e:
            raise ValueError(f"Failed to parse XML: {e}")

    @classmethod
    def from_xml_element(cls, element: Element) -> Optional[Self]:
        """Create an instance of ProductionDocumentXml from an XML element"""
        return cls._from_xml_element_internal(element)

    @classmethod
    def _from_xml_element_internal(cls, element: Element) -> Optional[Self]:
        """Internal method to create instance from XML element"""
        if element is None:
            return None

        data = {}
        
        # Process field: items (XML name: items)
        # Handle XML element: items
        # Single element field: items
        child_element = element.find("items")
        if child_element is not None:
            # Complex object field
            nested_obj = ProductionDocumentXmlItems.from_xml_element(child_element)
            if nested_obj is not None:
                data["items"] = nested_obj
        
        # Process field: conception (XML name: conception)
        # Handle XML element: conception
        # Single element field: conception
        child_element = element.find("conception")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["conception"] = text_value
                
        
        # Process field: conception_code (XML name: conceptionCode)
        # Handle XML element: conception_code
        # Single element field: conception_code
        child_element = element.find("conceptionCode")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["conceptionCode"] = text_value
                
        
        # Process field: comment (XML name: comment)
        # Handle XML element: comment
        # Single element field: comment
        child_element = element.find("comment")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["comment"] = text_value
                
        
        # Process field: document_number (XML name: documentNumber)
        # Handle XML element: document_number
        # Single element field: document_number
        child_element = element.find("documentNumber")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["documentNumber"] = text_value
                
        
        # Process field: date_incoming (XML name: dateIncoming)
        # Handle XML element: date_incoming
        # Single element field: date_incoming
        child_element = element.find("dateIncoming")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["dateIncoming"] = text_value
                
        
        # Process field: store_to (XML name: storeTo)
        # Handle XML element: store_to
        # Single element field: store_to
        child_element = element.find("storeTo")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["storeTo"] = text_value
                
        
        # Process field: store_from (XML name: storeFrom)
        # Handle XML element: store_from
        # Single element field: store_from
        child_element = element.find("storeFrom")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["storeFrom"] = text_value
                
        
        
        return cls.from_dict(data)

    def to_xml(self) -> str:
        """Create an XML string representation of the model"""
        root_element = Element(self.__xml_config.get("root_name", self.__class__.__name__))
        
        # Populate the XML element with model data
        self._to_xml_element_internal(root_element)
        
        # Convert to string with XML declaration
        return etree.tostring(root_element, encoding="utf-8", pretty_print=True, xml_declaration=True).decode("utf-8")

    def _to_xml_element_internal(self, element: Element) -> None:
        """Internal method to populate an XML element with model data"""
        
        # Process field: items (XML name: items)
        value = getattr(self, "items", None)
        if value is not None:  # Skip None values
            # Single element field: items
            child_element = SubElement(element, "items")
            # Complex object field
            if hasattr(value, "_to_xml_element_internal"):
                value._to_xml_element_internal(child_element)
        # Process field: conception (XML name: conception)
        value = getattr(self, "conception", None)
        if value is not None:  # Skip None values
            # Single element field: conception
            child_element = SubElement(element, "conception")
            child_element.text = value
            
        # Process field: conception_code (XML name: conceptionCode)
        value = getattr(self, "conception_code", None)
        if value is not None:  # Skip None values
            # Single element field: conception_code
            child_element = SubElement(element, "conceptionCode")
            child_element.text = value
            
        # Process field: comment (XML name: comment)
        value = getattr(self, "comment", None)
        if value is not None:  # Skip None values
            # Single element field: comment
            child_element = SubElement(element, "comment")
            child_element.text = value
            
        # Process field: document_number (XML name: documentNumber)
        value = getattr(self, "document_number", None)
        if value is not None:  # Skip None values
            # Single element field: document_number
            child_element = SubElement(element, "documentNumber")
            child_element.text = value
            
        # Process field: date_incoming (XML name: dateIncoming)
        value = getattr(self, "date_incoming", None)
        if value is not None:  # Skip None values
            # Single element field: date_incoming
            child_element = SubElement(element, "dateIncoming")
            child_element.text = value
            
        # Process field: store_to (XML name: storeTo)
        value = getattr(self, "store_to", None)
        if value is not None:  # Skip None values
            # Single element field: store_to
            child_element = SubElement(element, "storeTo")
            child_element.text = value
            
        # Process field: store_from (XML name: storeFrom)
        value = getattr(self, "store_from", None)
        if value is not None:  # Skip None values
            # Single element field: store_from
            child_element = SubElement(element, "storeFrom")
            child_element.text = value
            


