# coding: utf-8

"""
    iikoServer API

    API Server позволяет сделать интеграции, которые предназначены для работы с номенклатурой, отчетами, документами, данными сотрудников и приказами.   ### Ограничения и рекомендации - Запросы должны выполняться последовательно друг за другом - Запрашивайте данные за период не длиннее одного месяца, в идеале — за один день или неделю - Для OLAPv2-отчетов рекомендуется устанавливать build-summary=false - При построении OLAPv2-отчета рекомендуется использовать не более 7 полей - Перед выполнением запросов проверяйте их на демо сервере

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field
from typing import Any, ClassVar, Dict, List, Optional
from iikoserver_client.models.item_validation_error_dto import ItemValidationErrorDto
from iikoserver_client.models.validation_error_dto import ValidationErrorDto
from typing import Optional, Set, Union
from typing_extensions import Self

# XML support imports
from lxml import etree
from lxml.etree import Element, SubElement

class CashShiftValidationErrorsDto(BaseModel):
    """
    CashShiftValidationErrorsDto
    """ # noqa: E501
    document_error: Optional[List[ValidationErrorDto]] = Field(default=None, description="Ошибки в полях документа", alias="documentError")
    item_error: Optional[List[ItemValidationErrorDto]] = Field(default=None, description="Ошибки в полях элементов документа", alias="itemError")
    __properties: ClassVar[List[str]] = ["documentError", "itemError"]
    
    # XML metadata for the class
    __xml_config: ClassVar[Dict[str, Any]] = {
        "root_name": "CashShiftValidationErrorsDto",
        
        
        "field_configs": {
            "document_error": {
                "xml_name": "documentError",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "item_error": {
                "xml_name": "itemError",
                
                
                "is_attribute": False,
                "is_wrapped": False
            }
        }
    }


    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of CashShiftValidationErrorsDto from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in document_error (list)
        _items = []
        if self.document_error:
            for _item_document_error in self.document_error:
                if _item_document_error:
                    _items.append(_item_document_error.to_dict())
            _dict['documentError'] = _items
        # override the default output from pydantic by calling `to_dict()` of each item in item_error (list)
        _items = []
        if self.item_error:
            for _item_item_error in self.item_error:
                if _item_item_error:
                    _items.append(_item_item_error.to_dict())
            _dict['itemError'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of CashShiftValidationErrorsDto from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "documentError": [ValidationErrorDto.from_dict(_item) for _item in obj["documentError"]] if obj.get("documentError") is not None else None,
            "itemError": [ItemValidationErrorDto.from_dict(_item) for _item in obj["itemError"]] if obj.get("itemError") is not None else None
        })
        return _obj



    @classmethod
    def from_xml(cls, xml_str: str) -> Optional[Self]:
        """Create an instance of CashShiftValidationErrorsDto from an XML string"""
        try:
            if isinstance(xml_str, str):
                xml_str = xml_str.encode('utf-8')
            root = etree.fromstring(xml_str)
            return cls.from_xml_element(root)
        except Exception as e:
            raise ValueError(f"Failed to parse XML: {e}")

    @classmethod
    def from_xml_element(cls, element: Element) -> Optional[Self]:
        """Create an instance of CashShiftValidationErrorsDto from an XML element"""
        return cls._from_xml_element_internal(element)

    @classmethod
    def _from_xml_element_internal(cls, element: Element) -> Optional[Self]:
        """Internal method to create instance from XML element"""
        if element is None:
            return None

        data = {}
        
        # Process field: document_error (XML name: documentError)
        # Handle XML element: document_error
        # Array field: document_error
        # Non-wrapped array - direct elements
        # Complex items in non-wrapped array
        child_elements = element.findall("documentError")
        items = []
        for child in child_elements:
            item_obj = ValidationErrorDto.from_xml_element(child)
            if item_obj is not None:
                items.append(item_obj)
        data["documentError"] = items
        
        # Process field: item_error (XML name: itemError)
        # Handle XML element: item_error
        # Array field: item_error
        # Non-wrapped array - direct elements
        # Complex items in non-wrapped array
        child_elements = element.findall("itemError")
        items = []
        for child in child_elements:
            item_obj = ItemValidationErrorDto.from_xml_element(child)
            if item_obj is not None:
                items.append(item_obj)
        data["itemError"] = items
        
        
        return cls.from_dict(data)

    def to_xml(self) -> str:
        """Create an XML string representation of the model"""
        root_element = Element(self.__xml_config.get("root_name", self.__class__.__name__))
        
        # Populate the XML element with model data
        self._to_xml_element_internal(root_element)
        
        # Convert to string with XML declaration
        return etree.tostring(root_element, encoding="utf-8", pretty_print=True, xml_declaration=True).decode("utf-8")

    def _to_xml_element_internal(self, element: Element) -> None:
        """Internal method to populate an XML element with model data"""
        
        # Process field: document_error (XML name: documentError)
        value = getattr(self, "document_error", None)
        if value is not None:  # Skip None values
            # Array field: document_error
            # Non-wrapped array - add elements directly
            for item in value:
                # Complex items in non-wrapped array
                if hasattr(item, "_to_xml_element_internal"):
                    item_element = SubElement(element, "documentError")
                    item._to_xml_element_internal(item_element)
        # Process field: item_error (XML name: itemError)
        value = getattr(self, "item_error", None)
        if value is not None:  # Skip None values
            # Array field: item_error
            # Non-wrapped array - add elements directly
            for item in value:
                # Complex items in non-wrapped array
                if hasattr(item, "_to_xml_element_internal"):
                    item_element = SubElement(element, "itemError")
                    item._to_xml_element_internal(item_element)


