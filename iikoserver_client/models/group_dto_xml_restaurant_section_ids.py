# coding: utf-8

"""
    iikoServer API

    API Server позволяет сделать интеграции, которые предназначены для работы с номенклатурой, отчетами, документами, данными сотрудников и приказами.   ### Ограничения и рекомендации - Запросы должны выполняться последовательно друг за другом - Запрашивайте данные за период не длиннее одного месяца, в идеале — за один день или неделю - Для OLAPv2-отчетов рекомендуется устанавливать build-summary=false - При построении OLAPv2-отчета рекомендуется использовать не более 7 полей - Перед выполнением запросов проверяйте их на демо сервере

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field
from typing import Any, ClassVar, Dict, List, Optional
from iikoserver_client.models.id_name_dto import IdNameDto
from typing import Optional, Set, Union
from typing_extensions import Self

# XML support imports
from lxml import etree
from lxml.etree import Element, SubElement

class GroupDtoXmlRestaurantSectionIds(BaseModel):
    """
    GroupDtoXmlRestaurantSectionIds
    """ # noqa: E501
    restaurant_section_ids: Optional[List[IdNameDto]] = Field(default=None, description="Отделения ресторана.", alias="restaurantSectionIds")
    __properties: ClassVar[List[str]] = ["restaurantSectionIds"]
    
    # XML metadata for the class
    __xml_config: ClassVar[Dict[str, Any]] = {
        "root_name": "restaurantSectionIds",
        
        
        "field_configs": {
            "restaurant_section_ids": {
                "xml_name": "restaurantSectionId",
                
                
                "is_attribute": False,
                "is_wrapped": True
            }
        }
    }


    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of GroupDtoXmlRestaurantSectionIds from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in restaurant_section_ids (list)
        _items = []
        if self.restaurant_section_ids:
            for _item_restaurant_section_ids in self.restaurant_section_ids:
                if _item_restaurant_section_ids:
                    _items.append(_item_restaurant_section_ids.to_dict())
            _dict['restaurantSectionIds'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of GroupDtoXmlRestaurantSectionIds from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "restaurantSectionIds": [IdNameDto.from_dict(_item) for _item in obj["restaurantSectionIds"]] if obj.get("restaurantSectionIds") is not None else None
        })
        return _obj



    @classmethod
    def from_xml(cls, xml_str: str) -> Optional[Self]:
        """Create an instance of GroupDtoXmlRestaurantSectionIds from an XML string"""
        try:
            if isinstance(xml_str, str):
                xml_str = xml_str.encode('utf-8')
            root = etree.fromstring(xml_str)
            return cls.from_xml_element(root)
        except Exception as e:
            raise ValueError(f"Failed to parse XML: {e}")

    @classmethod
    def from_xml_element(cls, element: Element) -> Optional[Self]:
        """Create an instance of GroupDtoXmlRestaurantSectionIds from an XML element"""
        return cls._from_xml_element_internal(element)

    @classmethod
    def _from_xml_element_internal(cls, element: Element) -> Optional[Self]:
        """Internal method to create instance from XML element"""
        if element is None:
            return None

        data = {}
        
        # Process field: restaurant_section_ids (XML name: restaurantSectionId)
        # Handle XML element: restaurant_section_ids
        # Array field: restaurant_section_ids
        # Wrapped array - current element is the wrapper, look for items directly
        items = []
        # Complex items in wrapped array
        child_elements = element.findall("restaurantSectionId")
        for child in child_elements:
            item_obj = IdNameDto.from_xml_element(child)
            if item_obj is not None:
                items.append(item_obj)
        data["restaurantSectionIds"] = items
        
        
        return cls.from_dict(data)

    def to_xml(self) -> str:
        """Create an XML string representation of the model"""
        root_element = Element(self.__xml_config.get("root_name", self.__class__.__name__))
        
        # Populate the XML element with model data
        self._to_xml_element_internal(root_element)
        
        # Convert to string with XML declaration
        return etree.tostring(root_element, encoding="utf-8", pretty_print=True, xml_declaration=True).decode("utf-8")

    def _to_xml_element_internal(self, element: Element) -> None:
        """Internal method to populate an XML element with model data"""
        
        # Process field: restaurant_section_ids (XML name: restaurantSectionId)
        value = getattr(self, "restaurant_section_ids", None)
        if value is not None:  # Skip None values
            # Array field: restaurant_section_ids
            # Wrapped array - elements added directly to parent (wrapper already exists)
            for item in value:
                # Complex items in wrapped array
                if hasattr(item, "_to_xml_element_internal"):
                    item_element = SubElement(element, "restaurantSectionId")
                    item._to_xml_element_internal(item_element)


