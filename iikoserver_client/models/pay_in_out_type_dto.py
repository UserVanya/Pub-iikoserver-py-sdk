# coding: utf-8

"""
    iikoServer API

    API Server позволяет сделать интеграции, которые предназначены для работы с номенклатурой, отчетами, документами, данными сотрудников и приказами.   ### Ограничения и рекомендации - Запросы должны выполняться последовательно друг за другом - Запрашивайте данные за период не длиннее одного месяца, в идеале — за один день или неделю - Для OLAPv2-отчетов рекомендуется устанавливать build-summary=false - При построении OLAPv2-отчета рекомендуется использовать не более 7 полей - Перед выполнением запросов проверяйте их на демо сервере

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictFloat, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional, Union
from iikoserver_client.models.cash_flow_category_dto import CashFlowCategoryDto
from iikoserver_client.models.conception_dto import ConceptionDto
from iikoserver_client.models.counter_agent_type_enum import CounterAgentTypeEnum
from iikoserver_client.models.transaction_pay_in_out_type_enum import TransactionPayInOutTypeEnum
from typing import Optional, Set, Union
from typing_extensions import Self

# XML support imports
from lxml import etree
from lxml.etree import Element, SubElement

class PayInOutTypeDto(BaseModel):
    """
    PayInOutTypeDto
    """ # noqa: E501
    id: StrictStr = Field(description="UUID внесения/изъятия в базе iiko")
    chief_account: Optional[StrictStr] = Field(default=None, description="UUID шеф-счёта. При изъятии перемещаются на корр. счёт, а при внесении наоборот", alias="chiefAccount")
    account: Optional[StrictStr] = Field(default=None, description="UUID корр-счёта. При изъятии перемещаются на корр. счёт, а при внесении наоборот")
    counteragent_type: CounterAgentTypeEnum = Field(alias="counteragentType")
    transaction_type: TransactionPayInOutTypeEnum = Field(alias="transactionType")
    cash_flow_category: Optional[CashFlowCategoryDto] = Field(default=None, alias="cashFlowCategory")
    conception: Optional[ConceptionDto] = None
    limit: Union[StrictFloat, StrictInt] = Field(description="Предельная сумма для внесений/изъятий на iikoFront")
    comment: Optional[StrictStr] = Field(default=None, description="Комментарий")
    mandatory_front_comment: StrictBool = Field(description="Требовать ввода комментария к операции в iikoFront", alias="mandatoryFrontComment")
    is_deleted: StrictBool = Field(description="Удален", alias="isDeleted")
    __properties: ClassVar[List[str]] = ["id", "chiefAccount", "account", "counteragentType", "transactionType", "cashFlowCategory", "conception", "limit", "comment", "mandatoryFrontComment", "isDeleted"]
    
    # XML metadata for the class
    __xml_config: ClassVar[Dict[str, Any]] = {
        "root_name": "PayInOutTypeDto",
        
        
        "field_configs": {
            "id": {
                "xml_name": "id",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "chief_account": {
                "xml_name": "chiefAccount",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "account": {
                "xml_name": "account",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "counteragent_type": {
                "xml_name": "counteragentType",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "transaction_type": {
                "xml_name": "transactionType",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "cash_flow_category": {
                "xml_name": "cashFlowCategory",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "conception": {
                "xml_name": "conception",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "limit": {
                "xml_name": "limit",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "comment": {
                "xml_name": "comment",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "mandatory_front_comment": {
                "xml_name": "mandatoryFrontComment",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "is_deleted": {
                "xml_name": "isDeleted",
                
                
                "is_attribute": False,
                "is_wrapped": False
            }
        }
    }


    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of PayInOutTypeDto from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of cash_flow_category
        if self.cash_flow_category:
            _dict['cashFlowCategory'] = self.cash_flow_category.to_dict()
        # override the default output from pydantic by calling `to_dict()` of conception
        if self.conception:
            _dict['conception'] = self.conception.to_dict()
        # set to None if chief_account (nullable) is None
        # and model_fields_set contains the field
        if self.chief_account is None and "chief_account" in self.model_fields_set:
            _dict['chiefAccount'] = None

        # set to None if account (nullable) is None
        # and model_fields_set contains the field
        if self.account is None and "account" in self.model_fields_set:
            _dict['account'] = None

        # set to None if comment (nullable) is None
        # and model_fields_set contains the field
        if self.comment is None and "comment" in self.model_fields_set:
            _dict['comment'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of PayInOutTypeDto from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "id": obj.get("id"),
            "chiefAccount": obj.get("chiefAccount"),
            "account": obj.get("account"),
            "counteragentType": obj.get("counteragentType"),
            "transactionType": obj.get("transactionType"),
            "cashFlowCategory": CashFlowCategoryDto.from_dict(obj["cashFlowCategory"]) if obj.get("cashFlowCategory") is not None else None,
            "conception": ConceptionDto.from_dict(obj["conception"]) if obj.get("conception") is not None else None,
            "limit": obj.get("limit"),
            "comment": obj.get("comment"),
            "mandatoryFrontComment": obj.get("mandatoryFrontComment"),
            "isDeleted": obj.get("isDeleted")
        })
        return _obj



    @classmethod
    def from_xml(cls, xml_str: str) -> Optional[Self]:
        """Create an instance of PayInOutTypeDto from an XML string"""
        try:
            if isinstance(xml_str, str):
                xml_str = xml_str.encode('utf-8')
            root = etree.fromstring(xml_str)
            return cls.from_xml_element(root)
        except Exception as e:
            raise ValueError(f"Failed to parse XML: {e}")

    @classmethod
    def from_xml_element(cls, element: Element) -> Optional[Self]:
        """Create an instance of PayInOutTypeDto from an XML element"""
        return cls._from_xml_element_internal(element)

    @classmethod
    def _from_xml_element_internal(cls, element: Element) -> Optional[Self]:
        """Internal method to create instance from XML element"""
        if element is None:
            return None

        data = {}
        
        # Process field: id (XML name: id)
        # Handle XML element: id
        # Single element field: id
        child_element = element.find("id")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["id"] = text_value
                
        
        # Process field: chief_account (XML name: chiefAccount)
        # Handle XML element: chief_account
        # Single element field: chief_account
        child_element = element.find("chiefAccount")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["chiefAccount"] = text_value
                
        
        # Process field: account (XML name: account)
        # Handle XML element: account
        # Single element field: account
        child_element = element.find("account")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["account"] = text_value
                
        
        # Process field: counteragent_type (XML name: counteragentType)
        # Handle XML element: counteragent_type
        # Single element field: counteragent_type
        child_element = element.find("counteragentType")
        if child_element is not None:
            # Enum field
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["counteragentType"] = text_value
        
        # Process field: transaction_type (XML name: transactionType)
        # Handle XML element: transaction_type
        # Single element field: transaction_type
        child_element = element.find("transactionType")
        if child_element is not None:
            # Enum field
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["transactionType"] = text_value
        
        # Process field: cash_flow_category (XML name: cashFlowCategory)
        # Handle XML element: cash_flow_category
        # Single element field: cash_flow_category
        child_element = element.find("cashFlowCategory")
        if child_element is not None:
            # Complex object field
            nested_obj = CashFlowCategoryDto.from_xml_element(child_element)
            if nested_obj is not None:
                data["cashFlowCategory"] = nested_obj
        
        # Process field: conception (XML name: conception)
        # Handle XML element: conception
        # Single element field: conception
        child_element = element.find("conception")
        if child_element is not None:
            # Complex object field
            nested_obj = ConceptionDto.from_xml_element(child_element)
            if nested_obj is not None:
                data["conception"] = nested_obj
        
        # Process field: limit (XML name: limit)
        # Handle XML element: limit
        # Single element field: limit
        child_element = element.find("limit")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["limit"] = float(text_value)
                except ValueError:
                    data["limit"] = text_value
                
        
        # Process field: comment (XML name: comment)
        # Handle XML element: comment
        # Single element field: comment
        child_element = element.find("comment")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["comment"] = text_value
                
        
        # Process field: mandatory_front_comment (XML name: mandatoryFrontComment)
        # Handle XML element: mandatory_front_comment
        # Single element field: mandatory_front_comment
        child_element = element.find("mandatoryFrontComment")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["mandatoryFrontComment"] = text_value.lower() == 'true'
                
        
        # Process field: is_deleted (XML name: isDeleted)
        # Handle XML element: is_deleted
        # Single element field: is_deleted
        child_element = element.find("isDeleted")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["isDeleted"] = text_value.lower() == 'true'
                
        
        
        return cls.from_dict(data)

    def to_xml(self) -> str:
        """Create an XML string representation of the model"""
        root_element = Element(self.__xml_config.get("root_name", self.__class__.__name__))
        
        # Populate the XML element with model data
        self._to_xml_element_internal(root_element)
        
        # Convert to string with XML declaration
        return etree.tostring(root_element, encoding="utf-8", pretty_print=True, xml_declaration=True).decode("utf-8")

    def _to_xml_element_internal(self, element: Element) -> None:
        """Internal method to populate an XML element with model data"""
        
        # Process field: id (XML name: id)
        value = getattr(self, "id", None)
        if value is not None:  # Skip None values
            # Single element field: id
            child_element = SubElement(element, "id")
            child_element.text = value
            
        # Process field: chief_account (XML name: chiefAccount)
        value = getattr(self, "chief_account", None)
        if value is not None:  # Skip None values
            # Single element field: chief_account
            child_element = SubElement(element, "chiefAccount")
            child_element.text = value
            
        # Process field: account (XML name: account)
        value = getattr(self, "account", None)
        if value is not None:  # Skip None values
            # Single element field: account
            child_element = SubElement(element, "account")
            child_element.text = value
            
        # Process field: counteragent_type (XML name: counteragentType)
        value = getattr(self, "counteragent_type", None)
        if value is not None:  # Skip None values
            # Single element field: counteragent_type
            child_element = SubElement(element, "counteragentType")
            # Enum field
            child_element.text = str(value)
        # Process field: transaction_type (XML name: transactionType)
        value = getattr(self, "transaction_type", None)
        if value is not None:  # Skip None values
            # Single element field: transaction_type
            child_element = SubElement(element, "transactionType")
            # Enum field
            child_element.text = str(value)
        # Process field: cash_flow_category (XML name: cashFlowCategory)
        value = getattr(self, "cash_flow_category", None)
        if value is not None:  # Skip None values
            # Single element field: cash_flow_category
            child_element = SubElement(element, "cashFlowCategory")
            # Complex object field
            if hasattr(value, "_to_xml_element_internal"):
                value._to_xml_element_internal(child_element)
        # Process field: conception (XML name: conception)
        value = getattr(self, "conception", None)
        if value is not None:  # Skip None values
            # Single element field: conception
            child_element = SubElement(element, "conception")
            # Complex object field
            if hasattr(value, "_to_xml_element_internal"):
                value._to_xml_element_internal(child_element)
        # Process field: limit (XML name: limit)
        value = getattr(self, "limit", None)
        if value is not None:  # Skip None values
            # Single element field: limit
            child_element = SubElement(element, "limit")
            child_element.text = str(value)
            
        # Process field: comment (XML name: comment)
        value = getattr(self, "comment", None)
        if value is not None:  # Skip None values
            # Single element field: comment
            child_element = SubElement(element, "comment")
            child_element.text = value
            
        # Process field: mandatory_front_comment (XML name: mandatoryFrontComment)
        value = getattr(self, "mandatory_front_comment", None)
        if value is not None:  # Skip None values
            # Single element field: mandatory_front_comment
            child_element = SubElement(element, "mandatoryFrontComment")
            child_element.text = str(value).lower()
            
        # Process field: is_deleted (XML name: isDeleted)
        value = getattr(self, "is_deleted", None)
        if value is not None:  # Skip None values
            # Single element field: is_deleted
            child_element = SubElement(element, "isDeleted")
            child_element.text = str(value).lower()
            


