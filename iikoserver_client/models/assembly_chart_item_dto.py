# coding: utf-8

"""
    iikoServer API

    API Server позволяет сделать интеграции, которые предназначены для работы с номенклатурой, отчетами, документами, данными сотрудников и приказами.   ### Ограничения и рекомендации - Запросы должны выполняться последовательно друг за другом - Запрашивайте данные за период не длиннее одного месяца, в идеале — за один день или неделю - Для OLAPv2-отчетов рекомендуется устанавливать build-summary=false - При построении OLAPv2-отчета рекомендуется использовать не более 7 полей - Перед выполнением запросов проверяйте их на демо сервере

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictFloat, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional, Union
from iikoserver_client.models.store_specification import StoreSpecification
from typing import Optional, Set, Union
from typing_extensions import Self

# XML support imports
from lxml import etree
from lxml.etree import Element, SubElement

class AssemblyChartItemDto(BaseModel):
    """
    AssemblyChartItemDto
    """ # noqa: E501
    sort_weight: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Порядок отображения строк в интерфейсе iikoOffice. Обычно целое число. Могут встречаться строки с одинаковым номером.", alias="sortWeight")
    product_id: StrictStr = Field(description="UUID списываемого ингредиента (товара, заготовки).", alias="productId")
    product_size_specification: Optional[StrictStr] = Field(default=None, description="UUID размера блюда. Null для строк 'общей' (COMMON) техкарты. 'Раздельные' (SPECIFIC) техкарты могут содержать строки для удаленных размеров и размеров из предыдущей сопоставленной блюду шкалы размеров (для обеспечения переходного периода при смене шкалы).", alias="productSizeSpecification")
    store_specification: Optional[StoreSpecification] = Field(default=None, alias="storeSpecification")
    amount_in: Union[StrictFloat, StrictInt] = Field(description="Брутто (в основных единицах измерения ингредиента). Именно это поле участвует в расчете списаний.", alias="amountIn")
    amount_middle: Union[StrictFloat, StrictInt] = Field(description="Нетто (в основных единицах измерения ингредиента, а НЕ в кг, отображаемых в iikoOffice).", alias="amountMiddle")
    amount_out: Union[StrictFloat, StrictInt] = Field(description="Выход готового продукта (в основных единицах измерения ингредиента, а НЕ в кг, отображаемых в iikoOffice).", alias="amountOut")
    amount_in1: Union[StrictFloat, StrictInt] = Field(description="Акт проработки/Проработка 1/Брутто (в кг).", alias="amountIn1")
    amount_out1: Union[StrictFloat, StrictInt] = Field(description="Акт проработки/Проработка 1/Нетто (в кг).", alias="amountOut1")
    amount_in2: Union[StrictFloat, StrictInt] = Field(description="Акт проработки/Проработка 2/Брутто (в кг).", alias="amountIn2")
    amount_out2: Union[StrictFloat, StrictInt] = Field(description="Акт проработки/Проработка 2/Нетто (в кг).", alias="amountOut2")
    amount_in3: Union[StrictFloat, StrictInt] = Field(description="Акт проработки/Проработка 3/Брутто (в кг).", alias="amountIn3")
    amount_out3: Union[StrictFloat, StrictInt] = Field(description="Акт проработки/Проработка 3/Нетто (в кг).", alias="amountOut3")
    package_count: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Часть от общей фасовки, которая входит в техкарту.", alias="packageCount")
    package_type_id: Optional[StrictStr] = Field(default=None, description="UUID фасовки ингредиента (product.Container).", alias="packageTypeId")
    id: StrictStr = Field(description="UUID строки")
    __properties: ClassVar[List[str]] = ["sortWeight", "productId", "productSizeSpecification", "storeSpecification", "amountIn", "amountMiddle", "amountOut", "amountIn1", "amountOut1", "amountIn2", "amountOut2", "amountIn3", "amountOut3", "packageCount", "packageTypeId", "id"]
    
    # XML metadata for the class
    __xml_config: ClassVar[Dict[str, Any]] = {
        "root_name": "AssemblyChartItemDto",
        
        
        "field_configs": {
            "sort_weight": {
                "xml_name": "sortWeight",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "product_id": {
                "xml_name": "productId",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "product_size_specification": {
                "xml_name": "productSizeSpecification",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "store_specification": {
                "xml_name": "storeSpecification",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "amount_in": {
                "xml_name": "amountIn",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "amount_middle": {
                "xml_name": "amountMiddle",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "amount_out": {
                "xml_name": "amountOut",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "amount_in1": {
                "xml_name": "amountIn1",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "amount_out1": {
                "xml_name": "amountOut1",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "amount_in2": {
                "xml_name": "amountIn2",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "amount_out2": {
                "xml_name": "amountOut2",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "amount_in3": {
                "xml_name": "amountIn3",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "amount_out3": {
                "xml_name": "amountOut3",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "package_count": {
                "xml_name": "packageCount",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "package_type_id": {
                "xml_name": "packageTypeId",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "id": {
                "xml_name": "id",
                
                
                "is_attribute": False,
                "is_wrapped": False
            }
        }
    }


    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of AssemblyChartItemDto from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of store_specification
        if self.store_specification:
            _dict['storeSpecification'] = self.store_specification.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of AssemblyChartItemDto from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "sortWeight": obj.get("sortWeight"),
            "productId": obj.get("productId"),
            "productSizeSpecification": obj.get("productSizeSpecification"),
            "storeSpecification": StoreSpecification.from_dict(obj["storeSpecification"]) if obj.get("storeSpecification") is not None else None,
            "amountIn": obj.get("amountIn"),
            "amountMiddle": obj.get("amountMiddle"),
            "amountOut": obj.get("amountOut"),
            "amountIn1": obj.get("amountIn1"),
            "amountOut1": obj.get("amountOut1"),
            "amountIn2": obj.get("amountIn2"),
            "amountOut2": obj.get("amountOut2"),
            "amountIn3": obj.get("amountIn3"),
            "amountOut3": obj.get("amountOut3"),
            "packageCount": obj.get("packageCount"),
            "packageTypeId": obj.get("packageTypeId"),
            "id": obj.get("id")
        })
        return _obj



    @classmethod
    def from_xml(cls, xml_str: str) -> Optional[Self]:
        """Create an instance of AssemblyChartItemDto from an XML string"""
        try:
            if isinstance(xml_str, str):
                xml_str = xml_str.encode('utf-8')
            root = etree.fromstring(xml_str)
            return cls.from_xml_element(root)
        except Exception as e:
            raise ValueError(f"Failed to parse XML: {e}")

    @classmethod
    def from_xml_element(cls, element: Element) -> Optional[Self]:
        """Create an instance of AssemblyChartItemDto from an XML element"""
        return cls._from_xml_element_internal(element)

    @classmethod
    def _from_xml_element_internal(cls, element: Element) -> Optional[Self]:
        """Internal method to create instance from XML element"""
        if element is None:
            return None

        data = {}
        
        # Process field: sort_weight (XML name: sortWeight)
        # Handle XML element: sort_weight
        # Single element field: sort_weight
        child_element = element.find("sortWeight")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["sortWeight"] = float(text_value)
                except ValueError:
                    data["sortWeight"] = text_value
                
        
        # Process field: product_id (XML name: productId)
        # Handle XML element: product_id
        # Single element field: product_id
        child_element = element.find("productId")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["productId"] = text_value
                
        
        # Process field: product_size_specification (XML name: productSizeSpecification)
        # Handle XML element: product_size_specification
        # Single element field: product_size_specification
        child_element = element.find("productSizeSpecification")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["productSizeSpecification"] = text_value
                
        
        # Process field: store_specification (XML name: storeSpecification)
        # Handle XML element: store_specification
        # Single element field: store_specification
        child_element = element.find("storeSpecification")
        if child_element is not None:
            # Complex object field
            nested_obj = StoreSpecification.from_xml_element(child_element)
            if nested_obj is not None:
                data["storeSpecification"] = nested_obj
        
        # Process field: amount_in (XML name: amountIn)
        # Handle XML element: amount_in
        # Single element field: amount_in
        child_element = element.find("amountIn")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["amountIn"] = float(text_value)
                except ValueError:
                    data["amountIn"] = text_value
                
        
        # Process field: amount_middle (XML name: amountMiddle)
        # Handle XML element: amount_middle
        # Single element field: amount_middle
        child_element = element.find("amountMiddle")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["amountMiddle"] = float(text_value)
                except ValueError:
                    data["amountMiddle"] = text_value
                
        
        # Process field: amount_out (XML name: amountOut)
        # Handle XML element: amount_out
        # Single element field: amount_out
        child_element = element.find("amountOut")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["amountOut"] = float(text_value)
                except ValueError:
                    data["amountOut"] = text_value
                
        
        # Process field: amount_in1 (XML name: amountIn1)
        # Handle XML element: amount_in1
        # Single element field: amount_in1
        child_element = element.find("amountIn1")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["amountIn1"] = float(text_value)
                except ValueError:
                    data["amountIn1"] = text_value
                
        
        # Process field: amount_out1 (XML name: amountOut1)
        # Handle XML element: amount_out1
        # Single element field: amount_out1
        child_element = element.find("amountOut1")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["amountOut1"] = float(text_value)
                except ValueError:
                    data["amountOut1"] = text_value
                
        
        # Process field: amount_in2 (XML name: amountIn2)
        # Handle XML element: amount_in2
        # Single element field: amount_in2
        child_element = element.find("amountIn2")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["amountIn2"] = float(text_value)
                except ValueError:
                    data["amountIn2"] = text_value
                
        
        # Process field: amount_out2 (XML name: amountOut2)
        # Handle XML element: amount_out2
        # Single element field: amount_out2
        child_element = element.find("amountOut2")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["amountOut2"] = float(text_value)
                except ValueError:
                    data["amountOut2"] = text_value
                
        
        # Process field: amount_in3 (XML name: amountIn3)
        # Handle XML element: amount_in3
        # Single element field: amount_in3
        child_element = element.find("amountIn3")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["amountIn3"] = float(text_value)
                except ValueError:
                    data["amountIn3"] = text_value
                
        
        # Process field: amount_out3 (XML name: amountOut3)
        # Handle XML element: amount_out3
        # Single element field: amount_out3
        child_element = element.find("amountOut3")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["amountOut3"] = float(text_value)
                except ValueError:
                    data["amountOut3"] = text_value
                
        
        # Process field: package_count (XML name: packageCount)
        # Handle XML element: package_count
        # Single element field: package_count
        child_element = element.find("packageCount")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["packageCount"] = float(text_value)
                except ValueError:
                    data["packageCount"] = text_value
                
        
        # Process field: package_type_id (XML name: packageTypeId)
        # Handle XML element: package_type_id
        # Single element field: package_type_id
        child_element = element.find("packageTypeId")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["packageTypeId"] = text_value
                
        
        # Process field: id (XML name: id)
        # Handle XML element: id
        # Single element field: id
        child_element = element.find("id")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["id"] = text_value
                
        
        
        return cls.from_dict(data)

    def to_xml(self) -> str:
        """Create an XML string representation of the model"""
        root_element = Element(self.__xml_config.get("root_name", self.__class__.__name__))
        
        # Populate the XML element with model data
        self._to_xml_element_internal(root_element)
        
        # Convert to string with XML declaration
        return etree.tostring(root_element, encoding="utf-8", pretty_print=True, xml_declaration=True).decode("utf-8")

    def _to_xml_element_internal(self, element: Element) -> None:
        """Internal method to populate an XML element with model data"""
        
        # Process field: sort_weight (XML name: sortWeight)
        value = getattr(self, "sort_weight", None)
        if value is not None:  # Skip None values
            # Single element field: sort_weight
            child_element = SubElement(element, "sortWeight")
            child_element.text = str(value)
            
        # Process field: product_id (XML name: productId)
        value = getattr(self, "product_id", None)
        if value is not None:  # Skip None values
            # Single element field: product_id
            child_element = SubElement(element, "productId")
            child_element.text = value
            
        # Process field: product_size_specification (XML name: productSizeSpecification)
        value = getattr(self, "product_size_specification", None)
        if value is not None:  # Skip None values
            # Single element field: product_size_specification
            child_element = SubElement(element, "productSizeSpecification")
            child_element.text = value
            
        # Process field: store_specification (XML name: storeSpecification)
        value = getattr(self, "store_specification", None)
        if value is not None:  # Skip None values
            # Single element field: store_specification
            child_element = SubElement(element, "storeSpecification")
            # Complex object field
            if hasattr(value, "_to_xml_element_internal"):
                value._to_xml_element_internal(child_element)
        # Process field: amount_in (XML name: amountIn)
        value = getattr(self, "amount_in", None)
        if value is not None:  # Skip None values
            # Single element field: amount_in
            child_element = SubElement(element, "amountIn")
            child_element.text = str(value)
            
        # Process field: amount_middle (XML name: amountMiddle)
        value = getattr(self, "amount_middle", None)
        if value is not None:  # Skip None values
            # Single element field: amount_middle
            child_element = SubElement(element, "amountMiddle")
            child_element.text = str(value)
            
        # Process field: amount_out (XML name: amountOut)
        value = getattr(self, "amount_out", None)
        if value is not None:  # Skip None values
            # Single element field: amount_out
            child_element = SubElement(element, "amountOut")
            child_element.text = str(value)
            
        # Process field: amount_in1 (XML name: amountIn1)
        value = getattr(self, "amount_in1", None)
        if value is not None:  # Skip None values
            # Single element field: amount_in1
            child_element = SubElement(element, "amountIn1")
            child_element.text = str(value)
            
        # Process field: amount_out1 (XML name: amountOut1)
        value = getattr(self, "amount_out1", None)
        if value is not None:  # Skip None values
            # Single element field: amount_out1
            child_element = SubElement(element, "amountOut1")
            child_element.text = str(value)
            
        # Process field: amount_in2 (XML name: amountIn2)
        value = getattr(self, "amount_in2", None)
        if value is not None:  # Skip None values
            # Single element field: amount_in2
            child_element = SubElement(element, "amountIn2")
            child_element.text = str(value)
            
        # Process field: amount_out2 (XML name: amountOut2)
        value = getattr(self, "amount_out2", None)
        if value is not None:  # Skip None values
            # Single element field: amount_out2
            child_element = SubElement(element, "amountOut2")
            child_element.text = str(value)
            
        # Process field: amount_in3 (XML name: amountIn3)
        value = getattr(self, "amount_in3", None)
        if value is not None:  # Skip None values
            # Single element field: amount_in3
            child_element = SubElement(element, "amountIn3")
            child_element.text = str(value)
            
        # Process field: amount_out3 (XML name: amountOut3)
        value = getattr(self, "amount_out3", None)
        if value is not None:  # Skip None values
            # Single element field: amount_out3
            child_element = SubElement(element, "amountOut3")
            child_element.text = str(value)
            
        # Process field: package_count (XML name: packageCount)
        value = getattr(self, "package_count", None)
        if value is not None:  # Skip None values
            # Single element field: package_count
            child_element = SubElement(element, "packageCount")
            child_element.text = str(value)
            
        # Process field: package_type_id (XML name: packageTypeId)
        value = getattr(self, "package_type_id", None)
        if value is not None:  # Skip None values
            # Single element field: package_type_id
            child_element = SubElement(element, "packageTypeId")
            child_element.text = value
            
        # Process field: id (XML name: id)
        value = getattr(self, "id", None)
        if value is not None:  # Skip None values
            # Single element field: id
            child_element = SubElement(element, "id")
            child_element.text = value
            


