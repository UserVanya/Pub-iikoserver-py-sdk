# coding: utf-8

"""
    iikoServer API

    API Server позволяет сделать интеграции, которые предназначены для работы с номенклатурой, отчетами, документами, данными сотрудников и приказами.   ### Ограничения и рекомендации - Запросы должны выполняться последовательно друг за другом - Запрашивайте данные за период не длиннее одного месяца, в идеале — за один день или неделю - Для OLAPv2-отчетов рекомендуется устанавливать build-summary=false - При построении OLAPv2-отчета рекомендуется использовать не более 7 полей - Перед выполнением запросов проверяйте их на демо сервере

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from iikoserver_client.models.payment_details_xml import PaymentDetailsXml
from typing import Optional, Set, Union
from typing_extensions import Self

# XML support imports
from lxml import etree
from lxml.etree import Element, SubElement

class AttendanceXml(BaseModel):
    """
    AttendanceXml
    """ # noqa: E501
    id: Optional[StrictStr] = Field(default=None, description="Идентификатор явки (при создании может быть не заполнено, при редактировании может измениться)")
    employee_id: StrictStr = Field(description="Идентификатор сотрудника", alias="employeeId")
    role_id: Optional[StrictStr] = Field(default=None, description="Должность, в которой работал сотрудник. У старых явок может быть null.", alias="roleId")
    attendance_type_id: Optional[StrictStr] = Field(default=None, description="Идентификатор типа явки", alias="attendanceTypeId")
    attendance_type: Optional[StrictStr] = Field(default=None, description="Тип явки", alias="attendanceType")
    comment: Optional[StrictStr] = Field(default=None, description="Комментарий")
    date_from: Optional[StrictStr] = Field(default=None, description="Время начала подтверждённой явки", alias="dateFrom")
    date_to: Optional[StrictStr] = Field(default=None, description="Время окончания подтверждённой явки", alias="dateTo")
    personal_date_from: Optional[datetime] = Field(default=None, description="Время начала персональной явки с iikoFront. Отсутствует, если явку создали в iikoOffice.", alias="personalDateFrom")
    personal_date_to: Optional[datetime] = Field(default=None, description="Время окончания персональной явки с iikoFront. Отсутствует, если явку создали в iikoOffice или явка не закрыта.", alias="personalDateTo")
    department_id: StrictStr = Field(description="Подразделение, в котором произошла явка", alias="departmentId")
    department_name: StrictStr = Field(description="Название подразделения, в котором произошла явка", alias="departmentName")
    payment_details: Optional[PaymentDetailsXml] = Field(default=None, alias="paymentDetails")
    created: Optional[datetime] = Field(default=None, description="Дата создания (при импорте игнорируется)")
    modified: Optional[datetime] = Field(default=None, description="Дата последней правки (при импорте игнорируется)")
    user_modified: Optional[StrictStr] = Field(default=None, description="Автор последней правки (при импорте игнорируется)", alias="userModified")
    __properties: ClassVar[List[str]] = ["id", "employeeId", "roleId", "attendanceTypeId", "attendanceType", "comment", "dateFrom", "dateTo", "personalDateFrom", "personalDateTo", "departmentId", "departmentName", "paymentDetails", "created", "modified", "userModified"]
    
    # XML metadata for the class
    __xml_config: ClassVar[Dict[str, Any]] = {
        "root_name": "attendance",
        
        
        "field_configs": {
            "id": {
                "xml_name": "id",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "employee_id": {
                "xml_name": "employeeId",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "role_id": {
                "xml_name": "roleId",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "attendance_type_id": {
                "xml_name": "attendanceTypeId",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "attendance_type": {
                "xml_name": "attendanceType",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "comment": {
                "xml_name": "comment",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "date_from": {
                "xml_name": "dateFrom",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "date_to": {
                "xml_name": "dateTo",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "personal_date_from": {
                "xml_name": "personalDateFrom",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "personal_date_to": {
                "xml_name": "personalDateTo",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "department_id": {
                "xml_name": "departmentId",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "department_name": {
                "xml_name": "departmentName",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "payment_details": {
                "xml_name": "paymentDetails",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "created": {
                "xml_name": "created",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "modified": {
                "xml_name": "modified",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "user_modified": {
                "xml_name": "userModified",
                
                
                "is_attribute": False,
                "is_wrapped": False
            }
        }
    }


    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of AttendanceXml from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of payment_details
        if self.payment_details:
            _dict['paymentDetails'] = self.payment_details.to_dict()
        # set to None if id (nullable) is None
        # and model_fields_set contains the field
        if self.id is None and "id" in self.model_fields_set:
            _dict['id'] = None

        # set to None if role_id (nullable) is None
        # and model_fields_set contains the field
        if self.role_id is None and "role_id" in self.model_fields_set:
            _dict['roleId'] = None

        # set to None if attendance_type_id (nullable) is None
        # and model_fields_set contains the field
        if self.attendance_type_id is None and "attendance_type_id" in self.model_fields_set:
            _dict['attendanceTypeId'] = None

        # set to None if attendance_type (nullable) is None
        # and model_fields_set contains the field
        if self.attendance_type is None and "attendance_type" in self.model_fields_set:
            _dict['attendanceType'] = None

        # set to None if comment (nullable) is None
        # and model_fields_set contains the field
        if self.comment is None and "comment" in self.model_fields_set:
            _dict['comment'] = None

        # set to None if date_from (nullable) is None
        # and model_fields_set contains the field
        if self.date_from is None and "date_from" in self.model_fields_set:
            _dict['dateFrom'] = None

        # set to None if date_to (nullable) is None
        # and model_fields_set contains the field
        if self.date_to is None and "date_to" in self.model_fields_set:
            _dict['dateTo'] = None

        # set to None if personal_date_from (nullable) is None
        # and model_fields_set contains the field
        if self.personal_date_from is None and "personal_date_from" in self.model_fields_set:
            _dict['personalDateFrom'] = None

        # set to None if personal_date_to (nullable) is None
        # and model_fields_set contains the field
        if self.personal_date_to is None and "personal_date_to" in self.model_fields_set:
            _dict['personalDateTo'] = None

        # set to None if created (nullable) is None
        # and model_fields_set contains the field
        if self.created is None and "created" in self.model_fields_set:
            _dict['created'] = None

        # set to None if modified (nullable) is None
        # and model_fields_set contains the field
        if self.modified is None and "modified" in self.model_fields_set:
            _dict['modified'] = None

        # set to None if user_modified (nullable) is None
        # and model_fields_set contains the field
        if self.user_modified is None and "user_modified" in self.model_fields_set:
            _dict['userModified'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of AttendanceXml from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "id": obj.get("id"),
            "employeeId": obj.get("employeeId"),
            "roleId": obj.get("roleId"),
            "attendanceTypeId": obj.get("attendanceTypeId"),
            "attendanceType": obj.get("attendanceType"),
            "comment": obj.get("comment"),
            "dateFrom": obj.get("dateFrom"),
            "dateTo": obj.get("dateTo"),
            "personalDateFrom": obj.get("personalDateFrom"),
            "personalDateTo": obj.get("personalDateTo"),
            "departmentId": obj.get("departmentId"),
            "departmentName": obj.get("departmentName"),
            "paymentDetails": PaymentDetailsXml.from_dict(obj["paymentDetails"]) if obj.get("paymentDetails") is not None else None,
            "created": obj.get("created"),
            "modified": obj.get("modified"),
            "userModified": obj.get("userModified")
        })
        return _obj



    @classmethod
    def from_xml(cls, xml_str: str) -> Optional[Self]:
        """Create an instance of AttendanceXml from an XML string"""
        try:
            if isinstance(xml_str, str):
                xml_str = xml_str.encode('utf-8')
            root = etree.fromstring(xml_str)
            return cls.from_xml_element(root)
        except Exception as e:
            raise ValueError(f"Failed to parse XML: {e}")

    @classmethod
    def from_xml_element(cls, element: Element) -> Optional[Self]:
        """Create an instance of AttendanceXml from an XML element"""
        return cls._from_xml_element_internal(element)

    @classmethod
    def _from_xml_element_internal(cls, element: Element) -> Optional[Self]:
        """Internal method to create instance from XML element"""
        if element is None:
            return None

        data = {}
        
        # Process field: id (XML name: id)
        # Handle XML element: id
        # Single element field: id
        child_element = element.find("id")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["id"] = text_value
                
        
        # Process field: employee_id (XML name: employeeId)
        # Handle XML element: employee_id
        # Single element field: employee_id
        child_element = element.find("employeeId")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["employeeId"] = text_value
                
        
        # Process field: role_id (XML name: roleId)
        # Handle XML element: role_id
        # Single element field: role_id
        child_element = element.find("roleId")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["roleId"] = text_value
                
        
        # Process field: attendance_type_id (XML name: attendanceTypeId)
        # Handle XML element: attendance_type_id
        # Single element field: attendance_type_id
        child_element = element.find("attendanceTypeId")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["attendanceTypeId"] = text_value
                
        
        # Process field: attendance_type (XML name: attendanceType)
        # Handle XML element: attendance_type
        # Single element field: attendance_type
        child_element = element.find("attendanceType")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["attendanceType"] = text_value
                
        
        # Process field: comment (XML name: comment)
        # Handle XML element: comment
        # Single element field: comment
        child_element = element.find("comment")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["comment"] = text_value
                
        
        # Process field: date_from (XML name: dateFrom)
        # Handle XML element: date_from
        # Single element field: date_from
        child_element = element.find("dateFrom")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["dateFrom"] = text_value
                
        
        # Process field: date_to (XML name: dateTo)
        # Handle XML element: date_to
        # Single element field: date_to
        child_element = element.find("dateTo")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["dateTo"] = text_value
                
        
        # Process field: personal_date_from (XML name: personalDateFrom)
        # Handle XML element: personal_date_from
        # Single element field: personal_date_from
        child_element = element.find("personalDateFrom")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                
                # Handle other primitive types (date, datetime, uuid, etc.)
                # DateTime field - handle "null" specially  
                if text_value == "null":
                    data["personalDateFrom"] = None
                else:
                    data["personalDateFrom"] = text_value
                
                
        
        # Process field: personal_date_to (XML name: personalDateTo)
        # Handle XML element: personal_date_to
        # Single element field: personal_date_to
        child_element = element.find("personalDateTo")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                
                # Handle other primitive types (date, datetime, uuid, etc.)
                # DateTime field - handle "null" specially  
                if text_value == "null":
                    data["personalDateTo"] = None
                else:
                    data["personalDateTo"] = text_value
                
                
        
        # Process field: department_id (XML name: departmentId)
        # Handle XML element: department_id
        # Single element field: department_id
        child_element = element.find("departmentId")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["departmentId"] = text_value
                
        
        # Process field: department_name (XML name: departmentName)
        # Handle XML element: department_name
        # Single element field: department_name
        child_element = element.find("departmentName")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["departmentName"] = text_value
                
        
        # Process field: payment_details (XML name: paymentDetails)
        # Handle XML element: payment_details
        # Single element field: payment_details
        child_element = element.find("paymentDetails")
        if child_element is not None:
            # Complex object field
            nested_obj = PaymentDetailsXml.from_xml_element(child_element)
            if nested_obj is not None:
                data["paymentDetails"] = nested_obj
        
        # Process field: created (XML name: created)
        # Handle XML element: created
        # Single element field: created
        child_element = element.find("created")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                
                # Handle other primitive types (date, datetime, uuid, etc.)
                # DateTime field - handle "null" specially  
                if text_value == "null":
                    data["created"] = None
                else:
                    data["created"] = text_value
                
                
        
        # Process field: modified (XML name: modified)
        # Handle XML element: modified
        # Single element field: modified
        child_element = element.find("modified")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                
                # Handle other primitive types (date, datetime, uuid, etc.)
                # DateTime field - handle "null" specially  
                if text_value == "null":
                    data["modified"] = None
                else:
                    data["modified"] = text_value
                
                
        
        # Process field: user_modified (XML name: userModified)
        # Handle XML element: user_modified
        # Single element field: user_modified
        child_element = element.find("userModified")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["userModified"] = text_value
                
        
        
        return cls.from_dict(data)

    def to_xml(self) -> str:
        """Create an XML string representation of the model"""
        root_element = Element(self.__xml_config.get("root_name", self.__class__.__name__))
        
        # Populate the XML element with model data
        self._to_xml_element_internal(root_element)
        
        # Convert to string with XML declaration
        return etree.tostring(root_element, encoding="utf-8", pretty_print=True, xml_declaration=True).decode("utf-8")

    def _to_xml_element_internal(self, element: Element) -> None:
        """Internal method to populate an XML element with model data"""
        
        # Process field: id (XML name: id)
        value = getattr(self, "id", None)
        if value is not None:  # Skip None values
            # Single element field: id
            child_element = SubElement(element, "id")
            child_element.text = value
            
        # Process field: employee_id (XML name: employeeId)
        value = getattr(self, "employee_id", None)
        if value is not None:  # Skip None values
            # Single element field: employee_id
            child_element = SubElement(element, "employeeId")
            child_element.text = value
            
        # Process field: role_id (XML name: roleId)
        value = getattr(self, "role_id", None)
        if value is not None:  # Skip None values
            # Single element field: role_id
            child_element = SubElement(element, "roleId")
            child_element.text = value
            
        # Process field: attendance_type_id (XML name: attendanceTypeId)
        value = getattr(self, "attendance_type_id", None)
        if value is not None:  # Skip None values
            # Single element field: attendance_type_id
            child_element = SubElement(element, "attendanceTypeId")
            child_element.text = value
            
        # Process field: attendance_type (XML name: attendanceType)
        value = getattr(self, "attendance_type", None)
        if value is not None:  # Skip None values
            # Single element field: attendance_type
            child_element = SubElement(element, "attendanceType")
            child_element.text = value
            
        # Process field: comment (XML name: comment)
        value = getattr(self, "comment", None)
        if value is not None:  # Skip None values
            # Single element field: comment
            child_element = SubElement(element, "comment")
            child_element.text = value
            
        # Process field: date_from (XML name: dateFrom)
        value = getattr(self, "date_from", None)
        if value is not None:  # Skip None values
            # Single element field: date_from
            child_element = SubElement(element, "dateFrom")
            child_element.text = value
            
        # Process field: date_to (XML name: dateTo)
        value = getattr(self, "date_to", None)
        if value is not None:  # Skip None values
            # Single element field: date_to
            child_element = SubElement(element, "dateTo")
            child_element.text = value
            
        # Process field: personal_date_from (XML name: personalDateFrom)
        value = getattr(self, "personal_date_from", None)
        if value is not None:  # Skip None values
            # Single element field: personal_date_from
            child_element = SubElement(element, "personalDateFrom")
            
            # Handle other primitive types (date, datetime, uuid, etc.)
            child_element.text = str(value)
            
        # Process field: personal_date_to (XML name: personalDateTo)
        value = getattr(self, "personal_date_to", None)
        if value is not None:  # Skip None values
            # Single element field: personal_date_to
            child_element = SubElement(element, "personalDateTo")
            
            # Handle other primitive types (date, datetime, uuid, etc.)
            child_element.text = str(value)
            
        # Process field: department_id (XML name: departmentId)
        value = getattr(self, "department_id", None)
        if value is not None:  # Skip None values
            # Single element field: department_id
            child_element = SubElement(element, "departmentId")
            child_element.text = value
            
        # Process field: department_name (XML name: departmentName)
        value = getattr(self, "department_name", None)
        if value is not None:  # Skip None values
            # Single element field: department_name
            child_element = SubElement(element, "departmentName")
            child_element.text = value
            
        # Process field: payment_details (XML name: paymentDetails)
        value = getattr(self, "payment_details", None)
        if value is not None:  # Skip None values
            # Single element field: payment_details
            child_element = SubElement(element, "paymentDetails")
            # Complex object field
            if hasattr(value, "_to_xml_element_internal"):
                value._to_xml_element_internal(child_element)
        # Process field: created (XML name: created)
        value = getattr(self, "created", None)
        if value is not None:  # Skip None values
            # Single element field: created
            child_element = SubElement(element, "created")
            
            # Handle other primitive types (date, datetime, uuid, etc.)
            child_element.text = str(value)
            
        # Process field: modified (XML name: modified)
        value = getattr(self, "modified", None)
        if value is not None:  # Skip None values
            # Single element field: modified
            child_element = SubElement(element, "modified")
            
            # Handle other primitive types (date, datetime, uuid, etc.)
            child_element.text = str(value)
            
        # Process field: user_modified (XML name: userModified)
        value = getattr(self, "user_modified", None)
        if value is not None:  # Skip None values
            # Single element field: user_modified
            child_element = SubElement(element, "userModified")
            child_element.text = value
            


