# coding: utf-8

"""
    iikoServer API

    API Server позволяет сделать интеграции, которые предназначены для работы с номенклатурой, отчетами, документами, данными сотрудников и приказами.   ### Ограничения и рекомендации - Запросы должны выполняться последовательно друг за другом - Запрашивайте данные за период не длиннее одного месяца, в идеале — за один день или неделю - Для OLAPv2-отчетов рекомендуется устанавливать build-summary=false - При построении OLAPv2-отчета рекомендуется использовать не более 7 полей - Перед выполнением запросов проверяйте их на демо сервере

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, field_validator
from typing import Any, ClassVar, Dict, List
from typing_extensions import Annotated
from typing import Optional, Set, Union
from typing_extensions import Self

# XML support imports
from lxml import etree
from lxml.etree import Element, SubElement

class PeriodScheduleItemDto(BaseModel):
    """
    Период действия (интервал) по дням недели
    """ # noqa: E501
    begin: Annotated[str, Field(strict=True)] = Field(description="Начало полуинтервала в виде \"HH:mm\"")
    end: Annotated[str, Field(strict=True)] = Field(description="Конец полуинтервала в виде \"HH:mm\"")
    days_of_week: List[Annotated[int, Field(le=7, strict=True, ge=1)]] = Field(description="Дни недели, в которых действует интервал", alias="daysOfWeek")
    __properties: ClassVar[List[str]] = ["begin", "end", "daysOfWeek"]
    
    # XML metadata for the class
    __xml_config: ClassVar[Dict[str, Any]] = {
        "root_name": "PeriodScheduleItemDto",
        
        
        "field_configs": {
            "begin": {
                "xml_name": "begin",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "end": {
                "xml_name": "end",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "days_of_week": {
                "xml_name": "daysOfWeek",
                
                
                "is_attribute": False,
                "is_wrapped": False
            }
        }
    }


    @field_validator('begin')
    def begin_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if not re.match(r"^([0-1]?[0-9]|2[0-3]):[0-5][0-9]$", value):
            raise ValueError(r"must validate the regular expression /^([0-1]?[0-9]|2[0-3]):[0-5][0-9]$/")
        return value

    @field_validator('end')
    def end_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if not re.match(r"^([0-1]?[0-9]|2[0-3]):[0-5][0-9]$", value):
            raise ValueError(r"must validate the regular expression /^([0-1]?[0-9]|2[0-3]):[0-5][0-9]$/")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of PeriodScheduleItemDto from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of PeriodScheduleItemDto from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "begin": obj.get("begin"),
            "end": obj.get("end"),
            "daysOfWeek": obj.get("daysOfWeek")
        })
        return _obj



    @classmethod
    def from_xml(cls, xml_str: str) -> Optional[Self]:
        """Create an instance of PeriodScheduleItemDto from an XML string"""
        try:
            if isinstance(xml_str, str):
                xml_str = xml_str.encode('utf-8')
            root = etree.fromstring(xml_str)
            return cls.from_xml_element(root)
        except Exception as e:
            raise ValueError(f"Failed to parse XML: {e}")

    @classmethod
    def from_xml_element(cls, element: Element) -> Optional[Self]:
        """Create an instance of PeriodScheduleItemDto from an XML element"""
        return cls._from_xml_element_internal(element)

    @classmethod
    def _from_xml_element_internal(cls, element: Element) -> Optional[Self]:
        """Internal method to create instance from XML element"""
        if element is None:
            return None

        data = {}
        
        # Process field: begin (XML name: begin)
        # Handle XML element: begin
        # Single element field: begin
        child_element = element.find("begin")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["begin"] = text_value
                
        
        # Process field: end (XML name: end)
        # Handle XML element: end
        # Single element field: end
        child_element = element.find("end")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["end"] = text_value
                
        
        # Process field: days_of_week (XML name: daysOfWeek)
        # Handle XML element: days_of_week
        # Array field: days_of_week
        # Non-wrapped array - direct elements
        # Primitive items in non-wrapped array
        child_elements = element.findall("daysOfWeek")
        items = []
        for child in child_elements:
            text_value = child.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    items.append(int(text_value))
                except ValueError:
                    items.append(text_value)
                
        data["daysOfWeek"] = items
        
        
        return cls.from_dict(data)

    def to_xml(self) -> str:
        """Create an XML string representation of the model"""
        root_element = Element(self.__xml_config.get("root_name", self.__class__.__name__))
        
        # Populate the XML element with model data
        self._to_xml_element_internal(root_element)
        
        # Convert to string with XML declaration
        return etree.tostring(root_element, encoding="utf-8", pretty_print=True, xml_declaration=True).decode("utf-8")

    def _to_xml_element_internal(self, element: Element) -> None:
        """Internal method to populate an XML element with model data"""
        
        # Process field: begin (XML name: begin)
        value = getattr(self, "begin", None)
        if value is not None:  # Skip None values
            # Single element field: begin
            child_element = SubElement(element, "begin")
            child_element.text = value
            
        # Process field: end (XML name: end)
        value = getattr(self, "end", None)
        if value is not None:  # Skip None values
            # Single element field: end
            child_element = SubElement(element, "end")
            child_element.text = value
            
        # Process field: days_of_week (XML name: daysOfWeek)
        value = getattr(self, "days_of_week", None)
        if value is not None:  # Skip None values
            # Array field: days_of_week
            # Non-wrapped array - add elements directly
            for item in value:
                # Primitive items in non-wrapped array
                item_element = SubElement(element, "daysOfWeek")
                item_element.text = str(item)
                


