# coding: utf-8

"""
    iikoServer API

    API Server позволяет сделать интеграции, которые предназначены для работы с номенклатурой, отчетами, документами, данными сотрудников и приказами.   ### Ограничения и рекомендации - Запросы должны выполняться последовательно друг за другом - Запрашивайте данные за период не длиннее одного месяца, в идеале — за один день или неделю - Для OLAPv2-отчетов рекомендуется устанавливать build-summary=false - При построении OLAPv2-отчета рекомендуется использовать не более 7 полей - Перед выполнением запросов проверяйте их на демо сервере

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictFloat, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional, Union
from iikoserver_client.models.inventory_item_status_enum import InventoryItemStatusEnum
from typing import Optional, Set, Union
from typing_extensions import Self

# XML support imports
from lxml import etree
from lxml.etree import Element, SubElement

class IncomingInventoryItemXml(BaseModel):
    """
    IncomingInventoryItemXml
    """ # noqa: E501
    status: Optional[InventoryItemStatusEnum] = None
    recalculation_number: Optional[StrictInt] = Field(default=None, description="Порядковый номер пересчета остатков по элементу номенклатуры. Нумерация с нуля. При импорте игнорируется.", alias="recalculationNumber")
    product_id: Optional[StrictStr] = Field(default=None, description="Элемент номенклатуры (id)", alias="productId")
    product_article: Optional[StrictStr] = Field(default=None, description="Элемент номенклатуры (артикул)", alias="productArticle")
    container_id: Optional[StrictStr] = Field(default=None, description="Фасовка (id)", alias="containerId")
    container_code: Optional[StrictStr] = Field(default=None, description="Фасовка (код/артикул)", alias="containerCode")
    amount_container: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Количество в фасовках", alias="amountContainer")
    amount_gross: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Вес с тарой. Информационное поле, используется только для отображения в бекофисе.", alias="amountGross")
    producer_id: Optional[StrictStr] = Field(default=None, description="Производитель или импортер товара. Используется в российской алкогольной декларации.", alias="producerId")
    comment: Optional[StrictStr] = Field(default=None, description="Произвольный комментарий к строке документа")
    __properties: ClassVar[List[str]] = ["status", "recalculationNumber", "productId", "productArticle", "containerId", "containerCode", "amountContainer", "amountGross", "producerId", "comment"]
    
    # XML metadata for the class
    __xml_config: ClassVar[Dict[str, Any]] = {
        "root_name": "IncomingInventoryItemXml",
        
        
        "field_configs": {
            "status": {
                "xml_name": "status",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "recalculation_number": {
                "xml_name": "recalculationNumber",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "product_id": {
                "xml_name": "productId",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "product_article": {
                "xml_name": "productArticle",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "container_id": {
                "xml_name": "containerId",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "container_code": {
                "xml_name": "containerCode",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "amount_container": {
                "xml_name": "amountContainer",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "amount_gross": {
                "xml_name": "amountGross",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "producer_id": {
                "xml_name": "producerId",
                
                
                "is_attribute": False,
                "is_wrapped": False
            },
            "comment": {
                "xml_name": "comment",
                
                
                "is_attribute": False,
                "is_wrapped": False
            }
        }
    }


    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of IncomingInventoryItemXml from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # set to None if recalculation_number (nullable) is None
        # and model_fields_set contains the field
        if self.recalculation_number is None and "recalculation_number" in self.model_fields_set:
            _dict['recalculationNumber'] = None

        # set to None if product_id (nullable) is None
        # and model_fields_set contains the field
        if self.product_id is None and "product_id" in self.model_fields_set:
            _dict['productId'] = None

        # set to None if product_article (nullable) is None
        # and model_fields_set contains the field
        if self.product_article is None and "product_article" in self.model_fields_set:
            _dict['productArticle'] = None

        # set to None if container_id (nullable) is None
        # and model_fields_set contains the field
        if self.container_id is None and "container_id" in self.model_fields_set:
            _dict['containerId'] = None

        # set to None if container_code (nullable) is None
        # and model_fields_set contains the field
        if self.container_code is None and "container_code" in self.model_fields_set:
            _dict['containerCode'] = None

        # set to None if amount_container (nullable) is None
        # and model_fields_set contains the field
        if self.amount_container is None and "amount_container" in self.model_fields_set:
            _dict['amountContainer'] = None

        # set to None if amount_gross (nullable) is None
        # and model_fields_set contains the field
        if self.amount_gross is None and "amount_gross" in self.model_fields_set:
            _dict['amountGross'] = None

        # set to None if producer_id (nullable) is None
        # and model_fields_set contains the field
        if self.producer_id is None and "producer_id" in self.model_fields_set:
            _dict['producerId'] = None

        # set to None if comment (nullable) is None
        # and model_fields_set contains the field
        if self.comment is None and "comment" in self.model_fields_set:
            _dict['comment'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of IncomingInventoryItemXml from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "status": obj.get("status"),
            "recalculationNumber": obj.get("recalculationNumber"),
            "productId": obj.get("productId"),
            "productArticle": obj.get("productArticle"),
            "containerId": obj.get("containerId"),
            "containerCode": obj.get("containerCode"),
            "amountContainer": obj.get("amountContainer"),
            "amountGross": obj.get("amountGross"),
            "producerId": obj.get("producerId"),
            "comment": obj.get("comment")
        })
        return _obj



    @classmethod
    def from_xml(cls, xml_str: str) -> Optional[Self]:
        """Create an instance of IncomingInventoryItemXml from an XML string"""
        try:
            if isinstance(xml_str, str):
                xml_str = xml_str.encode('utf-8')
            root = etree.fromstring(xml_str)
            return cls.from_xml_element(root)
        except Exception as e:
            raise ValueError(f"Failed to parse XML: {e}")

    @classmethod
    def from_xml_element(cls, element: Element) -> Optional[Self]:
        """Create an instance of IncomingInventoryItemXml from an XML element"""
        return cls._from_xml_element_internal(element)

    @classmethod
    def _from_xml_element_internal(cls, element: Element) -> Optional[Self]:
        """Internal method to create instance from XML element"""
        if element is None:
            return None

        data = {}
        
        # Process field: status (XML name: status)
        # Handle XML element: status
        # Single element field: status
        child_element = element.find("status")
        if child_element is not None:
            # Enum field
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["status"] = text_value
        
        # Process field: recalculation_number (XML name: recalculationNumber)
        # Handle XML element: recalculation_number
        # Single element field: recalculation_number
        child_element = element.find("recalculationNumber")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["recalculationNumber"] = int(text_value)
                except ValueError:
                    data["recalculationNumber"] = text_value
                
        
        # Process field: product_id (XML name: productId)
        # Handle XML element: product_id
        # Single element field: product_id
        child_element = element.find("productId")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["productId"] = text_value
                
        
        # Process field: product_article (XML name: productArticle)
        # Handle XML element: product_article
        # Single element field: product_article
        child_element = element.find("productArticle")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["productArticle"] = text_value
                
        
        # Process field: container_id (XML name: containerId)
        # Handle XML element: container_id
        # Single element field: container_id
        child_element = element.find("containerId")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["containerId"] = text_value
                
        
        # Process field: container_code (XML name: containerCode)
        # Handle XML element: container_code
        # Single element field: container_code
        child_element = element.find("containerCode")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["containerCode"] = text_value
                
        
        # Process field: amount_container (XML name: amountContainer)
        # Handle XML element: amount_container
        # Single element field: amount_container
        child_element = element.find("amountContainer")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["amountContainer"] = float(text_value)
                except ValueError:
                    data["amountContainer"] = text_value
                
        
        # Process field: amount_gross (XML name: amountGross)
        # Handle XML element: amount_gross
        # Single element field: amount_gross
        child_element = element.find("amountGross")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                try:
                    data["amountGross"] = float(text_value)
                except ValueError:
                    data["amountGross"] = text_value
                
        
        # Process field: producer_id (XML name: producerId)
        # Handle XML element: producer_id
        # Single element field: producer_id
        child_element = element.find("producerId")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["producerId"] = text_value
                
        
        # Process field: comment (XML name: comment)
        # Handle XML element: comment
        # Single element field: comment
        child_element = element.find("comment")
        if child_element is not None:
            # Check for null or empty values
            text_value = child_element.text
            if text_value is not None and text_value.strip() != "" and text_value != "null":
                data["comment"] = text_value
                
        
        
        return cls.from_dict(data)

    def to_xml(self) -> str:
        """Create an XML string representation of the model"""
        root_element = Element(self.__xml_config.get("root_name", self.__class__.__name__))
        
        # Populate the XML element with model data
        self._to_xml_element_internal(root_element)
        
        # Convert to string with XML declaration
        return etree.tostring(root_element, encoding="utf-8", pretty_print=True, xml_declaration=True).decode("utf-8")

    def _to_xml_element_internal(self, element: Element) -> None:
        """Internal method to populate an XML element with model data"""
        
        # Process field: status (XML name: status)
        value = getattr(self, "status", None)
        if value is not None:  # Skip None values
            # Single element field: status
            child_element = SubElement(element, "status")
            # Enum field
            child_element.text = str(value)
        # Process field: recalculation_number (XML name: recalculationNumber)
        value = getattr(self, "recalculation_number", None)
        if value is not None:  # Skip None values
            # Single element field: recalculation_number
            child_element = SubElement(element, "recalculationNumber")
            child_element.text = str(value)
            
        # Process field: product_id (XML name: productId)
        value = getattr(self, "product_id", None)
        if value is not None:  # Skip None values
            # Single element field: product_id
            child_element = SubElement(element, "productId")
            child_element.text = value
            
        # Process field: product_article (XML name: productArticle)
        value = getattr(self, "product_article", None)
        if value is not None:  # Skip None values
            # Single element field: product_article
            child_element = SubElement(element, "productArticle")
            child_element.text = value
            
        # Process field: container_id (XML name: containerId)
        value = getattr(self, "container_id", None)
        if value is not None:  # Skip None values
            # Single element field: container_id
            child_element = SubElement(element, "containerId")
            child_element.text = value
            
        # Process field: container_code (XML name: containerCode)
        value = getattr(self, "container_code", None)
        if value is not None:  # Skip None values
            # Single element field: container_code
            child_element = SubElement(element, "containerCode")
            child_element.text = value
            
        # Process field: amount_container (XML name: amountContainer)
        value = getattr(self, "amount_container", None)
        if value is not None:  # Skip None values
            # Single element field: amount_container
            child_element = SubElement(element, "amountContainer")
            child_element.text = str(value)
            
        # Process field: amount_gross (XML name: amountGross)
        value = getattr(self, "amount_gross", None)
        if value is not None:  # Skip None values
            # Single element field: amount_gross
            child_element = SubElement(element, "amountGross")
            child_element.text = str(value)
            
        # Process field: producer_id (XML name: producerId)
        value = getattr(self, "producer_id", None)
        if value is not None:  # Skip None values
            # Single element field: producer_id
            child_element = SubElement(element, "producerId")
            child_element.text = value
            
        # Process field: comment (XML name: comment)
        value = getattr(self, "comment", None)
        if value is not None:  # Skip None values
            # Single element field: comment
            child_element = SubElement(element, "comment")
            child_element.text = value
            


